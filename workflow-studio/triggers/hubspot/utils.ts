/**
 * HubSpot trigger utilities stub — stripped during Sim Studio fork.
 */

export function isHubSpotContactEventMatch(
  _triggerId: string,
  _subscriptionType: string
): boolean {
  return true
}
