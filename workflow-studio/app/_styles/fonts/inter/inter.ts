/** Inter font stub — loaded via system font stack instead of next/font/google to avoid build-time fetch */
export const inter = {
  variable: 'font-inter',
  className: 'font-inter',
  style: { fontFamily: 'Inter, system-ui, sans-serif' },
}
