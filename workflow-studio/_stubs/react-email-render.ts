/** @react-email/render stub — email rendering not used in local mode. */
export function render(_element: any, _options?: any): string {
  return ''
}

export function renderAsync(_element: any, _options?: any): Promise<string> {
  return Promise.resolve('')
}
