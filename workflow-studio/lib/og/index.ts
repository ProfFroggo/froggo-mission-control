/**
 * OG image stub — local mode, no image generation.
 */
export const OG_IMAGE_WIDTH = 1200
export const OG_IMAGE_HEIGHT = 630

export async function captureAndUploadOGImage(..._args: any[]): Promise<string | null> {
  return null
}
