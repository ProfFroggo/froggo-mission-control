/**
 * OAuth types stub — stripped during Sim Studio fork.
 * Re-exports from the existing index.ts for convenience.
 */

export type { OAuthProvider, OAuthService, OAuthServiceConfig, Credential } from '@/lib/oauth/index'
