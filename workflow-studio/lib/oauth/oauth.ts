/**
 * OAuth providers stub — stripped during Sim Studio fork.
 * Re-exports from the existing index.ts for convenience.
 */

import { OAUTH_PROVIDERS } from '@/lib/oauth/index'

export { OAUTH_PROVIDERS }
