---
phase: 50-agent-id-validation
plan: 02
type: execute
domain: security
---

<objective>
Apply validateAgentId to all remaining agent routes, replace the avatar route's inline validation with the shared util, and run a grep sweep to confirm no routes were missed.

Purpose: Defence-in-depth — every route that receives an agent ID from the URL or request body should validate it at the entry point, even if the route only does DB operations. Consistent validation at all boundaries.
Output: validateAgentId applied to catalog, hire, avatar, kill, config, session, status, models routes + grep sweep confirming full coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-agent-id-validation/50-01-SUMMARY.md
@src/lib/validateId.ts
@app/api/agents/[id]/avatar/route.ts
@app/api/agents/[id]/kill/route.ts
@app/api/agents/[id]/config/route.ts
@app/api/agents/[id]/session/route.ts
@app/api/agents/[id]/status/route.ts
@app/api/agents/[id]/models/route.ts
@app/api/catalog/agents/[id]/route.ts
@app/api/agents/hire/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply validation to catalog and hire routes (filesystem operations)</name>
  <files>
    app/api/catalog/agents/[id]/route.ts,
    app/api/agents/hire/route.ts
  </files>
  <action>
**catalog/agents/[id]/route.ts** — This route does filesystem operations (archive/rename workspace dir using id). Add validation guard:
- In GET handler: after extracting id from params, add `const guard = validateAgentId(id); if (guard) return guard;`
- In PATCH handler: same pattern after extracting id
- In DELETE handler: same pattern — this one does `join(HOME, 'mission-control', 'agents', id)` and archive renaming, so validation is most important here

Import `validateAgentId` from `@/lib/validateId`.

**hire/route.ts** — The id comes from the REQUEST BODY (not URL params), so validate the body value. After `const { id, name, ... } = await request.json()` and the existing `if (!id || !name)` check, add: `const guard = validateAgentId(id); if (guard) return guard;`. This is defence-in-depth since hire creates the workspace directory at `join(AGENTS_DIR, id)`.

Import `validateAgentId` from `@/lib/validateId`.
  </action>
  <verify>`npx tsc --noEmit 2>&1 | head -20` — zero errors</verify>
  <done>Both catalog and hire routes import and call validateAgentId, returning 400 for invalid IDs before any filesystem operations.</done>
</task>

<task type="auto">
  <name>Task 2: Apply validation to remaining routes + replace avatar inline check + grep sweep</name>
  <files>
    app/api/agents/[id]/avatar/route.ts,
    app/api/agents/[id]/kill/route.ts,
    app/api/agents/[id]/config/route.ts,
    app/api/agents/[id]/session/route.ts,
    app/api/agents/[id]/status/route.ts,
    app/api/agents/[id]/models/route.ts
  </files>
  <action>
For each route, read the file first, then add validation.

**avatar/route.ts** — Has existing inline validation `if (!/^[a-z0-9_-]+$/i.test(id))`. Replace it with the shared util:
- Add import: `import { validateAgentId } from '@/lib/validateId';`
- Replace the inline check with: `const guard = validateAgentId(id); if (guard) return new NextResponse(null, { status: 400 });` — keep the 400 with null body to match existing behaviour (avatar returns null body not JSON)

**kill/route.ts, config/route.ts, session/route.ts, status/route.ts, models/route.ts** — These routes only do DB operations (parameterized queries), so SQL injection is not a risk. But validate for defence-in-depth:
- Add import `validateAgentId` from `@/lib/validateId`
- Add guard immediately after extracting id from params: `const guard = validateAgentId(id); if (guard) return guard;`
- For each route, read the file first to understand the exact handler signature before editing

After all edits, run a grep sweep to confirm no agent routes are missing validation:
```bash
grep -rL "validateAgentId\|!/\^" app/api/agents/
```
This lists files that have neither the shared util nor any inline validation regex. Review the output — any route that constructs file paths from id and is not in the list needs to be added. Routes that only have `[id]` in URL but do zero filesystem ops with id (only DB) and already have the guard from this task are fine.

Run build to confirm zero TS errors: `npm run build 2>&1 | tail -10`
  </action>
  <verify>
1. `grep -rL "validateAgentId" app/api/agents/[id]/` — should return empty (all covered) or only routes confirmed to do zero filesystem ops with id
2. `npx tsc --noEmit` — zero errors
3. `npm run build 2>&1 | tail -5` — build succeeds
  </verify>
  <done>
- avatar route uses shared validateAgentId (inline check removed)
- kill, config, session, status, models routes all validate id at entry point
- grep sweep confirms full coverage across all app/api/agents/[id]/ routes
- Build passes with zero TypeScript errors
- Phase 50 complete
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `npm run build 2>&1 | tail -5` — succeeds
- [ ] `grep -rn "validateAgentId" app/api/agents/` — shows hits in all [id] route files
- [ ] `grep -n 'kevin.macarthur' app/api/agents/\[id\]/soul/route.ts` — returns nothing (from plan 01)
- [ ] `grep -n 'execSync' app/api/agents/\[id\]/spawn/route.ts` — returns nothing (from plan 01)
- [ ] `grep -rn "validateAgentId" app/api/catalog/agents/` — shows hit in catalog route
- [ ] `grep -n "validateAgentId" app/api/agents/hire/route.ts` — shows hit
</verification>

<success_criteria>
- All app/api/agents/[id]/* routes validate the id at entry point using validateAgentId or inline equivalent
- catalog/agents/[id]/route.ts validates id before filesystem operations
- hire/route.ts validates id from request body before workspace creation
- avatar/route.ts uses shared util instead of inline check
- Grep sweep confirms no routes missed
- TypeScript compiles clean, build passes
- Phase 50 complete — all agent ID validation in place
</success_criteria>

<output>
After completion, create `.planning/phases/50-agent-id-validation/50-02-SUMMARY.md`:

# Phase 50 Plan 02: Agent ID Validation — All Routes Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Phase 50 complete. Ready for Phase 51: path-traversal-library.
`/gsd:plan-phase 51`
</output>
