# Plan 03-04: Analytics + Marketplace API Routes + Verification

**Phase**: 3 — API Routes
**Goal**: Analytics, marketplace, and full route verification

## Tasks

### Task 1: Analytics + Marketplace routes
`app/api/analytics/token-usage/route.ts`, `task-stats/route.ts`, `agent-activity/route.ts`, `events/route.ts`
`app/api/marketplace/agents/route.ts`, `modules/route.ts`

### Task 2: Verify all routes
`npx tsc --noEmit` passes. Dev server starts. curl spot-checks return valid JSON.
