---
phase: 07-gsd-agent-planning
plan: 01
type: execute
---

<objective>
Give agents structured GSD-style planning for projects — phases, milestones, progress tracking.

Purpose: Agents can break large project work into phases with measurable milestones.
Output: project_phases table, MCP planning tools, enhanced planning instructions in agent prompt.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add project_phases table + API</name>
  <files>src/lib/database.ts, app/api/projects/[id]/phases/route.ts</files>
  <action>
  1. In database.ts ensureTables(), add project_phases table:
     - id TEXT PK, projectId TEXT FK, title TEXT, description TEXT, status TEXT (planned/in-progress/complete), assignedTo TEXT (agent), order INTEGER, createdAt/updatedAt INTEGER
  2. Create API route app/api/projects/[id]/phases/route.ts with GET (list phases), POST (create), PATCH (update status/assignedTo)
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>project_phases table exists, API CRUD works</done>
</task>

<task type="auto">
  <name>Task 2: Add MCP planning tools + enhance agent prompt</name>
  <files>tools/mission-control-db-mcp/src/index.ts, src/lib/taskDispatcher.ts</files>
  <action>
  1. Add 3 MCP tools to mission-control-db-mcp:
     - project_phase_list: GET /api/projects/{projectId}/phases
     - project_phase_create: POST /api/projects/{projectId}/phases
     - project_phase_update: PATCH /api/projects/{projectId}/phases?id={phaseId}
  2. Register them in the tools list with input schemas
  3. Add to TIER_TOOLS worker/trusted/admin lists in taskDispatcher.ts
  4. Add planning instructions to TASK_SUFFIX in taskDispatcher.ts:
     - "For complex tasks: break work into phases using project_phase_create"
     - "Update phase status as you complete each one"
  5. Rebuild MCP: cd tools/mission-control-db-mcp && npm run build
  </action>
  <verify>npx tsc --noEmit passes, MCP build succeeds</verify>
  <done>Agents can create/update project phases via MCP tools</done>
</task>

</tasks>
