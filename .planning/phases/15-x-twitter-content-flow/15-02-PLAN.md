---
phase: 15-x-twitter-content-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/XAgentChatPane.tsx
autonomous: true

must_haves:
  truths:
    - "Chat on Content Plan tab routes to the writer agent — not a researcher stub"
    - "Chat uses streaming (onDelta callbacks) so first token appears without waiting for full response"
    - "Chat send button uses bg-clawd-accent hover:bg-clawd-accent-dim (not btn-primary)"
  artifacts:
    - path: "src/components/XAgentChatPane.tsx"
      provides: "Agent chat pane with correct routing and streaming"
      contains: "sendChatWithCallbacks"
  key_links:
    - from: "src/components/XAgentChatPane.tsx"
      to: "gateway.sendChatWithCallbacks"
      via: "streaming chat call"
      pattern: "sendChatWithCallbacks"
    - from: "AGENT_ROUTING.plan"
      to: "writer agent"
      via: "agentId mapping"
      pattern: "plan.*writer"
---

<objective>
Verify the agent chat on Content Plan and Drafts tabs is correctly wired to the writer agent with streaming, and fix the send button styling to match the app-wide chat pattern.

Purpose: Satisfy XTW-09 (chat wired to real agent, not researcher stub) and XTW-10 (no multi-second delay before first token). Research confirmed the wiring is already correct — this plan verifies and applies a minor styling fix.
Output: Verified XAgentChatPane.tsx with correct routing and consistent send button styling.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/XAgentChatPane.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify agent routing and fix send button styling</name>
  <files>src/components/XAgentChatPane.tsx</files>
  <action>
**Verification steps (read and confirm, no changes needed if correct):**

1. Confirm `AGENT_ROUTING` on line 23 maps `plan` to `{ agentId: 'writer', displayName: 'Writer' }` — NOT researcher. This satisfies XTW-09.
2. Confirm `gateway.sendChatWithCallbacks` is used (line 149) with `onDelta` callback for streaming — this means tokens render as they arrive, satisfying XTW-10.
3. Confirm user message bubbles use `bg-clawd-accent/50 text-white` (line 265) — matches Phase 13 decision.

**Fix needed — send button styling on line 322:**

The send button currently uses `btn-primary` class:
```
className="btn-primary p-2 rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
```

Per Phase 13 decision, chat send buttons must use `bg-clawd-accent hover:bg-clawd-accent-dim`. Replace line 322's className with:
```
className="bg-clawd-accent hover:bg-clawd-accent-dim text-white p-2 rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
```

This is the ONLY code change in this plan. Everything else is verification that existing code is correct.
  </action>
  <verify>
Run `grep "agentId: 'writer'" src/components/XAgentChatPane.tsx` — confirms plan and drafts route to writer.
Run `grep "sendChatWithCallbacks" src/components/XAgentChatPane.tsx` — confirms streaming is used.
Run `grep "btn-primary" src/components/XAgentChatPane.tsx` — should return ZERO matches (old class removed).
Run `grep "bg-clawd-accent hover:bg-clawd-accent-dim" src/components/XAgentChatPane.tsx` — confirms new styling.
Run `grep "bg-clawd-accent/50 text-white" src/components/XAgentChatPane.tsx` — confirms user bubble styling.
Run `grep "onDelta" src/components/XAgentChatPane.tsx` — confirms streaming delta callback present (means first token renders immediately on arrival, not after full response).

XTW-10 "fast" definition: streaming means first token renders as soon as gateway delivers it (< 1s after model starts generating). The only unavoidable delay is session cold start on first message (expected). If `sendChatWithCallbacks` is used and `onDelta` callback updates state, this requirement is satisfied.
  </verify>
  <done>
Agent chat on plan/drafts tabs confirmed wired to writer agent with streaming callbacks. Send button uses bg-clawd-accent hover:bg-clawd-accent-dim. No researcher stub — XTW-09 and XTW-10 satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep "plan:.*writer" src/components/XAgentChatPane.tsx` — plan tab routes to writer
2. `grep "drafts:.*writer" src/components/XAgentChatPane.tsx` — drafts tab routes to writer (pattern may need grep of full AGENT_ROUTING object)
3. `grep "sendChatWithCallbacks" src/components/XAgentChatPane.tsx` — streaming confirmed
4. `grep "onDelta" src/components/XAgentChatPane.tsx` — delta streaming confirmed
5. `grep "btn-primary" src/components/XAgentChatPane.tsx` — zero matches (old class removed)
6. `grep "bg-clawd-accent hover:bg-clawd-accent-dim" src/components/XAgentChatPane.tsx` — send button fixed
</verification>

<success_criteria>
- XTW-09: AGENT_ROUTING maps plan and drafts to writer agent (not researcher)
- XTW-10: gateway.sendChatWithCallbacks with onDelta callback ensures streaming (first token appears immediately)
- Send button styling matches app-wide chat pattern (bg-clawd-accent hover:bg-clawd-accent-dim)
</success_criteria>

<output>
After completion, create `.planning/phases/15-x-twitter-content-flow/15-02-SUMMARY.md`
</output>
