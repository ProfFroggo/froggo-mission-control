---
phase: 33.1-create-agent-wizard-overhaul
plan: 01
subsystem: api + ui
tags: [hr-agent, wizard, soul-template, catalog]

requires:
  - phase: 33-01
    provides: Agent Library UI + Hire button
  - phase: 32-01
    provides: catalog REST API + POST /api/catalog/agents
provides:
  - POST /api/agents/hr/stream — HR conversational agent endpoint (claude CLI backed)
  - POST /api/catalog/agents — register custom agent in catalog (writes .catalog manifest + upserts DB)
  - catalogApi.registerAgent() in api.ts
  - v3.0 soul template in HRAgentCreationModal (YAML frontmatter + MCP tools + workspace path)
  - catalog step added to HRAgentCreationModal creation flow
affects: [34-agent-hire-wizard]

tech-stack:
  added: []
  patterns: [claude --print subprocess for HR chat, manifest + DB upsert for custom agents]

key-files:
  created: [app/api/agents/hr/stream/route.ts]
  modified: [app/api/catalog/agents/route.ts, src/lib/api.ts, src/components/HRAgentCreationModal.tsx]

key-decisions:
  - "HR stream endpoint uses claude --print with full self-contained prompt (system+history baked in by modal)"
  - "Uses claude-haiku-4-5-20251001 model for HR chat (trivial conversational task)"
  - "POST /api/catalog/agents writes .catalog/agents/{id}.json + upserts catalog_agents with installed=1"
  - "Soul template uses YAML frontmatter matching existing agent format + selects model based on capabilities"
  - "catalog step added as step 3 of 4 in HRAgentCreationModal creation flow"

issues-created: []

duration: 15min
completed: 2026-03-06
---

# Phase 33.1 Plan 01: Create Agent Wizard Overhaul Summary

**HR/stream endpoint fixed, soul template updated to v3.0, custom agents register in catalog**

## Performance
- **Duration:** 15 min
- **Completed:** 2026-03-06
- **Tasks:** 1

## Accomplishments
- `POST /api/agents/hr/stream` — spawns claude --print, pipes full prompt from modal, returns `{ response }` JSON
- `POST /api/catalog/agents` — writes `.catalog/agents/{id}.json` + INSERT OR REPLACE in `catalog_agents` with installed=1
- `catalogApi.registerAgent()` added to api.ts
- Soul template updated: YAML frontmatter (name/description/model/permissionMode/maxTurns/memory/tools/mcpServers), v3.0 MCP tools section, workspace path, model auto-selection (Sonnet for coding agents, Haiku otherwise)
- Creation flow: 4 steps (db → soul → catalog → activate)
- TypeScript clean, build passes

## Task Commits
1. **Task 1: hr/stream + catalog registration + v3.0 soul** — `8e87ecc` (feat)

## Files Created/Modified
- `app/api/agents/hr/stream/route.ts` — new endpoint
- `app/api/catalog/agents/route.ts` — POST handler added
- `src/lib/api.ts` — catalogApi.registerAgent() added
- `src/components/HRAgentCreationModal.tsx` — v3.0 soul template + catalog step

## Deviations from Plan
- Soul route writes to `.claude/agents/{id}.md` (in-project) not `~/mission-control/agents/{id}/SOUL.md` (workspace) — this is correct for agent definitions; workspace SOUL.md would be a Phase 34 enhancement for the full hire wizard

## Next Phase Readiness
- Phase 33.1 complete — wizard functional end-to-end (HR chat → soul → catalog → activate)
- Ready for Phase 34: Agent Hire Wizard (HR agent-backed workspace creation)

---
*Phase: 33.1-create-agent-wizard-overhaul*
*Completed: 2026-03-06*
