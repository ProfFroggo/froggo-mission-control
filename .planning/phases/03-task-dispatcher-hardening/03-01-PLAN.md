---
phase: 03-task-dispatcher-hardening
plan: 01
type: execute
---

<objective>
Harden the task dispatcher with process timeout, agent status tracking, and circuit breaker API.

Purpose: Prevent hung processes from consuming resources indefinitely, give the UI visibility into dispatch health, and ensure agent status reflects reality.
Output: Process timeout kill, agent busy/idle tracking, circuit breaker health API endpoint.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/taskDispatcher.ts
@src/lib/taskDispatcherCron.ts

**Prior work (from critical bug fixes):**
- Phase 3 roadmap goals (spawn args, circuit breaker, failure→todo) are 90% complete
- This plan covers remaining hardening gaps
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 30-minute process timeout + agent busy/idle status tracking</name>
  <files>src/lib/taskDispatcher.ts</files>
  <action>
  1. After `proc.unref()` in spawnAttempt, add a 30-minute setTimeout that kills the process if still running. On kill: log activity, decrement activeDispatches, re-dispatch or move to human-review.
  2. On dispatch start: UPDATE agents SET status = 'busy' WHERE id = agentId
  3. On clean exit (code 0): UPDATE agents SET status = 'idle' WHERE id = agentId
  4. On failure/timeout: UPDATE agents SET status = 'idle' WHERE id = agentId (unless circuit breaker sets 'offline')
  Track the timeout ref so it can be cleared on normal process exit.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>Hung processes killed after 30 min. Agent status reflects busy/idle correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add circuit breaker health API route</name>
  <files>app/api/dispatch-health/route.ts</files>
  <action>
  Create GET endpoint that returns:
  - circuitBreakers: result of getCircuitBreakerState()
  - activeDispatches count
  - recentFailures (from task_activity where action='dispatch_failed' in last hour)
  Import getCircuitBreakerState and export activeDispatches from taskDispatcher.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>GET /api/dispatch-health returns circuit breaker state + dispatch metrics.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` — zero errors
- [ ] Process timeout logic is correct (setTimeout + clearTimeout on exit)
- [ ] Agent status updates don't block dispatch on failure
</verification>

<success_criteria>
- All tasks completed
- Type checks pass
- Hung process timeout implemented
- Agent busy/idle tracking implemented
- Circuit breaker API accessible
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-dispatcher-hardening/03-01-SUMMARY.md`
</output>
