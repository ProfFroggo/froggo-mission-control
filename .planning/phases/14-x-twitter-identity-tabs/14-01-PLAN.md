---
phase: 14-x-twitter-identity-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/XTwitterPage.tsx
  - src/core/CoreViews.tsx
  - src/components/XDraftComposer.tsx
autonomous: true

must_haves:
  truths:
    - "X/Twitter page header shows the X logo — no Twitter bird icon"
    - "Sidebar label reads 'X / Twitter' with spaces around the slash"
    - "All inputs, cards, and backgrounds on X/Twitter components use dark mode tokens"
  artifacts:
    - path: "src/components/XTwitterPage.tsx"
      provides: "X logo in page header"
      contains: "XLogo"
    - path: "src/core/CoreViews.tsx"
      provides: "Sidebar label with correct spacing"
      contains: "X / Twitter"
  key_links:
    - from: "src/components/XTwitterPage.tsx"
      to: "XLogo inline SVG"
      via: "local component definition"
      pattern: "XLogo"
---

<objective>
Replace the Twitter bird icon with the X logo on the X/Twitter page, update the sidebar label to "X / Twitter", and fix any remaining non-dark-mode styled elements on X components.

Purpose: Satisfy XTW-01 (icon), XTW-02 (dark mode), and XTW-03 (label) requirements.
Output: Updated XTwitterPage.tsx, CoreViews.tsx, and XDraftComposer.tsx with correct branding and dark mode tokens.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/XTwitterPage.tsx
@src/core/CoreViews.tsx
@src/components/XDraftComposer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Twitter icon with X logo and update sidebar label</name>
  <files>src/components/XTwitterPage.tsx, src/core/CoreViews.tsx</files>
  <action>
In `src/components/XTwitterPage.tsx`:

1. Replace the import on line 2:
   OLD: `import { Twitter, PieChart, X } from 'lucide-react';`
   NEW: `import { PieChart, X } from 'lucide-react';`

2. Add a local XLogo component BEFORE the default export (after imports, before the function):
```tsx
const XLogo = ({ size = 24 }: { size?: number }) => (
  <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor">
    <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z" />
  </svg>
);
```

3. Replace the Twitter icon usage on line 21:
   OLD: `<Twitter size={24} className="text-info" />`
   NEW: `<XLogo size={24} />`

In `src/core/CoreViews.tsx`:

4. Update sidebar label on line 73:
   OLD: `label: 'X/Twitter'`
   NEW: `label: 'X / Twitter'`
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | grep -i "XTwitterPage\|CoreViews"` — no new errors related to these files.
Run `grep -n "Twitter" src/components/XTwitterPage.tsx` — should NOT find the lucide Twitter import (only the text "X / Twitter" in the heading).
Run `grep "X / Twitter" src/core/CoreViews.tsx` — should find the updated label.
  </verify>
  <done>
XTwitterPage header shows XLogo SVG, not Twitter bird. Sidebar says "X / Twitter" with spaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix non-dark-mode styling in X components</name>
  <files>src/components/XDraftComposer.tsx</files>
  <action>
In `src/components/XDraftComposer.tsx` line 301, fix the non-token border color:
   OLD: `border border-gray-800`
   NEW: `border border-clawd-border`

This is the only non-token styling found across all X*.tsx components. All other inputs, dropdowns, cards, and backgrounds already use `bg-clawd-bg`, `bg-clawd-surface`, `bg-clawd-bg-alt`, `border-clawd-border`, etc.
  </action>
  <verify>
Run `grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-" src/components/X*.tsx` — should return zero matches.
  </verify>
  <done>
All X/Twitter components use dark mode tokens exclusively — no hardcoded gray/white colors remain.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "Twitter" src/components/XTwitterPage.tsx` — should only match the heading text "X / Twitter", not the lucide import
2. `grep "XLogo" src/components/XTwitterPage.tsx` — should match the component definition and usage
3. `grep "X / Twitter" src/core/CoreViews.tsx` — should match the updated sidebar label
4. `grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-" src/components/X*.tsx` — zero matches
5. `npx tsc --noEmit 2>&1 | grep -c "error"` — no new errors from these changes
</verification>

<success_criteria>
- XTW-01: Twitter bird icon replaced with X logo SVG in the page header
- XTW-02: All X/Twitter components use dark mode tokens (no hardcoded gray/white)
- XTW-03: Sidebar label reads "X / Twitter" and page heading reads "X / Twitter"
</success_criteria>

<output>
After completion, create `.planning/phases/14-x-twitter-identity-tabs/14-01-SUMMARY.md`
</output>
