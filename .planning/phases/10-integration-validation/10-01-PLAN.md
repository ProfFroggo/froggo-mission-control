---
phase: 10-integration-validation
plan: 01
type: execute
---

<objective>
End-to-end verification that the full pipeline works autonomously.

Purpose: Prove all 9 phases work together — task creation through self-healing recovery.
Output: Pipeline smoke test endpoint, health check integration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline smoke test endpoint</name>
  <files>app/api/pipeline-test/route.ts</files>
  <action>
  Create POST /api/pipeline-test that:
  1. Creates a test task (title: "Pipeline Test: {timestamp}", priority: p3, assigned to a test agent)
  2. Adds planning notes and 2 subtasks
  3. Returns the task ID and expected flow
  4. The test task then flows through: todo → internal-review → Clara pre-review → in-progress → agent works → review → Clara post-review → done
  5. No special code needed — just creates a real task that exercises the existing pipeline

  Also create GET /api/pipeline-test?id={taskId} that returns the current state of a test task
  (status, subtask completion, activity log, review status) for monitoring.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>POST creates test task, GET monitors its pipeline progress.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance health check with pipeline status</name>
  <files>app/api/health/route.ts</files>
  <action>
  Add pipeline health metrics to the existing health endpoint:
  1. tasksInPipeline: count of tasks in each status (todo, internal-review, in-progress, review, human-review)
  2. dispatchHealth: import from dispatch-health (activeDispatches, circuit breakers)
  3. cronHealth: last cron run timestamp from schedule.json state
  4. memoryHealth: count of total agent memory files
  5. knowledgeHealth: total articles, stale count (>90 days)
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>GET /api/health returns comprehensive pipeline health metrics.</done>
</task>

</tasks>
