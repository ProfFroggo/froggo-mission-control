---
phase: 12-setup-wizard
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - src/components/writing/SetupWizard.tsx
  - src/components/writing/WritingWorkspace.tsx
autonomous: false

must_haves:
  truths:
    - "SetupWizard renders WizardReview component when step is 'review'"
    - "SetupWizard renders WizardPlanPreview in conversation sidebar"
    - "User can quit mid-wizard, restart app, and resume where they left off"
    - "On WritingWorkspace mount, pending wizard sessions are detected and resume is offered"
    - "Full wizard flow works end-to-end: braindump -> conversation -> extract -> review -> create"
  artifacts:
    - path: "src/components/writing/SetupWizard.tsx"
      provides: "Complete wizard orchestrator with review integration and resume"
      min_lines: 100
  key_links:
    - from: "src/components/writing/SetupWizard.tsx"
      to: "src/components/writing/WizardReview.tsx"
      via: "conditional render when step === review"
      pattern: "WizardReview"
    - from: "src/components/writing/WritingWorkspace.tsx"
      to: "electron/writing-wizard-service.ts"
      via: "writing:wizard:list IPC for pending wizard detection"
      pattern: "wizard.*list"
---

<objective>
Wire the complete wizard flow: integrate WizardReview into SetupWizard, add WizardPlanPreview sidebar to conversation step, implement wizard resume on app restart, and verify the full end-to-end flow with a human checkpoint.

Purpose: This completes all WIZARD requirements by connecting Plans 02 and 03, adding persistence/resume (WIZARD-08), and verifying the full user experience.
Output: Updated SetupWizard with review integration + resume logic. Human verification of the complete flow.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-setup-wizard/12-RESEARCH.md
@.planning/phases/12-setup-wizard/12-01-SUMMARY.md
@.planning/phases/12-setup-wizard/12-02-SUMMARY.md
@.planning/phases/12-setup-wizard/12-03-SUMMARY.md

Key source files to read:
@src/components/writing/SetupWizard.tsx — current orchestrator (from Plan 02)
@src/components/writing/WizardReview.tsx — review form (from Plan 03)
@src/components/writing/WizardPlanPreview.tsx — plan preview (from Plan 03)
@src/components/writing/WizardChat.tsx — chat component (from Plan 02)
@src/components/writing/WritingWorkspace.tsx — routing (from Plan 02)
@src/store/wizardStore.ts — loadState action for resume
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire WizardReview, WizardPlanPreview, and resume logic into SetupWizard + WritingWorkspace</name>
  <files>
    src/components/writing/SetupWizard.tsx
    src/components/writing/WritingWorkspace.tsx
  </files>
  <action>
**src/components/writing/SetupWizard.tsx:**

1. Replace the placeholder `review` step render with `<WizardReview />` component import and render.

2. In the `conversation` step view, add `<WizardPlanPreview />` as a sidebar next to `<WizardChat />`. Use a flex layout: WizardChat takes flex-1, WizardPlanPreview takes a fixed ~280px right sidebar (hidden if plan is null, shown if plan exists from a previous extraction attempt).

3. In the `complete` step, auto-redirect: useEffect that when step === 'complete', calls wizardStore.reset() after a brief delay (500ms) to return to project list.

4. In the `creating` step, render a centered loading spinner with "Creating your project..." text.

**src/components/writing/WritingWorkspace.tsx:**

Add wizard resume detection on mount:

1. Add a `pendingWizard` local state (null or { sessionId, step, brainDump, ... })
2. Add a `showResumePrompt` local state (boolean, default false)
3. In a useEffect (runs on mount, only when wizardStep === 'idle' and no activeProject):
   - Call `(window as any).clawdbot?.writing?.wizard?.list()`
   - If result has wizards with length > 0, set pendingWizard to the first one and showResumePrompt to true
4. When showResumePrompt is true (and wizard is idle), render a banner/modal before ProjectSelector:
   - "You have an unfinished book plan: '{pendingWizard.brainDump?.slice(0, 50)}...'. Resume?"
   - "Resume" button: calls wizardStore.loadState(pendingWizard) which restores step, messages, plan, etc.
   - "Discard" button: calls bridge()?.delete(pendingWizard.sessionId), sets showResumePrompt to false
5. Keep the existing routing logic: wizard active → SetupWizard, no project → ProjectSelector, project → ProjectEditor
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | head -30`. Grep for `WizardReview` in SetupWizard.tsx. Grep for `WizardPlanPreview` in SetupWizard.tsx. Grep for `wizard.*list\|showResumePrompt\|pendingWizard` in WritingWorkspace.tsx.
  </verify>
  <done>
SetupWizard renders WizardReview in review step and WizardPlanPreview as conversation sidebar. WritingWorkspace detects pending wizard sessions on mount and offers resume/discard. Complete step auto-redirects to project list.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete setup wizard flow covering all 11 WIZARD requirements:
- WIZARD-01: Conversational AI wizard for book planning
- WIZARD-02: Brain dump / free-form description input
- WIZARD-03: Character profiles generated and populated in memory store
- WIZARD-04: Chapter outline generated from conversation
- WIZARD-05: Story arc / plot structure summary generated
- WIZARD-06: Review and edit proposed plan before creation
- WIZARD-07: Quick-create still works (secondary "New Project" button)
- WIZARD-08: Wizard state persists across navigation and restarts
- WIZARD-09: Agent-specialized prompts (Jess for memoir, Writer for novel)
- WIZARD-10: Atomic project creation with chapters, characters, timeline
- WIZARD-11: Genre/type beyond memoir/novel
  </what-built>
  <how-to-verify>
1. Build and open the app:
   ```
   cd ~/froggo-dashboard && npm run build:dev && open release/dev/mac-arm64/Froggo\ Dev.app
   ```

2. **Quick-create still works (WIZARD-07):**
   - Go to Writing tab
   - Click "New Project" (secondary button)
   - Enter a title, select memoir or novel, create
   - Verify project appears in list → delete it

3. **Start wizard (WIZARD-01, -02, -11):**
   - Click "Plan with AI" button
   - Select book type (try "Other" and type a custom genre like "Sci-Fi")
   - Enter a brain dump: "A story about a detective in a futuristic city who discovers that the city's AI has been manipulating crime statistics to justify its own expansion."
   - Click "Start Planning"

4. **Conversation (WIZARD-09):**
   - AI should respond with questions/proposals about the plot
   - Send 2-3 follow-up messages refining chapters and characters
   - Verify streaming works (tokens appear one by one)

5. **Extraction (WIZARD-04, -05):**
   - Click "Generate Plan"
   - Wait for extraction to complete
   - Should transition to review step

6. **Review and edit (WIZARD-06, -03):**
   - Verify all fields shown: title, type, genre, premise, themes, story arc, chapters, characters, timeline
   - Edit the title
   - Add a new chapter
   - Remove a character
   - Add a theme

7. **Create project (WIZARD-10):**
   - Click "Create Project"
   - Verify project appears in list
   - Open the project
   - Check chapters exist in sidebar
   - Check characters exist in memory tab
   - Check timeline events exist in memory tab

8. **Resume (WIZARD-08):**
   - Start a NEW wizard (Plan with AI)
   - Enter brain dump and start conversation
   - Send at least 1 message to AI
   - Close the app completely (Cmd+Q)
   - Reopen the app, go to Writing tab
   - Should see "Resume?" prompt
   - Click Resume → should return to conversation with messages intact

9. **Discard resume:**
   - If still showing resume prompt, click Discard instead
   - Should go to normal project list

Type "approved" if all checks pass, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Human verification covers full end-to-end flow. If approved, Phase 12 is complete.
</verification>

<success_criteria>
- Full wizard flow works: braindump → conversation → extraction → review → create
- Created project has chapters, characters, and timeline populated
- Quick-create still works alongside wizard
- Wizard state survives app restart with resume prompt
- Agent specialization works (memoir uses Jess preamble, novel uses Writer)
- Custom genre/type works (WIZARD-11)
</success_criteria>

<output>
After completion, create `.planning/phases/12-setup-wizard/12-04-SUMMARY.md`
</output>
