---
phase: 21-session-architecture
plan: 01
type: execute
---

<objective>
Session UI — users can see session state (memory loaded, KB articles, context usage, compaction status) and manage sessions (new, export, delete).

Purpose: Transparency into what the agent knows and how its context is assembled.
Output: Session indicators in chat headers + session management in settings.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/phases/21-session-architecture/21.4-01-SUMMARY.md
@src/lib/sessionService.ts
@src/components/XAgentChatPane.tsx
@src/components/ChatPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session stats endpoint + header indicators</name>
  <files>app/api/sessions/stats/route.ts, src/components/XAgentChatPane.tsx</files>
  <action>
  1. Create GET /api/sessions/stats?key={sessionKey}:
     - Uses sessionService.getSessionStats()
     - Returns: { messageCount, age, compacted, lastActivity, memoryFileCount, kbArticleCount, tokenEstimate }

  2. In XAgentChatPane header (the agent chat slide-in panel):
     - Fetch session stats on mount and after every message
     - Show compact indicators below agent name:
       - Message count: "42 msgs"
       - Memory: "3 memory files" (or "No memory" in dim)
       - KB: "2 KB articles" (or "No KB" in dim)
       - Context: progress bar showing estimated % of context used
       - Compacted badge if conversation has been compacted
     - Style: text-[10px] text-mission-control-text-dim, inline badges

  3. Add "New session" button (small, in header):
     - Clears messages for current sessionKey
     - Resets compact_summary
     - Shows toast "Session reset"

  All indicators use design tokens. No hardcoded colors.
  </action>
  <verify>npx tsc --noEmit. Session stats endpoint returns data. Indicators render in social chat header. New session button works.</verify>
  <done>Users can see what's in the agent's context. Can reset sessions. Indicators show memory/KB/compaction state.</done>
</task>

<task type="auto">
  <name>Task 2: Session management in settings</name>
  <files>src/components/EnhancedSettingsPanel.tsx</files>
  <action>
  Add a "Sessions" tab or section to the main settings panel:

  1. List all active sessions from sessions table:
     - Session key, agent name, surface type, message count, last activity, compacted status
     - Sorted by last activity (most recent first)
     - Group by surface type (Chat, Task, Social, Room)

  2. Per session:
     - "Export" button → downloads all messages as markdown file
     - "Delete" button → removes session + its messages from DB
     - "View" link → navigates to the appropriate chat surface

  3. Summary stats at top:
     - Total sessions, total messages, oldest session, most active agent

  Keep it simple — this is a management/debug surface, not a primary workflow.
  </action>
  <verify>npx tsc --noEmit. Settings shows sessions list. Export downloads markdown. Delete removes session.</verify>
  <done>Session management available in settings. Users can audit, export, and clean up sessions.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] Session stats endpoint returns correct data
- [ ] Chat header shows memory/KB/compaction indicators
- [ ] "New session" button resets conversation
- [ ] Settings shows all sessions with management actions
- [ ] Export produces valid markdown
- [ ] Delete removes session + messages from DB
</verification>

<success_criteria>
- Users have full visibility into agent context assembly
- Sessions can be managed (viewed, exported, deleted)
- Indicators are unobtrusive (small text, design tokens)
- No stale sessions accumulate indefinitely
</success_criteria>

<output>
After completion, create `.planning/phases/21-session-architecture/21.5-01-SUMMARY.md`
</output>
