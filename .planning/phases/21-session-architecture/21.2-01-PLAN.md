---
phase: 21-session-architecture
plan: 01
type: execute
---

<objective>
Inject agent memory files into every session. Each agent has a memory directory with .md files containing learnings, patterns, and context. These must be loaded and injected.

Purpose: Agents remember past work, decisions, and patterns across sessions.
Output: sessionService loads memory files and includes them in context.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/phases/21-session-architecture/ROADMAP.md
@.planning/phases/21-session-architecture/21.1-01-SUMMARY.md
@src/lib/sessionService.ts
@tools/memory-mcp/src/index.ts (reference: how memory MCP reads vault files)

Agent memory location: ~/mission-control/memory/agents/{agentId}/
Shared memory: ~/mission-control/memory/shared/
Memory vault root: ~/mission-control/memory/

Each agent dir may contain:
- learnings.md — accumulated learnings from task completions
- patterns.md — patterns the agent has noticed
- preferences.md — user preferences this agent has learned
- context.md — persistent context about the project/domain
- Any other .md files the agent created
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loadAgentMemory to sessionService</name>
  <files>src/lib/sessionService.ts</files>
  <action>
  Add function `loadAgentMemory(agentId: string): string`:

  1. Construct path: `~/mission-control/memory/agents/{agentId}/`
  2. If directory doesn't exist, create it (mkdir -p) and return empty string
  3. Read all .md files in the directory, sorted by mtime (newest first)
  4. For each file: read content, prepend filename as header
  5. Budget: 2000 tokens (~8000 chars). Truncate oldest files first if over budget.
  6. Also load shared memory: `~/mission-control/memory/shared/` — read all .md files (500 token budget)

  Format:
  ```
  --- YOUR MEMORY FILES ---
  These are your accumulated learnings. Reference them when relevant.

  ## learnings.md (modified 2 days ago)
  [content...]

  ## patterns.md (modified 5 days ago)
  [content...]

  ## SHARED MEMORY
  [shared files...]
  ```

  7. Integrate into `buildSessionContext()` — add memoryContext field from loadAgentMemory()
  8. Include in the assembled system prompt after SOUL.md, before conversation history
  </action>
  <verify>npx tsc --noEmit. Function reads from correct directories. Returns formatted string with token estimate.</verify>
  <done>Agent memory files loaded into every session. Empty dirs created. Budget respected.</done>
</task>

<task type="auto">
  <name>Task 2: Add loadKnowledgeBase to sessionService</name>
  <files>src/lib/sessionService.ts</files>
  <action>
  Add function `loadKnowledgeBase(agentId: string, surface: string, metadata?: Record<string, any>): string`:

  1. Query knowledge_base table for relevant articles
  2. Selection logic:
     - Always include: articles with scope='agents' or scope='all'
     - For social-manager: prioritize articles with tags containing 'brand', 'voice', 'strategy', 'x-twitter'
     - For task sessions: match articles by project tags if task has project
     - Sort by: relevance score (tag match count) → recency (updated_at DESC)
     - Limit: 5 articles, 1500 tokens total (~6000 chars)
  3. For social-manager specifically: ALWAYS load "Voice and Style Guide" if it exists (by title match)

  Format:
  ```
  --- KNOWLEDGE BASE ---
  Reference these when answering questions or making decisions.

  ### [Article Title]
  [Summary or first 300 chars of content]

  ### [Article Title]
  [Summary or first 300 chars of content]
  ```

  4. Integrate into `buildSessionContext()` — add knowledgeContext field
  5. Include after memory, before surface-specific context
  </action>
  <verify>npx tsc --noEmit. KB articles loaded and formatted. Voice guide always loaded for social-manager.</verify>
  <done>Knowledge base injected into every session. Relevant articles selected per agent+surface. Token budget respected.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] Agent memory files loaded from ~/mission-control/memory/agents/{id}/
- [ ] Shared memory loaded from ~/mission-control/memory/shared/
- [ ] Knowledge base articles selected per agent scope + tags
- [ ] Voice & Style Guide always loaded for social-manager
- [ ] Token budgets respected (2000 memory, 1500 KB)
</verification>

<success_criteria>
- Every agent session includes memory files and KB articles
- Empty memory dirs auto-created for new agents
- Social-manager always has Voice & Style Guide in context
- Token budgets prevent context overflow
</success_criteria>

<output>
After completion, create `.planning/phases/21-session-architecture/21.2-01-SUMMARY.md`
</output>
