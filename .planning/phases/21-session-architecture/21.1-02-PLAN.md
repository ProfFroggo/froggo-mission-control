---
phase: 21-session-architecture
plan: 02
type: execute
---

<objective>
Wire remaining chat surfaces (TaskChatTab, ChatPanel, ChatRoomView) to the unified session service. Deprecate old endpoints.

Purpose: All 5 chat surfaces use one session system. No more scattered implementations.
Output: All surfaces wired. Old generate-reply endpoint deprecated.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/phases/21-session-architecture/ROADMAP.md
@.planning/phases/21-session-architecture/21.1-01-SUMMARY.md
@src/lib/sessionService.ts
@src/components/TaskChatTab.tsx
@src/components/ChatPanel.tsx
@app/api/agents/[id]/chat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TaskChatTab to session service</name>
  <files>src/components/TaskChatTab.tsx</files>
  <action>
  TaskChatTab currently uses the same SSE streaming as ChatPanel via /api/agents/{id}/chat.

  Update to use POST /api/sessions/chat:
  1. Session key: `task:{agentId}:{taskId}` (includes agent ID so switching agents on same task creates new session)
  2. Surface: 'task'
  3. contextId: taskId
  4. metadata: { taskId, taskTitle, assignedTo: agentId }

  The session service's surface-specific context (Phase 21.4) will later inject task planning notes.
  For now, just wire the basic session flow: send message → get response → persist.

  Keep the existing message history loading from messages table.
  Replace the SSE fetch with a simple POST fetch (JSON response, not streaming).
  Show loading spinner while waiting (already exists).
  </action>
  <verify>npx tsc --noEmit. Task chat sends to /api/sessions/chat. Response rendered. Messages persist with task session key.</verify>
  <done>TaskChatTab uses session service. Agent identity loaded from SOUL.md. Messages isolated per task+agent.</done>
</task>

<task type="auto">
  <name>Task 2: Update ChatPanel main chat to use session service internally</name>
  <files>app/api/agents/[id]/chat/route.ts</files>
  <action>
  The main ChatPanel SSE endpoint (/api/agents/{id}/chat) is the most mature implementation.
  Rather than rewriting the SSE streaming (which works well), update its internals to use sessionService:

  1. Import sessionService functions
  2. Use loadAgentIdentity() instead of inline SOUL.md loading
  3. Use buildSessionContext() for context assembly
  4. Keep SSE streaming for response delivery (better UX than JSON)
  5. Use saveMessage() for persistence (replaces inline DB calls)

  This means ChatPanel keeps its SSE UX but uses the same identity/context/persistence layer as everyone else.

  Session key stays as `chat:{agentId}` (backward compatible).

  DO NOT break the existing ChatPanel — it's the most-used surface.
  </action>
  <verify>npx tsc --noEmit. Main chat still works with SSE streaming. Agent identity consistent with session service.</verify>
  <done>ChatPanel internals use sessionService. Same SSE UX. Identity/persistence unified.</done>
</task>

<task type="auto">
  <name>Task 3: Deprecate generate-reply + mark old endpoints</name>
  <files>app/api/chat/generate-reply/route.ts</files>
  <action>
  Add a deprecation notice to generate-reply:
  - Add console.warn on every call: "[DEPRECATED] /api/chat/generate-reply — use /api/sessions/chat instead"
  - Keep it working (backward compat for any remaining callers)
  - Update the CommsInbox3Pane if it uses generate-reply → point to /api/sessions/chat
  - Grep for all remaining generate-reply callers and note them as tech debt
  </action>
  <verify>npx tsc --noEmit. All primary surfaces use session service. generate-reply still works but logs deprecation.</verify>
  <done>Session service is the primary path for all chat. generate-reply deprecated with warnings.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] TaskChatTab uses /api/sessions/chat
- [ ] ChatPanel internals use sessionService
- [ ] generate-reply logs deprecation warning
- [ ] All 5 surfaces use unified session identity
</verification>

<success_criteria>
- Every chat surface uses sessionService for identity + persistence
- No surface talks directly to Claude CLI without going through sessionService
- Session keys follow unified format
- Agent identity consistent across all surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/21-session-architecture/21.1-02-SUMMARY.md`
</output>
