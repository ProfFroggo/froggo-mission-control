---
phase: 88-platform-hardening
plan: 88.3-01
type: summary
status: complete
---

# Phase 88.3 — Database Performance

## What was done

### Task 1: Composite indexes
- Added `idx_token_usage_agent_ts ON token_usage(agentId, timestamp)` — covers agent cost rollups
- Added `idx_tasks_assigned_status_updated ON tasks(assignedTo, status, updatedAt DESC)` — covers dispatch queries
- Skipped `idx_task_activity_task_ts` — already existed as `idx_task_activity_task_created`

### Task 2: Fix N+1 in projects list
- Replaced per-project `memberStmt.all(p.id)` loop with single batch `WHERE pm.projectId IN (...)` query
- In-memory Map grouping by projectId

### Task 3: Fix N+1 in campaigns list
- Same batch-fetch pattern as projects — single `WHERE cm.campaignId IN (...)` query

### Task 4: Unbounded query limits
- Added `LIMIT 200` to both automations list queries (with/without status filter)

## Verification
- [x] `npm run build:verify` passes

## Files changed
- `src/lib/database.ts` — 2 new composite indexes
- `app/api/projects/route.ts` — N+1 fix (batch member fetch)
- `app/api/campaigns/route.ts` — N+1 fix (batch member fetch)
- `app/api/automations/route.ts` — LIMIT 200 on list queries
