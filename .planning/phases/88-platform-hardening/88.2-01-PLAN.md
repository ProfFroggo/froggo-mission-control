---
phase: 88-platform-hardening
plan: 88.2-01
type: execute
---

<objective>
Fix 4 data integrity issues: budgets table missing PK, inbox agent spawn leak, recurrence race condition, and multi-step mutations without transactions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add PK and unique constraint to budgets table</name>
  <files>src/lib/database.ts</files>
  <action>Add `id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(8))))` and `UNIQUE(agentId, period, name)` to budgets CREATE TABLE.</action>
</task>

<task type="auto">
  <name>Task 2: Guard inbox agent spawn</name>
  <files>app/api/inbox/route.ts</files>
  <action>Add concurrency limit (max 2), 5-min timeout with SIGKILL, and proper cleanup.</action>
</task>

<task type="auto">
  <name>Task 3: Wrap recurrence in transaction</name>
  <files>app/api/tasks/[id]/route.ts</files>
  <action>Wrap the check-then-insert for recurring task spawn in db.transaction().</action>
</task>

<task type="auto">
  <name>Task 4: Wrap multi-step mutations in transactions</name>
  <files>app/api/inbox/[id]/convert-to-task/route.ts, app/api/projects/route.ts</files>
  <action>Wrap convert-to-task (INSERT + UPDATE) and project creation (INSERT project + members + chat room) in db.transaction().</action>
</task>

</tasks>

<verification>
- [ ] `npm run build:verify` passes
- [ ] budgets table has PK and unique constraint
- [ ] inbox spawn has concurrency limit and timeout
- [ ] recurrence insert wrapped in transaction
- [ ] convert-to-task and project creation wrapped in transactions
</verification>
