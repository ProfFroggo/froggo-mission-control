---
phase: 04-cleanup-debloat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/readState.ts
  - src/lib/queryCache.ts
  - src/lib/optimizedQueries.ts
  - src/lib/performanceMonitoring.ts
  - src/lib/smartAccountSelector.ts
  - src/lib/voiceService.ts
  - src/api/gateway.ts
  - src/components/ThreePaneInbox.tsx
  - src/components/CommsInbox.tsx
  - src/components/UnifiedCommsInbox.tsx
  - src/components/ThreadedCommsInbox.tsx
  - src/components/CalendarPanel.tsx
  - src/components/ProtectedPanels.tsx
  - src/store/store.ts
  - src/lib/agents.ts
  - src/components/CodeAgentDashboard.tsx
  - src/components/XAutomationsPanel.tsx
  - src/components/TaskDetailPanel.tsx
  - src/components/SessionsFilter.tsx
  - src/components/InboxPanel.tsx
  - src/components/SettingsPanel.tsx
  - src/components/ReportsPanel.tsx
  - src/components/QuickStatsWidget.tsx
  - src/components/Kanban.tsx
  - src/components/ContextControlBoard.tsx
  - src/components/EpicCalendar.tsx
  - src/components/MarkdownMessage.tsx
  - src/lib/agentContext.ts.bak
  - src/components/EpicCalendar.tsx.bak
  - src/components/PerformanceBenchmarks.tsx.bak
  - src/components/UsageStatsPanel.tsx.bak
  - src/components/VoicePanel.tsx.bak
  - src/lib/geminiLiveService.ts.blob-bak
  - src/lib/geminiLiveService.ts.minimal-bak
  - src/lib/geminiLiveService.ts.pre-minimal
  - src/components/Dashboard.tsx.backup
  - src/components/ContextControlBoard.tsx.backup
  - src/components/EnhancedSettingsPanel.tsx.backup
  - src/components/Kanban.tsx.backup-before-portal
  - src/components/Kanban.tsx.backup-column-controls
  - src/components/Sidebar.tsx.backup-20260211
  - src/components/TeamVoiceMeeting.tsx.backup-20260211
  - src/store/panelConfig.ts.backup-20260211
  - src/components/Dashboard.original.tsx
autonomous: true

must_haves:
  truths:
    - "No .ts/.tsx file in src/ imports from any of the 7 deleted lib files"
    - "No .bak/.backup/.original files exist in src/"
    - "ProtectedPanels no longer exports ThreePaneInbox, CommsInbox, UnifiedCommsInbox, CalendarPanel, or ContentCalendar lazy import"
    - "clearCompletedApprovals function does not exist in store.ts"
    - "getAgentPrompt function does not exist in agents.ts"
    - "No commented-out functions or underscore-prefixed unused state variables remain in targeted components"
    - "TypeScript build passes with zero errors"
  artifacts:
    - path: "src/components/ProtectedPanels.tsx"
      provides: "Only live lazy exports remain"
    - path: "src/store/store.ts"
      provides: "No dead clearCompletedApprovals code"
    - path: "src/lib/agents.ts"
      provides: "No dead getAgentPrompt code"
  key_links:
    - from: "src/components/ProtectedPanels.tsx"
      to: "src/components/App.tsx"
      via: "lazy imports consumed by App"
      pattern: "ProtectedPanels"
---

<objective>
Delete all dead code, dead files, and backup files from src/ to produce a lean codebase.

Purpose: Remove ~1,200+ lines of dead code and 17 backup files that add maintenance burden, confuse imports, and inflate bundle size.
Output: Clean src/ tree with zero dead lib files, zero dead component files, zero dead store code, zero backup files.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-debloat/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead lib files, dead component files, and dead ProtectedPanels exports</name>
  <files>
    src/lib/readState.ts
    src/lib/queryCache.ts
    src/lib/optimizedQueries.ts
    src/lib/performanceMonitoring.ts
    src/lib/smartAccountSelector.ts
    src/lib/voiceService.ts
    src/api/gateway.ts
    src/components/ThreePaneInbox.tsx
    src/components/CommsInbox.tsx
    src/components/UnifiedCommsInbox.tsx
    src/components/ThreadedCommsInbox.tsx
    src/components/CalendarPanel.tsx
    src/components/ProtectedPanels.tsx
  </files>
  <action>
**Step 1: Delete 7 dead lib files.**

```bash
rm src/lib/readState.ts src/lib/queryCache.ts src/lib/optimizedQueries.ts src/lib/performanceMonitoring.ts src/lib/smartAccountSelector.ts src/lib/voiceService.ts src/api/gateway.ts
```

**CRITICAL:** Do NOT delete `src/lib/performanceMonitor.ts` (no "ing") -- it is ALIVE, imported by PerformanceProfiler.tsx.

**Step 2: Delete 5 dead component files.**

```bash
rm src/components/ThreePaneInbox.tsx src/components/CommsInbox.tsx src/components/UnifiedCommsInbox.tsx src/components/ThreadedCommsInbox.tsx src/components/CalendarPanel.tsx
```

Do NOT delete `src/components/ContentCalendar.tsx` -- it is used by XPanel.tsx.

**Step 3: Remove dead exports from ProtectedPanels.tsx.**

Open `src/components/ProtectedPanels.tsx` and remove:
- The lazy import lines for ThreePaneInbox, CommsInbox, UnifiedCommsInbox, CalendarPanel, ContentCalendar (5 lazy import lines)
- The corresponding export lines for those 5 components

Keep all other lazy imports and exports (they are live). The ContentCalendar component FILE stays, but its ProtectedPanels export is dead (nothing imports it from ProtectedPanels).

**Step 4: Verify no broken imports remain.**

```bash
grep -r "from.*readState" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*queryCache" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*optimizedQueries" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*performanceMonitoring" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*smartAccountSelector" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*voiceService" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "from.*api/gateway" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
grep -r "ThreePaneInbox\|CommsInbox\|UnifiedCommsInbox\|ThreadedCommsInbox\|CalendarPanel" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".bak" | grep -v ".backup"
```

All greps should return zero results.
  </action>
  <verify>
Run all the grep checks from Step 4. Each should return zero hits.
Run `npx tsc --noEmit` to confirm no type errors from broken imports.
  </verify>
  <done>7 dead lib files deleted, 5 dead component files deleted, 5 dead ProtectedPanels exports removed. Zero broken imports in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead store code, dead component code, and all backup files</name>
  <files>
    src/store/store.ts
    src/lib/agents.ts
    src/components/CodeAgentDashboard.tsx
    src/components/XAutomationsPanel.tsx
    src/components/TaskDetailPanel.tsx
    src/components/SessionsFilter.tsx
    src/components/InboxPanel.tsx
    src/components/SettingsPanel.tsx
    src/components/ReportsPanel.tsx
    src/components/QuickStatsWidget.tsx
    src/components/Kanban.tsx
    src/components/ContextControlBoard.tsx
    src/components/EpicCalendar.tsx
    src/components/MarkdownMessage.tsx
    (17 backup files)
  </files>
  <action>
**Step 1: Remove `clearCompletedApprovals` from store.ts.**

In `src/store/store.ts`:
- Remove from the interface (around L218): `clearCompletedApprovals: () => void;`
- Remove the implementation (around L1131): the `clearCompletedApprovals: () => set(...)` block

Do NOT delete the `sessions` field -- it is ALIVE (used by 4+ components).

**Step 2: Remove `getAgentPrompt` from agents.ts.**

In `src/lib/agents.ts`:
- Delete the `getAgentPrompt` function definition (around L127-130)
- Find the call site (around L36): `const systemPrompt = getAgentPrompt(agentId);` -- delete this line
- Update the template literal that uses `systemPrompt` (around L40): change from `\`${systemPrompt}\n\n## YOUR TASK\n${task}\`` to `\`## YOUR TASK\n${task}\``
- If `getAgentPrompt` is exported, remove its export

**Step 3: Remove underscore-prefixed unused state variables.**

Remove the following destructured state declarations (the entire `const [...] = useState(...)` line):

- `CodeAgentDashboard.tsx` ~L41: `const [_totalCost, _setTotalCost] = useState(0);` -- remove entire line
- `XAutomationsPanel.tsx` ~L78: `const [_executions, _setExecutions] = useState<any[]>([]);` -- remove entire line
- `TaskDetailPanel.tsx` ~L30: `const [_loading, _setLoading] = useState(true);` -- remove entire line
- `TaskDetailPanel.tsx` ~L48: `const [_showAgentWarning, _setShowAgentWarning] = useState(false);` -- remove entire line
- `TaskDetailPanel.tsx` ~L49: `const [_activeAgentSession, _setActiveAgentSession] = useState<any>(null);` -- remove entire line
- `SessionsFilter.tsx` ~L62: `const [_showDropdown, _setShowDropdown] = useState(false);` -- remove entire line
- `InboxPanel.tsx` ~L114: `const [_collapsedLanes, _setCollapsedLanes] = useState<Set<string>>(new Set());` -- remove entire line
- `SettingsPanel.tsx` ~L168: the `_notifPrefs` state -- remove entire line AND the `setNotifPrefs(...)` call (since neither side is useful)

**Step 4: Remove commented-out code blocks.**

Remove these commented-out blocks:
- `CodeAgentDashboard.tsx` ~L141+: commented `formatDuration` function -- delete all commented lines
- `ReportsPanel.tsx` ~L105: commented `__report` assignment line
- `QuickStatsWidget.tsx` ~L9: commented `__activeSessions` line
- `Kanban.tsx` ~L24-27: commented `formatRelativeTime` function
- `Kanban.tsx` ~L39: commented `isDueSoon` check
- `Kanban.tsx` ~L260-262: commented `assignees` useMemo
- `XAutomationsPanel.tsx` ~L68+: commented `ACTION_ICONS` object
- `ContextControlBoard.tsx` ~L77: commented `__escaped` variable
- `EpicCalendar.tsx` ~L746: commented `__lastDay` variable
- `MarkdownMessage.tsx` ~L108: commented `parts` array

**Step 5: Delete all 17 backup/bak files.**

```bash
rm src/lib/agentContext.ts.bak
rm src/components/EpicCalendar.tsx.bak
rm src/components/PerformanceBenchmarks.tsx.bak
rm src/components/UsageStatsPanel.tsx.bak
rm src/components/VoicePanel.tsx.bak
rm src/lib/geminiLiveService.ts.blob-bak
rm src/lib/geminiLiveService.ts.minimal-bak
rm src/lib/geminiLiveService.ts.pre-minimal
rm src/components/Dashboard.tsx.backup
rm src/components/ContextControlBoard.tsx.backup
rm src/components/EnhancedSettingsPanel.tsx.backup
rm "src/components/Kanban.tsx.backup-before-portal"
rm "src/components/Kanban.tsx.backup-column-controls"
rm "src/components/Sidebar.tsx.backup-20260211"
rm "src/components/TeamVoiceMeeting.tsx.backup-20260211"
rm "src/store/panelConfig.ts.backup-20260211"
rm src/components/Dashboard.original.tsx
```
  </action>
  <verify>
Run `npx tsc --noEmit` -- should pass with zero errors.
Run `find src/ -name "*.bak" -o -name "*.backup*" -o -name "*.blob-bak" -o -name "*.minimal-bak" -o -name "*.pre-minimal" -o -name "*.original.*"` -- should return zero results.
Run `grep -r "clearCompletedApprovals" src/ --include="*.ts" --include="*.tsx"` -- zero results.
Run `grep -r "getAgentPrompt" src/ --include="*.ts" --include="*.tsx"` -- zero results.
  </verify>
  <done>Dead store code removed, 8 unused state variables removed, 10 commented-out code blocks removed, 17 backup files deleted. TypeScript build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `find src/ -name "*.bak" -o -name "*.backup*" -o -name "*.blob-bak" -o -name "*.minimal-bak" -o -name "*.pre-minimal" -o -name "*.original.*"` returns nothing
3. `grep -r "from.*readState\|from.*queryCache\|from.*optimizedQueries\|from.*performanceMonitoring\|from.*smartAccountSelector\|from.*voiceService\|from.*api/gateway" src/ --include="*.ts" --include="*.tsx" | grep -v ".bak" | grep -v ".backup"` returns nothing
4. `grep -r "clearCompletedApprovals\|getAgentPrompt" src/ --include="*.ts" --include="*.tsx"` returns nothing
5. `grep -r "ThreePaneInbox\|CommsInbox\|UnifiedCommsInbox\|ThreadedCommsInbox" src/ --include="*.ts" --include="*.tsx" | grep -v ".bak"` returns nothing
</verification>

<success_criteria>
- All 7 dead lib files deleted
- All 5 dead component files deleted
- 5 dead ProtectedPanels exports removed
- clearCompletedApprovals removed from store.ts
- getAgentPrompt removed from agents.ts
- 8+ unused state variables removed
- 10 commented-out code blocks removed
- All 17 backup/bak/original files deleted
- TypeScript build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-debloat/04-01-SUMMARY.md`
</output>
