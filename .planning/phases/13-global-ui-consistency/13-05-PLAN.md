---
phase: 13-global-ui-consistency
plan: 05
type: execute
wave: 2
depends_on: ["13-03", "13-04"]
files_modified:
  - src/components/XAgentChatPane.tsx
  - src/components/FinanceAgentChat.tsx
  - src/components/ChatRoomView.tsx
  - src/components/QuickActions.tsx
autonomous: true

must_haves:
  truths:
    - "Chat input bar is pinned to the bottom of the panel in XAgentChatPane, FinanceAgentChat, ChatRoomView, and QuickActions — it does not scroll with messages or float mid-screen"
    - "Parent container uses flex flex-col h-full and messages area uses flex-1 overflow-y-auto"
    - "Messages scroll independently while input bar remains fixed at the bottom"
    - "Input bar has border-t border-clawd-border bg-clawd-surface consistent with ChatPanel reference"
  artifacts:
    - path: "src/components/XAgentChatPane.tsx"
      provides: "X/Twitter chat with flex layout and pinned input bar"
      contains: "flex flex-col h-full"
    - path: "src/components/FinanceAgentChat.tsx"
      provides: "Finance chat with flex layout and pinned input bar"
      contains: "flex flex-col h-full"
    - path: "src/components/ChatRoomView.tsx"
      provides: "Room chat with flex layout and pinned input bar"
      contains: "flex flex-col h-full"
    - path: "src/components/QuickActions.tsx"
      provides: "Quick actions text chat with flex layout and pinned input bar"
      contains: "flex flex-col h-full"
  key_links:
    - from: "ChatPanel.tsx (reference)"
      to: "XAgentChatPane.tsx, FinanceAgentChat.tsx, ChatRoomView.tsx, QuickActions.tsx"
      via: "Identical flex layout chain: outer flex flex-col h-full, messages flex-1 overflow-y-auto, input p-4 border-t border-clawd-border bg-clawd-surface"
      pattern: "flex flex-col h-full"
---

<objective>
Fix chat input bar bottom alignment (UI-06) across XAgentChatPane, FinanceAgentChat, ChatRoomView, and QuickActions by auditing and correcting the flex layout chain to match ChatPanel.tsx reference pattern.

Purpose: The chat input bar must be pinned to the bottom of its panel — it should never scroll with messages or float mid-screen. This requires a specific flex layout chain: parent `flex flex-col h-full`, messages area `flex-1 overflow-y-auto`, input bar pinned at bottom with `border-t border-clawd-border bg-clawd-surface`.

Output: Four files audited and fixed to match the ChatPanel.tsx flex layout reference.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-global-ui-consistency/13-04-SUMMARY.md
@src/components/ChatPanel.tsx
@src/components/XAgentChatPane.tsx
@src/components/FinanceAgentChat.tsx
@src/components/ChatRoomView.tsx
@src/components/QuickActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix flex layout in XAgentChatPane and FinanceAgentChat</name>
  <files>src/components/XAgentChatPane.tsx, src/components/FinanceAgentChat.tsx</files>
  <action>
**Reference pattern from ChatPanel.tsx:**
- Outer container: `h-full flex flex-col` (line 836)
- Messages area: `flex-1 overflow-y-auto p-4` (line 1029)
- Input bar: `p-4 border-t border-clawd-border bg-clawd-surface` (line 1097) — sits after the messages div, NOT inside it

**XAgentChatPane.tsx (309 lines):**
1. Read the full component and find its outer container div
2. Ensure the outermost wrapper uses `flex flex-col h-full` (or equivalent)
3. Find the messages/conversation scroll area — ensure it uses `flex-1 overflow-y-auto`
4. Find the input bar/form area — ensure it is a sibling of the messages area (not nested inside), and uses `border-t border-clawd-border bg-clawd-surface` for visual consistency
5. If the input bar is inside a scrollable container, move it outside so it pins to the bottom

**FinanceAgentChat.tsx (286 lines):**
Same audit:
1. Ensure outer container uses `flex flex-col h-full`
2. Ensure messages area uses `flex-1 overflow-y-auto`
3. Ensure input bar is a sibling of messages (not nested), with `border-t border-clawd-border bg-clawd-surface`

Do NOT change any content, styling of individual messages, or functional behavior. Only fix the flex layout chain and input bar container placement.
  </action>
  <verify>
1. `grep -n 'flex flex-col h-full\|flex-col.*h-full\|h-full.*flex.*flex-col' src/components/XAgentChatPane.tsx` — should show outer container
2. `grep -n 'flex-1 overflow-y-auto\|flex-1.*overflow-y-auto' src/components/XAgentChatPane.tsx` — should show messages area
3. `grep -n 'border-t border-clawd-border' src/components/XAgentChatPane.tsx` — should show input bar
4. Same three greps for FinanceAgentChat.tsx
5. `npx tsc --noEmit` passes
  </verify>
  <done>XAgentChatPane and FinanceAgentChat both use the flex flex-col h-full / flex-1 overflow-y-auto / pinned input bar pattern matching ChatPanel.tsx reference.</done>
</task>

<task type="auto">
  <name>Task 2: Fix flex layout in ChatRoomView and QuickActions</name>
  <files>src/components/ChatRoomView.tsx, src/components/QuickActions.tsx</files>
  <action>
**ChatRoomView.tsx (877 lines):**
1. Read the full component and find its outer container div
2. Ensure the outermost wrapper uses `flex flex-col h-full`
3. Find the messages scroll area — ensure it uses `flex-1 overflow-y-auto`
4. Find the input bar/form — ensure it is a sibling of messages (not nested inside scroll area), with `border-t border-clawd-border bg-clawd-surface`
5. If there are intermediate wrapper divs breaking the flex chain, fix them to propagate height correctly (add `flex-1 flex flex-col` or `h-full` as needed)

**QuickActions.tsx — text chat section only:**
1. Find the text chat section (the area with chat messages and text input — NOT the voice/quick action buttons)
2. Ensure the text chat container uses `flex flex-col h-full` (or flex-1 flex flex-col if nested)
3. Ensure the text chat messages area uses `flex-1 overflow-y-auto`
4. Ensure the text input bar is a sibling of messages, with `border-t border-clawd-border bg-clawd-surface`

Do NOT change any content, styling of individual messages, or functional behavior. Only fix the flex layout chain and input bar container placement.
  </action>
  <verify>
1. `grep -n 'flex flex-col h-full\|flex-col.*h-full\|h-full.*flex.*flex-col' src/components/ChatRoomView.tsx` — should show outer container
2. `grep -n 'flex-1 overflow-y-auto\|flex-1.*overflow-y-auto' src/components/ChatRoomView.tsx` — should show messages area
3. `grep -n 'border-t border-clawd-border' src/components/ChatRoomView.tsx` — should show input bar
4. `grep -n 'flex-1 overflow-y-auto\|flex-1.*overflow-y-auto' src/components/QuickActions.tsx` — should show text chat messages area
5. `grep -n 'border-t border-clawd-border' src/components/QuickActions.tsx` — should show text chat input bar
6. `npx tsc --noEmit` passes
  </verify>
  <done>ChatRoomView and QuickActions text chat both use the flex flex-col h-full / flex-1 overflow-y-auto / pinned input bar pattern matching ChatPanel.tsx reference.</done>
</task>

</tasks>

<verification>
After both tasks:
1. All four files have `flex flex-col h-full` (or equivalent) on outer container
2. All four files have `flex-1 overflow-y-auto` on messages area
3. All four files have `border-t border-clawd-border bg-clawd-surface` on input bar
4. Input bar is a sibling of messages area (not nested inside scroll container) in all four files
5. `npx tsc --noEmit` passes with no errors
6. Visual check: in each chat view, scroll messages up — input bar must remain pinned at bottom
</verification>

<success_criteria>
- Chat input bar is pinned to the bottom of the panel in all four components
- Messages scroll independently of the input bar
- Flex layout chain matches ChatPanel.tsx reference: outer flex flex-col h-full, messages flex-1 overflow-y-auto, input pinned at bottom
- No regression in chat functionality or message display
</success_criteria>

<output>
After completion, create `.planning/phases/13-global-ui-consistency/13-05-SUMMARY.md`
</output>
