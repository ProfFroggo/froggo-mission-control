---
phase: 13-global-ui-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AgentPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Agents page 'More/Less' button border uses per-agent theme color instead of generic gray"
    - "Agents page action row divider line above buttons uses per-agent theme color"
    - "Agents page expanded section divider line uses per-agent theme color"
  artifacts:
    - path: "src/components/AgentPanel.tsx"
      provides: "Agent card borders using theme.border from getAgentTheme()"
      contains: "theme.border"
  key_links:
    - from: "src/components/AgentPanel.tsx"
      to: "src/utils/agentThemes.ts"
      via: "getAgentTheme(agent.id)"
      pattern: "getAgentTheme"
---

<objective>
Fix the Agents page so that the "More/Less" button border and the horizontal divider lines above/below the dropdown actions use per-agent theme colors instead of the generic `border-clawd-border/50` (which appears as a dim gray regardless of agent).

Purpose: UI-02 and UI-03 require agent-specific theming for dropdown borders and dividers. The `getAgentTheme()` utility already provides per-agent `border` classes (e.g., `border-green-500/30`, `border-blue-500/30`), but AgentPanel.tsx ignores them for these elements.

Output: AgentPanel.tsx updated with three border class replacements.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/AgentPanel.tsx
@src/utils/agentThemes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace generic borders with per-agent theme borders</name>
  <files>src/components/AgentPanel.tsx</files>
  <action>
AgentPanel.tsx already calls `getAgentTheme(agent.id)` and has access to `theme` variable inside the card render. Make these three specific replacements:

**Change 1 — Action row divider (line ~350):**
Find:
```tsx
<div className="flex items-center gap-2 pt-2 border-t border-clawd-border/50">
```
Replace with:
```tsx
<div className={`flex items-center gap-2 pt-2 border-t ${theme.border}`}>
```

**Change 2 — More/Less button border (line ~364):**
Find the button with class containing:
```
border border-clawd-border/50 rounded-lg hover:bg-clawd-border/30
```
Replace with:
```
border ${theme.border} rounded-lg hover:bg-clawd-border/30
```
NOTE: The className must become a template literal using backticks and `${}` since `theme.border` is dynamic. The full className should be:
```tsx
className={`flex items-center gap-1 px-2.5 py-1.5 text-xs text-clawd-text-dim hover:text-clawd-text border ${theme.border} rounded-lg hover:bg-clawd-border/30 transition-colors`}
```

**Change 3 — Expanded section divider (line ~387):**
Find:
```tsx
className="mt-4 pt-4 border-t border-clawd-border/50 space-y-4"
```
Replace with:
```tsx
className={`mt-4 pt-4 border-t ${theme.border} space-y-4`}
```

IMPORTANT: Verify that `theme` (from `getAgentTheme`) is in scope at each replacement site. It should be — the card render function destructures or accesses the theme for each agent in the `.map()` loop. If `theme` is not directly available, look for how the component accesses the agent theme (likely `const theme = getAgentTheme(agent.id)` inside the map callback) and use that.
  </action>
  <verify>
1. `grep -n 'border-clawd-border/50' src/components/AgentPanel.tsx` — should return 0 matches (all replaced)
2. `grep -n 'theme.border' src/components/AgentPanel.tsx` — should return 3+ matches (the three replacements)
3. `npx tsc --noEmit` — no type errors
  </verify>
  <done>All three border elements in AgentPanel.tsx (action row divider, More/Less button, expanded section divider) use per-agent theme.border class instead of generic border-clawd-border/50.</done>
</task>

</tasks>

<verification>
1. `grep -c 'border-clawd-border/50' src/components/AgentPanel.tsx` returns 0
2. `grep -c 'theme.border' src/components/AgentPanel.tsx` returns 3 or more
3. TypeScript compiles without errors
</verification>

<success_criteria>
- Zero remaining `border-clawd-border/50` in AgentPanel.tsx
- Three `theme.border` usages for action divider, More/Less button, and expanded divider
- Each agent card's borders visually match that agent's theme color
</success_criteria>

<output>
After completion, create `.planning/phases/13-global-ui-consistency/13-02-SUMMARY.md`
</output>
