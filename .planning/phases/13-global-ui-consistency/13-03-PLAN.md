---
phase: 13-global-ui-consistency
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/ChatPanel.tsx
  - src/components/ChatRoomView.tsx
  - src/components/AgentChatModal.tsx
  - src/components/writing/ChatMessage.tsx
autonomous: true

must_haves:
  truths:
    - "ChatPanel user bubbles use bg-clawd-accent/50 (50% opacity green), not solid bg-clawd-accent"
    - "ChatRoomView user bubbles use bg-clawd-accent/50, not bg-clawd-accent/10"
    - "AgentChatModal user bubbles use bg-clawd-accent/50, not bg-clawd-accent/10"
    - "Writing ChatMessage user bubbles use bg-clawd-accent/50, not bg-clawd-accent/10"
    - "All four chat components use consistent user bubble styling: bg-clawd-accent/50 text-white rounded-2xl rounded-tr-sm"
  artifacts:
    - path: "src/components/ChatPanel.tsx"
      provides: "Reference chat with 50% opacity user bubbles"
      contains: "bg-clawd-accent/50"
    - path: "src/components/ChatRoomView.tsx"
      provides: "Room chat with standardized user bubbles"
      contains: "bg-clawd-accent/50"
    - path: "src/components/AgentChatModal.tsx"
      provides: "Agent chat modal with standardized user bubbles"
      contains: "bg-clawd-accent/50"
    - path: "src/components/writing/ChatMessage.tsx"
      provides: "Writing chat with standardized user bubbles"
      contains: "bg-clawd-accent/50"
  key_links:
    - from: "ChatPanel.tsx"
      to: "ChatRoomView.tsx, AgentChatModal.tsx, writing/ChatMessage.tsx"
      via: "Identical user bubble class pattern"
      pattern: "bg-clawd-accent/50 text-white"
---

<objective>
Standardize user message chat bubble styling across the four green-themed chat components (ChatPanel, ChatRoomView, AgentChatModal, Writing ChatMessage) to use `bg-clawd-accent/50 text-white` — a 50% opacity green background that is visually distinct from full-brightness green without being jarring.

Purpose: UI-04 requires 50% opacity green for user bubbles. UI-05 requires all chat dialogues to use consistent styling. These four components currently use different opacities (solid, 10%, 10%, 10%) and must be standardized.

Output: Four files updated with consistent user bubble classes.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ChatPanel.tsx
@src/components/ChatRoomView.tsx
@src/components/AgentChatModal.tsx
@src/components/writing/ChatMessage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ChatPanel and ChatRoomView user bubbles</name>
  <files>src/components/ChatPanel.tsx, src/components/ChatRoomView.tsx</files>
  <action>
**ChatPanel.tsx (reference chat — line ~1335):**
Find the user bubble class:
```
'bg-clawd-accent text-white rounded-tr-sm'
```
Replace with:
```
'bg-clawd-accent/50 text-white rounded-tr-sm'
```
This is inside the MessageItem component's ternary for `isUser`. The line is approximately 1335.

**ChatRoomView.tsx (room chat — line ~642):**
Find the user bubble class:
```
'bg-clawd-accent/10 border border-clawd-accent/30 rounded-tr-sm'
```
Replace with:
```
'bg-clawd-accent/50 text-white rounded-tr-sm'
```
Note: This removes the border and changes from 10% to 50% opacity, and adds `text-white` to match the reference pattern. The `border border-clawd-accent/30` is dropped because the reference ChatPanel user bubble does not have a border.
  </action>
  <verify>
1. `grep -n 'bg-clawd-accent/50' src/components/ChatPanel.tsx` — should show the user bubble line
2. `grep -n 'bg-clawd-accent/50' src/components/ChatRoomView.tsx` — should show the user bubble line
3. `grep -n 'bg-clawd-accent/10' src/components/ChatRoomView.tsx` — should return 0 matches for user bubble context (other /10 usages for non-bubble elements are OK)
  </verify>
  <done>ChatPanel uses bg-clawd-accent/50 (was solid). ChatRoomView uses bg-clawd-accent/50 text-white (was /10 with border).</done>
</task>

<task type="auto">
  <name>Task 2: Fix AgentChatModal and Writing ChatMessage user bubbles</name>
  <files>src/components/AgentChatModal.tsx, src/components/writing/ChatMessage.tsx</files>
  <action>
**AgentChatModal.tsx:**
Find the user bubble class (search for `bg-clawd-accent/10` in the message rendering section):
```
'bg-clawd-accent/10 border border-clawd-accent/30'
```
Replace with:
```
'bg-clawd-accent/50 text-white'
```
Remove the `border border-clawd-accent/30` portion. Keep any `rounded-*` classes that follow. The result should look like:
```
'bg-clawd-accent/50 text-white rounded-2xl rounded-tr-sm'
```
If the existing rounded classes differ (e.g., `rounded-lg` instead of `rounded-2xl`), change them to `rounded-2xl rounded-tr-sm` to match the ChatPanel reference standard.

**writing/ChatMessage.tsx (line ~42):**
Find:
```tsx
<div className="max-w-[85%] bg-clawd-accent/10 border border-clawd-accent/30 rounded-2xl rounded-tr-sm px-4 py-3 text-sm">
```
Replace with:
```tsx
<div className="max-w-[85%] bg-clawd-accent/50 text-white rounded-2xl rounded-tr-sm px-4 py-3 text-sm">
```
Changes: `bg-clawd-accent/10` -> `bg-clawd-accent/50`, remove `border border-clawd-accent/30`, add `text-white`.
  </action>
  <verify>
1. `grep -n 'bg-clawd-accent/50' src/components/AgentChatModal.tsx` — should show the user bubble line
2. `grep -n 'bg-clawd-accent/50' src/components/writing/ChatMessage.tsx` — should show the user bubble line
3. `grep -n 'bg-clawd-accent/10.*border' src/components/AgentChatModal.tsx` — should return 0 (no more 10% user bubbles with border)
4. `grep -n 'bg-clawd-accent/10.*border' src/components/writing/ChatMessage.tsx` — should return 0
  </verify>
  <done>AgentChatModal uses bg-clawd-accent/50 text-white (was /10 with border). Writing ChatMessage uses bg-clawd-accent/50 text-white (was /10 with border). All four green-themed chat components now match.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn 'bg-clawd-accent/50' src/components/ChatPanel.tsx src/components/ChatRoomView.tsx src/components/AgentChatModal.tsx src/components/writing/ChatMessage.tsx` — all four files should have at least one match
2. No remaining `bg-clawd-accent/10 border border-clawd-accent/30` patterns in user bubble rendering sections of these four files
3. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All four chat components use `bg-clawd-accent/50 text-white` for user message bubbles
- Visual consistency: user bubbles appear as 50% opacity green with white text across Chat, Room, Agent Modal, and Writing pages
- No remaining solid green (`bg-clawd-accent` without opacity) or 10% green (`bg-clawd-accent/10`) for user bubbles in these files
</success_criteria>

<output>
After completion, create `.planning/phases/13-global-ui-consistency/13-03-SUMMARY.md`
</output>
