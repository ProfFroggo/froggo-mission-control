---
phase: 13-global-ui-consistency
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/XAgentChatPane.tsx
  - src/components/FinanceAgentChat.tsx
  - src/components/VoiceChatPanel.tsx
  - src/components/QuickActions.tsx
autonomous: true

must_haves:
  truths:
    - "XAgentChatPane user bubbles use bg-clawd-accent/50 text-white, not bg-info-subtle text-info (blue)"
    - "FinanceAgentChat user bubbles use bg-clawd-accent/50 text-white, not bg-blue-600 (hardcoded blue)"
    - "FinanceAgentChat send button uses bg-clawd-accent, not bg-blue-600"
    - "VoiceChatPanel user/transcript bubbles use bg-clawd-accent/50, not solid bg-clawd-accent"
    - "QuickActions voice transcript user bubbles use bg-clawd-accent/50, not bg-info-subtle (blue)"
    - "QuickActions text chat user bubbles use bg-clawd-accent/50 text-white, not bg-clawd-accent/20"
    - "All chat input fields in these components use bg-clawd-bg-alt (resolves to dark background), not transparent"
  artifacts:
    - path: "src/components/XAgentChatPane.tsx"
      provides: "X/Twitter chat with standardized user bubbles and dark-mode inputs"
      contains: "bg-clawd-accent/50"
    - path: "src/components/FinanceAgentChat.tsx"
      provides: "Finance chat with standardized user bubbles and accent-colored send button"
      contains: "bg-clawd-accent/50"
    - path: "src/components/VoiceChatPanel.tsx"
      provides: "Voice chat transcript with standardized user bubbles"
      contains: "bg-clawd-accent/50"
    - path: "src/components/QuickActions.tsx"
      provides: "Quick actions chat with standardized user bubbles"
      contains: "bg-clawd-accent/50"
  key_links:
    - from: "FinanceAgentChat.tsx"
      to: "tailwind.config.js"
      via: "bg-clawd-accent replaces bg-blue-600"
      pattern: "bg-clawd-accent"
    - from: "XAgentChatPane.tsx"
      to: "tailwind.config.js"
      via: "bg-clawd-accent/50 replaces bg-info-subtle"
      pattern: "bg-clawd-accent/50"
---

<objective>
Standardize user message chat bubble styling across the four non-standard chat components (XAgentChatPane, FinanceAgentChat, VoiceChatPanel, QuickActions) to use `bg-clawd-accent/50 text-white`. These components currently use blue, hardcoded blue, solid green, or 20% green for user bubbles.

Purpose: UI-04 requires 50% opacity green for ALL user bubbles. UI-05 requires all chat dialogues to match Chat page styling. These four components use completely wrong colors (blue info, hardcoded blue-600) or wrong opacities and must be fixed. Also fixes remaining input field dark-mode issues where bg-clawd-bg-alt was previously undefined (now defined by Plan 01).

Output: Four files updated with consistent user bubble classes and corrected input backgrounds.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-global-ui-consistency/13-01-SUMMARY.md
@src/components/XAgentChatPane.tsx
@src/components/FinanceAgentChat.tsx
@src/components/VoiceChatPanel.tsx
@src/components/QuickActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix XAgentChatPane and FinanceAgentChat user bubbles and inputs</name>
  <files>src/components/XAgentChatPane.tsx, src/components/FinanceAgentChat.tsx</files>
  <action>
**XAgentChatPane.tsx (line ~241):**
Find the user bubble class:
```
'bg-info-subtle text-info'
```
(This is in the ternary for `msg.role === 'user'`)
Replace with:
```
'bg-clawd-accent/50 text-white'
```

Also in XAgentChatPane.tsx, find the agent name badge (line ~206) that uses `bg-info-subtle text-info`:
```tsx
<span className="px-2 py-1 text-xs bg-info-subtle text-info rounded-full">
```
Leave this one ALONE — it's a badge/tag, not a chat bubble. Only change the user message bubble.

**FinanceAgentChat.tsx (line ~206):**
Find the user bubble class:
```
'bg-blue-600 text-white'
```
Replace with:
```
'bg-clawd-accent/50 text-white'
```

**FinanceAgentChat.tsx send button (line ~270):**
Find the send button class:
```
className="p-2 bg-blue-600 hover:bg-blue-700 disabled:bg-clawd-bg-alt disabled:cursor-not-allowed text-white rounded-lg transition-colors"
```
Replace with:
```
className="p-2 bg-clawd-accent hover:bg-clawd-accent-dim disabled:bg-clawd-bg-alt disabled:cursor-not-allowed text-white rounded-lg transition-colors"
```
Changes: `bg-blue-600` -> `bg-clawd-accent`, `hover:bg-blue-700` -> `hover:bg-clawd-accent-dim`.

NOTE: Both FinanceAgentChat.tsx and XAgentChatPane.tsx use `bg-clawd-bg-alt` for input backgrounds and agent message bubbles. Since Plan 01 defines this token, these will now render correctly as dark backgrounds. No changes needed to the `bg-clawd-bg-alt` usages — they are correct, they just needed the token to be defined.
  </action>
  <verify>
1. `grep -n 'bg-info-subtle' src/components/XAgentChatPane.tsx` — should still show the badge line (~206) but NOT the user bubble line
2. `grep -n 'bg-blue-600' src/components/FinanceAgentChat.tsx` — should return 0 matches
3. `grep -n 'bg-clawd-accent/50' src/components/XAgentChatPane.tsx` — should show user bubble
4. `grep -n 'bg-clawd-accent/50' src/components/FinanceAgentChat.tsx` — should show user bubble
5. `grep -n 'bg-clawd-accent' src/components/FinanceAgentChat.tsx` — should show send button
  </verify>
  <done>XAgentChatPane user bubbles are green (was blue). FinanceAgentChat user bubbles and send button use accent color (was hardcoded blue-600). Input backgrounds use bg-clawd-bg-alt which now resolves to a real dark color.</done>
</task>

<task type="auto">
  <name>Task 2: Fix VoiceChatPanel and QuickActions user bubbles</name>
  <files>src/components/VoiceChatPanel.tsx, src/components/QuickActions.tsx</files>
  <action>
**VoiceChatPanel.tsx (line ~619-621):**
Find the user message bubble in the transcript rendering. Look for the ternary that checks message role. The user bubble currently uses solid `bg-clawd-accent`:
```
? 'bg-clawd-accent text-white rounded-tr-sm'
```
Replace with:
```
? 'bg-clawd-accent/50 text-white rounded-tr-sm'
```

Also in VoiceChatPanel.tsx, the agent bubble uses `bg-clawd-card` (line ~621):
```
: 'bg-clawd-card text-clawd-text border border-clawd-border'
```
Leave this alone — `bg-clawd-card` is now defined by Plan 01 and will resolve correctly. The agent bubble styling is acceptable (matches surface/card pattern).

**QuickActions.tsx — voice transcript (line ~1041):**
Find:
```
entry.role === 'user' ? 'bg-info-subtle text-info'
```
Replace with:
```
entry.role === 'user' ? 'bg-clawd-accent/50 text-white'
```

**QuickActions.tsx — text chat (line ~1141):**
Find:
```
? 'bg-clawd-accent/20 text-clawd-text'
```
Replace with:
```
? 'bg-clawd-accent/50 text-white'
```
Changes: `/20` -> `/50`, `text-clawd-text` -> `text-white` to match the standardized pattern.
  </action>
  <verify>
1. `grep -n 'bg-clawd-accent/50' src/components/VoiceChatPanel.tsx` — should show user bubble
2. `grep -n 'bg-clawd-accent/50' src/components/QuickActions.tsx` — should show 2 matches (voice + text chat)
3. `grep -n 'bg-info-subtle' src/components/QuickActions.tsx` — should return 0 matches (removed from voice transcript)
4. `grep -n 'bg-clawd-accent/20 text-clawd-text' src/components/QuickActions.tsx` — should return 0 matches
5. `npx tsc --noEmit` passes
  </verify>
  <done>VoiceChatPanel user bubbles are 50% opacity (was solid). QuickActions voice transcript and text chat both use bg-clawd-accent/50 text-white (was blue and 20% green respectively). All 8 chat implementations now use consistent user bubble styling.</done>
</task>

</tasks>

<verification>
After both tasks, verify across ALL 8 chat components:
1. `grep -rn 'bg-clawd-accent/50' src/components/ChatPanel.tsx src/components/ChatRoomView.tsx src/components/AgentChatModal.tsx src/components/writing/ChatMessage.tsx src/components/XAgentChatPane.tsx src/components/FinanceAgentChat.tsx src/components/VoiceChatPanel.tsx src/components/QuickActions.tsx` — all 8 files should have at least one match
2. `grep -rn 'bg-blue-600' src/components/` — should return 0 matches (no more hardcoded blue)
3. `grep -rn 'bg-info-subtle.*text-info' src/components/QuickActions.tsx` — 0 matches in chat bubble context
4. `npx tsc --noEmit` passes with no errors
</verification>

<success_criteria>
- All 8 chat components use `bg-clawd-accent/50 text-white` for user message bubbles
- No remaining `bg-blue-600` anywhere in src/components/
- No remaining blue-themed (`bg-info-subtle text-info`) user chat bubbles
- FinanceAgentChat send button uses `bg-clawd-accent` instead of `bg-blue-600`
- All input fields using `bg-clawd-bg-alt` render with visible dark background (token now defined)
</success_criteria>

<output>
After completion, create `.planning/phases/13-global-ui-consistency/13-04-SUMMARY.md`
</output>
