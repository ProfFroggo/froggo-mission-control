---
phase: 04-auto-advance-recovery
plan: 01
type: execute
---

<objective>
Close every gap in the task pipeline — auto-redispatch on human-review resolution, stuck task escalation, stale human-review alerts.

Purpose: Every task status has a clear next step. No dead ends.
Output: Automatic agent re-dispatch after human resolves blocker, escalation for 4h+ stuck tasks, 24h stale human-review alerts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Auto re-dispatch when human resolves human-review</name>
  <files>app/api/tasks/[id]/route.ts</files>
  <action>
  In the PATCH handler, after the status update is written to DB, add a check:
  If the previous status was 'human-review' AND the new status is 'in-progress' AND there's an assignedTo agent → call dispatchTask(id).
  Log activity: "Human resolved blocker — re-dispatching agent {agentId}".
  Also handle human-review→todo (human wants the task replanned) — no dispatch needed, just log.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>Moving task from human-review to in-progress auto-dispatches the assigned agent.</done>
</task>

<task type="auto">
  <name>Task 2: Escalation for stuck tasks + stale human-review alerts</name>
  <files>src/lib/claraReviewCron.ts</files>
  <action>
  In runReviewCycle(), add two new recovery passes:

  1. Stuck in-progress escalation: tasks stuck >4h (had 3+ stuck-recovery re-dispatches) → move to human-review with "Agent unable to complete after multiple re-dispatches — needs human attention."

  2. Stale human-review alert: tasks in human-review >24h → post a reminder to mission-control chat room: "Task X has been in human-review for >24h — needs attention."
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>4h+ stuck tasks escalate to human-review. 24h+ human-review tasks generate reminders.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` — zero errors
- [ ] human-review → in-progress triggers dispatch
- [ ] 4h stuck tasks escalate
- [ ] 24h human-review tasks get reminder
</verification>

<success_criteria>
- All tasks completed
- Type checks pass
- No dead-end states in pipeline
</success_criteria>
