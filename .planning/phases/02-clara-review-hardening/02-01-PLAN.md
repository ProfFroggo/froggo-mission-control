---
phase: 02-clara-review-hardening
type: execute
---

<objective>
Harden Clara's review subprocess with proper error logging, spawn error handling, and process exit tracking. Make every failure visible and debuggable.

Purpose: Silent failures were the root cause of the stuck pipeline. This phase ensures every failure produces actionable logs.
Output: All Clara subprocess failures are logged with stderr, exit codes, and context.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/claraReviewCron.ts
@.planning/phases/01-pipeline-critical-fix/01-01-SUMMARY.md
@.planning/phases/01-pipeline-critical-fix/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error logging to all proc.on('error') handlers</name>
  <files>src/lib/claraReviewCron.ts</files>
  <action>
Find both `proc.on('error')` handlers (pre-review ~line 316, post-review ~line 541). Currently they just clear timeout and remove from tracking set. Add:

1. Log the actual error: `console.error('[clara-review-cron] Spawn error for task ${task.id}:', err.message)`
2. Log activity to DB: INSERT INTO task_activity with action='clara-spawn-error', message containing the error
3. In the pre-review handler: reset task from internal-review back to having reviewStatus=NULL so next cycle retries
4. In the post-review handler: leave task in review with reviewStatus=NULL so next cycle retries

Also: reduce the 23 silent `catch { /* non-critical */ }` blocks to only silence truly non-critical ones. For anything related to spawning, DB writes for task state, or dispatch calls — add `console.error` with context.
  </action>
  <verify>
grep -c "console.error" src/lib/claraReviewCron.ts — should be significantly more than before.
grep "clara-spawn-error" src/lib/claraReviewCron.ts — should find activity logging.
npx tsc --noEmit — zero errors.
  </verify>
  <done>
All spawn errors are logged with error message and task context. Spawn failures produce visible activity entries. Silent catch blocks reduced to truly non-critical operations only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add process exit code tracking and non-zero exit handling</name>
  <files>src/lib/claraReviewCron.ts</files>
  <action>
The existing `proc.on('close')` handlers check if Clara made a decision, but don't handle non-zero exit codes. Enhance both handlers:

1. In the `proc.on('close')` callback, capture the exit code parameter: `proc.on('close', (code) => { ... })`
2. If code is non-zero AND stderrBuf has content, log it as an error:
   `console.error('[clara-review-cron] Clara exited ${code} for ${task.id}: ${stderrBuf.slice(0,300)}')`
3. Log the exit code to task_activity so it's visible in the UI:
   `action: 'clara-process-exit', message: 'Clara process exited with code ${code}: ${stderrBuf.slice(0,200)}'`
4. On non-zero exit: ensure the task doesn't get stuck — reset reviewStatus to NULL

Note: The existing stderr capture (stderrBuf) was added in a previous fix. Make sure the close handler has access to it (it's in the same scope).

Also verify the existing duplicate `proc.on('close')` handlers don't conflict — there are currently TWO close handlers per spawn (one from the stderr capture fix, one from the original code). Merge them into one clean handler.
  </action>
  <verify>
grep "clara-process-exit" src/lib/claraReviewCron.ts — should find activity logging.
Verify only ONE proc.on('close') handler per spawn (not duplicates).
npx tsc --noEmit — zero errors.
  </verify>
  <done>
Non-zero exit codes are logged with stderr content. Activity entries show process failures in the UI. No duplicate close handlers. All review subprocess states are recoverable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Clara review cycle telemetry summary</name>
  <files>src/lib/claraReviewCron.ts</files>
  <action>
At the end of `runReviewCycle()`, before the return statement, add a summary log line that shows what happened in this cycle:

```
console.log(`[clara-review-cron] Cycle complete: ${queued} queued, ${advanced} advanced, ${recovered} recovered, ${reset} reset`);
```

Track these counts throughout the function:
- `advanced` = todo tasks auto-advanced to internal-review
- `recovered` = stuck in-progress tasks re-dispatched
- `reset` = stale review statuses cleared

This gives a quick pulse on system health every 3 minutes in the server logs.
  </action>
  <verify>
grep "Cycle complete" src/lib/claraReviewCron.ts — should find the summary log.
npx tsc --noEmit — zero errors.
  </verify>
  <done>
Every review cycle logs a summary of actions taken. System health is visible in server logs at a glance.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` succeeds
- [ ] No duplicate proc.on('close') handlers
- [ ] All spawn errors logged with context
- [ ] Non-zero exit codes tracked in activity log
- [ ] Cycle summary log shows action counts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every Clara subprocess failure produces visible, debuggable output
- No silent failures remain in the review pipeline
- Phase 2 complete

</success_criteria>

<output>
After completion, create `.planning/phases/02-clara-review-hardening/02-01-SUMMARY.md`
</output>
