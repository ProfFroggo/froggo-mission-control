---
phase: 01-pipeline-critical-fix
type: execute
---

<objective>
Close the autonomous pipeline loop: when Clara rejects a post-work review, automatically re-dispatch the agent with Clara's feedback so it can fix the issue and resubmit.

Purpose: Without this, rejected tasks sit in in-progress forever with no running agent. This is THE critical missing piece for autonomous operation.
Output: Rejected tasks automatically get re-dispatched with feedback, stuck in-progress tasks get recovered.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/claraReviewCron.ts
@src/lib/taskDispatcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-dispatch agent after Clara post-review rejection</name>
  <files>src/lib/claraReviewCron.ts</files>
  <action>
In the post-work review outcome handler (around line 489, inside the `if (reviewed.reviewStatus === 'rejected')` block), after logging the rejection activity, add a re-dispatch call:

1. After `db2.prepare(...).run(...)` for `review-rejected` activity, add:
   - Build a re-dispatch message that includes Clara's feedback: `"Clara rejected your work: {reviewNotes}. Fix the issues and resubmit by setting status to 'review' when done."`
   - Call `dispatchTask(task.id as string)` to re-spawn the agent
   - Log activity: `'redispatch-after-rejection'` with message `"Re-dispatching {agentId} to implement Clara's feedback"`

2. The task stays in `in-progress` status (Clara already set it there on rejection). The agent gets re-spawned with the feedback as context.

3. Important: Check that the task is actually in `in-progress` before re-dispatching (it should be, since Clara sets it). Don't re-dispatch if status changed.

4. Add a 3-second delay before re-dispatch to let DB writes settle:
   ```
   setTimeout(() => {
     const check = db.prepare('SELECT status FROM tasks WHERE id = ?').get(taskId);
     if (check?.status === 'in-progress') {
       dispatchTask(taskId);
     }
   }, 3000);
   ```
  </action>
  <verify>
Grep for 'redispatch-after-rejection' in claraReviewCron.ts to confirm the activity log exists.
Grep for 'dispatchTask' calls — should now appear in BOTH pre-review (approval dispatch) AND post-review (rejection re-dispatch) sections.
  </verify>
  <done>
When Clara rejects a post-work review, the agent is automatically re-dispatched within 5 seconds with Clara's feedback as context. The task stays in in-progress and the agent can fix the issue.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure re-dispatched agent receives Clara's feedback in system prompt</name>
  <files>src/lib/taskDispatcher.ts</files>
  <action>
In the `buildTaskMessage` function (or wherever the dispatch message is constructed), enhance it to include Clara's review feedback when re-dispatching a rejected task:

1. Find `buildTaskMessage` function in taskDispatcher.ts
2. Inside it, check if the task has `reviewStatus === 'rejected'` and `reviewNotes` is non-empty
3. If so, prepend Clara's feedback to the task message:
   ```
   IMPORTANT — Clara (quality reviewer) rejected your previous submission:
   "${task.reviewNotes}"

   Fix the issues described above, then set the task status to 'review' when complete.
   ```
4. Also check task_activity for the most recent `review-rejected` entry to get the full feedback context

This ensures the agent knows exactly what to fix when re-dispatched after a Clara rejection.
  </action>
  <verify>
Read buildTaskMessage function and confirm it checks reviewStatus/reviewNotes.
Run `npx tsc --noEmit` — zero errors.
  </verify>
  <done>
Re-dispatched agents receive Clara's rejection feedback as part of their task instructions. The agent knows exactly what needs fixing.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` succeeds
- [ ] Grep confirms `dispatchTask` is called in post-review rejection handler
- [ ] Grep confirms `reviewNotes` is included in task message for rejected tasks
- [ ] No new TypeScript errors introduced
</verification>

<success_criteria>

- Clara post-review rejection triggers automatic agent re-dispatch
- Re-dispatched agent receives Clara's feedback in its instructions
- Task stays in in-progress during the fix cycle
- Activity log shows 'redispatch-after-rejection' entries
- No dead-end states — rejected tasks always get re-worked

</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-critical-fix/01-01-SUMMARY.md`
</output>
