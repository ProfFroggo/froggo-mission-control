# Plan 02-02: Verify Database Creates Correctly

**Phase**: 2 — Database Layer
**Goal**: DB file created at ~/froggo/data/froggo.db with all tables, agents seeded

## Tasks

### Task 1: Verify database creation
Run node script to call `getDb()` and check all tables exist. Must show all 18 tables.

### Task 2: Verify agent seed data
Query agents table — must have all 13 agents from registry.

### Task 3: Verify chat room seed data
Query chat_rooms table — must have 4 rooms (general, code-review, planning, incidents).

## Success Criteria
- `~/froggo/data/froggo.db` created
- All 18 tables present in sqlite_master
- 13 agents seeded
- 4 chat rooms seeded
