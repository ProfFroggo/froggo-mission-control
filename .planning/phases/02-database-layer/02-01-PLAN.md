# Plan 02-01: Create src/lib/database.ts with Full Schema

**Phase**: 2 — Database Layer
**Goal**: `getDb()` singleton with WAL mode and complete schema — all tables created on first run

## Tasks

### Task 1: Create src/lib/database.ts
Create with:
- `getDb()` singleton (lazy init, module-level `_db`)
- WAL mode + foreign keys pragmas
- `initSchema()` with ALL tables: tasks, subtasks, task_activity, task_labels, task_attachments, agents, sessions, messages, approvals, inbox, chat_rooms, chat_room_messages, agent_sessions, scheduled_items, module_state, analytics_events, library_files, settings
- All indexes
- Seed: 4 default chat rooms + 13 agents from registry
- DB_PATH = `FROGGO_DB_PATH` env || `~/froggo/data/froggo.db`
- Auto-create directory if missing

Source: fork-merge.md §2.1 (complete implementation provided)

### Task 2: Verify TypeScript compiles
Run `npx tsc --noEmit` — must be clean.

## Success Criteria
- `src/lib/database.ts` exists with complete schema
- `tsc --noEmit` passes
- `getDb` and default export both exported
