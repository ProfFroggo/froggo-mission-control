---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/secret-store.ts
  - electron/x-api-client.ts
  - electron/connected-accounts-service.ts
  - electron/main.ts
  - src/components/VoiceChatPanel.tsx
  - src/components/TeamVoiceMeeting.tsx
  - src/components/QuickActions.tsx
  - src/components/MeetingsPanel.tsx
  - src/components/MeetingScribe.tsx
  - src/components/MeetingTranscribe.tsx
  - src/lib/gateway.ts
  - src/store/userSettings.ts
  - src/components/EnhancedSettingsPanel.tsx
  - src/components/SettingsPanel.tsx
  - src/components/MorningBrief.tsx
  - src/components/CalendarWidget.tsx
  - src/components/QuickModals.tsx
  - src/components/CommsInbox3Pane.tsx
  - src/lib/priorityScoring.ts
  - src/lib/smartAccountSelector.ts
  - src/lib/folderRules.ts
  - src/components/ConnectedAccountsPanel.tsx
autonomous: true

must_haves:
  truths:
    - "grep -rn for known API tokens returns zero hits in src/ and electron/"
    - "grep -rn for known PII strings returns zero hits in src/ and electron/ (excluding .bak files)"
    - "The encryption key 'default-key-change-me-in-production' no longer appears in source"
    - "electron/secret-store.ts exists with storeSecret/getSecret/hasSecret/deleteSecret using Electron safeStorage"
    - "App builds successfully with npm run electron:build"
  artifacts:
    - path: "electron/secret-store.ts"
      provides: "Keychain-backed secret storage via Electron safeStorage"
      exports: ["storeSecret", "getSecret", "hasSecret", "deleteSecret"]
    - path: "src/store/userSettings.ts"
      provides: "User settings with placeholder defaults instead of real PII"
      contains: "placeholder values (not real PII)"
  key_links:
    - from: "electron/x-api-client.ts"
      to: "electron/secret-store.ts"
      via: "getSecret('x-bearer-token') call"
      pattern: "getSecret.*x-bearer"
    - from: "electron/connected-accounts-service.ts"
      to: "electron/secret-store.ts"
      via: "safeStorage for encryption instead of hardcoded key"
      pattern: "safeStorage|getSecret"
    - from: "src/components/VoiceChatPanel.tsx"
      to: "window.clawdbot.settings.getApiKey"
      via: "IPC call to main process for Gemini key"
      pattern: "clawdbot.*getApiKey|settings.*gemini"
---

<objective>
Remove all hardcoded credentials, API tokens, and PII from source code. Replace with Electron safeStorage (for secrets) and the existing useUserSettings store (for PII).

Purpose: Addresses SEC-01 (API tokens), SEC-02 (PII), and SEC-07 (encryption key). After this plan, no sensitive data remains in committed source files.

Output: `electron/secret-store.ts` (new), updated token-consuming files, sanitized PII defaults in userSettings store and all referencing components.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening/01-RESEARCH.md

Key source files:
@electron/database.ts
@electron/x-api-client.ts
@electron/connected-accounts-service.ts
@src/lib/gateway.ts
@src/components/VoiceChatPanel.tsx
@src/store/userSettings.ts
@electron/main.ts (for IPC handler registration — add settings:getApiKey handler)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create secret store and migrate all hardcoded tokens</name>
  <files>
    electron/secret-store.ts (new)
    electron/x-api-client.ts
    electron/connected-accounts-service.ts
    src/components/VoiceChatPanel.tsx
    src/components/TeamVoiceMeeting.tsx
    src/components/QuickActions.tsx
    src/components/MeetingsPanel.tsx
    src/components/MeetingScribe.tsx
    src/components/MeetingTranscribe.tsx
    src/lib/gateway.ts
    electron/main.ts (add IPC handlers for settings:getApiKey and settings:storeApiKey)
  </files>
  <action>
    1. Create `electron/secret-store.ts` implementing safeStorage-backed secret storage:
       - `storeSecret(key: string, value: string): void` — encrypts with safeStorage, writes to `~/.openclaw/credentials/dashboard/{key}.enc`
       - `getSecret(key: string): string | null` — reads and decrypts, returns null if missing
       - `hasSecret(key: string): boolean` — checks existence
       - `deleteSecret(key: string): void` — removes file
       - Sanitize key to filesystem-safe characters (alphanumeric, dash, underscore only)
       - IMPORTANT: safeStorage can only be called after app.ready. The functions should throw a clear error if called too early.

    2. Modify `electron/x-api-client.ts`:
       - Remove hardcoded `BEARER_TOKEN` and `ACCESS_TOKEN` constants (lines 7-8)
       - Add `let bearerToken: string | null = null; let accessToken: string | null = null;`
       - Add `export function initXApiTokens(): void` that loads tokens from secret store: `bearerToken = getSecret('x-bearer-token'); accessToken = getSecret('x-access-token');`
       - Update `bearerHeaders` to use the mutable `bearerToken` variable
       - All functions that use tokens should check for null and throw descriptive error: `'X API token not configured. Add it in Settings > API Keys.'`
       - For dev fallback: check `process.env.X_BEARER_TOKEN` and `process.env.X_ACCESS_TOKEN` before secret store

    3. Modify `electron/connected-accounts-service.ts`:
       - Remove hardcoded `ENCRYPTION_KEY` constant (line 30)
       - Replace the `encrypt()`/`decrypt()` functions to use `safeStorage.encryptString()`/`safeStorage.decryptString()` directly instead of AES with a hardcoded key
       - The encrypt function should return a base64-encoded Buffer (since safeStorage returns a Buffer)
       - The decrypt function should accept base64 string, convert to Buffer, then call `safeStorage.decryptString()`
       - Also fix `DB_PATH` on line 27 from `~/Froggo/clawd/data/froggo.db` to `~/froggo/data/froggo.db` (wrong path)

    4. Modify `src/lib/gateway.ts`:
       - Remove hardcoded `DEFAULT_TOKEN` constant (line 4)
       - Change to `const DEFAULT_TOKEN = '';` (empty string — forces loading from settings)
       - The `getSettings()` function already loads from localStorage (`froggo-settings`), which is the correct flow

    5. Remove hardcoded Gemini API keys from ALL 6 files. NOTE: Two different keys exist: `[REDACTED]...` and `[REDACTED]...`. All must use the same IPC pattern: `await window.clawdbot.settings.getApiKey('gemini')`.

       - `src/components/VoiceChatPanel.tsx` (line 25): Remove `FALLBACK_GEMINI_API_KEY = '[REDACTED]...'`. Update `loadApiKey()` to: first check Vite env vars (existing), then call `await window.clawdbot.settings.getApiKey('gemini')` via IPC, then return empty string if nothing found. Since `loadApiKey` is currently sync, make it async (called in useEffect).

       - `src/components/TeamVoiceMeeting.tsx` (line 26): Remove `FALLBACK_GEMINI_API_KEY = '[REDACTED]...'`. Same IPC pattern as VoiceChatPanel.

       - `src/components/QuickActions.tsx` (line 605): Remove the hardcoded `return '[REDACTED]...'` in the getGeminiKey helper. Replace with IPC call to `window.clawdbot.settings.getApiKey('gemini')`.

       - `src/components/MeetingsPanel.tsx` (line 21): Remove `GEMINI_API_KEY = '[REDACTED]...'`. Replace with IPC call.

       - `src/components/MeetingScribe.tsx` (line 15): Remove `GEMINI_API_KEY = '[REDACTED]...'`. Replace with IPC call.

       - `src/components/MeetingTranscribe.tsx` (line 16): Remove `FALLBACK_GEMINI_API_KEY = '[REDACTED]...'`. Replace with IPC call.

    6. Register IPC handlers in `electron/main.ts` (add near the settings section):
       - `settings:getApiKey` — takes key name string, returns `getSecret(key)` from secret-store
       - `settings:storeApiKey` — takes (key, value), calls `storeSecret(key, value)`, returns success
       - `settings:hasApiKey` — takes key name, returns `hasSecret(key)`
       - Make sure these are registered AFTER app.ready (safeStorage requirement)

    AVOID: Do NOT use `.env` files for the packaged Electron app — they don't survive electron-builder. Use safeStorage for runtime secrets, env vars only as dev fallback.
  </action>
  <verify>
    Run `grep -rn '[REDACTED]\|[REDACTED]\|[REDACTED]\|[REDACTED]\|default-key-change-me' electron/ src/ --include='*.ts' --include='*.tsx'` — must return zero results (covers both Gemini keys, Twitter Bearer, X Access Token pattern, gateway token, and encryption key).
    Run `npx tsc --noEmit` from the project root — must compile without errors.
  </verify>
  <done>
    All hardcoded secrets removed from source: Twitter Bearer, X Access Token, 2 Gemini API keys ([REDACTED]... in 3 files, [REDACTED]... in 3 files), Gateway token, Encryption key. Secret store module exists. IPC handlers registered. Build compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded PII with settings store references</name>
  <files>
    src/store/userSettings.ts
    src/components/EnhancedSettingsPanel.tsx
    src/components/SettingsPanel.tsx
    src/components/MorningBrief.tsx
    src/components/CalendarWidget.tsx
    src/components/QuickModals.tsx
    src/components/CommsInbox3Pane.tsx
    src/components/VoiceChatPanel.tsx (voice system prompt section ~line 1105)
    src/lib/priorityScoring.ts
    src/lib/smartAccountSelector.ts
    src/lib/folderRules.ts
    src/components/ConnectedAccountsPanel.tsx
    electron/connected-accounts-service.ts (GOG_ACCOUNTS array ~line 123)
  </files>
  <action>
    1. Update `src/store/userSettings.ts` defaults (lines 21-31):
       - Change `name` from hardcoded value to `''` (empty — forces user to set in Settings)
       - Change `email` from hardcoded value to `''`
       - Change `phone` from hardcoded value to `''`
       - Change `emailAccounts` from the 3 hardcoded accounts to `[]` (empty array)
       - Existing users with localStorage data won't be affected (persist middleware loads saved values)

    2. For each component that hardcodes PII as a default/fallback:
       - `src/components/EnhancedSettingsPanel.tsx` line ~152: Replace hardcoded email with `useUserSettings.getState().emailAccounts[0]?.email || ''`
       - `src/components/SettingsPanel.tsx` line ~103: Same pattern
       - `src/components/MorningBrief.tsx` line ~134: Same pattern — use settings store for calendar account
       - `src/components/CalendarWidget.tsx` lines ~39, ~52: Replace with settings store reference
       - `src/components/QuickModals.tsx` line ~28: Replace with settings store reference
       - `src/components/CommsInbox3Pane.tsx` lines ~103-104: Replace both email refs with settings store
       - `src/components/ConnectedAccountsPanel.tsx` line ~338: Replace example email in placeholder text with generic `'user@example.com'`

    3. For lib files with PII:
       - `src/lib/priorityScoring.ts` line ~92: Replace hardcoded name with settings store reference (or make the comparison dynamic)
       - `src/lib/smartAccountSelector.ts` lines ~119, ~240: Replace hardcoded email with settings store reference
       - `src/lib/folderRules.ts` line ~252: Replace hardcoded domain with dynamic reference from email accounts

    4. For `src/components/VoiceChatPanel.tsx` line ~1105:
       - The system prompt contains name, location, and family details. Replace with a template that reads from useUserSettings: `const { name } = useUserSettings.getState();` and interpolate into the prompt string
       - Remove specific family details (wife/daughters), location (Gibraltar), and employer references

    5. For `electron/connected-accounts-service.ts` lines ~123-126:
       - Replace hardcoded `GOG_ACCOUNTS` array with an empty default
       - Add a method `setGogAccounts(accounts: string[])` or read from the settings store via IPC

    AVOID: Don't break the UI for existing users — the Zustand persist middleware will load saved values from localStorage on startup, so changing defaults only affects fresh installs.
  </action>
  <verify>
    Run `grep -rn` for known PII strings in `src/ electron/ --include='*.ts' --include='*.tsx'` — must return zero results (excluding .bak files).
    Run `npx tsc --noEmit` — must compile without errors.
  </verify>
  <done>
    All 14 PII locations from the research inventory show zero hardcoded personal data. User settings store has generic placeholder defaults. All components read from the settings store or accept dynamic values. Build compiles.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn` for known API tokens in `electron/ src/` returns empty
2. `grep -rn` for known PII strings in `src/ electron/` returns empty (excluding .bak files)
3. `npx tsc --noEmit` succeeds
4. `electron/secret-store.ts` exists with all 4 exported functions
5. `npm run electron:build` completes successfully
</verification>

<success_criteria>
- Zero hardcoded secrets in src/ and electron/ (SEC-01 complete)
- Zero hardcoded PII in src/ and electron/ (SEC-02 complete)
- Encryption key derived from safeStorage, not hardcoded (SEC-07 complete)
- App compiles and builds without errors
- Existing user data preserved via localStorage persistence
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-01-SUMMARY.md`
</output>
