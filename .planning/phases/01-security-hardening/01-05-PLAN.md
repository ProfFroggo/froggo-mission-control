---
phase: 01-security-hardening
plan: 05
type: execute
wave: 3
depends_on: ["01-04"]
files_modified: ["electron/main.ts"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero .replace(/'/g patterns in tasks:create/sync handler"
    - "Zero .replace(/'/g patterns in task attachment handlers"
    - "Zero .replace(/'/g patterns in library handlers"
    - "Zero .replace(/'/g patterns in inbox:addWithMetadata, inbox:list, inbox:markRead, inbox:filter handlers"
    - "Zero exec/execSync sqlite3 calls in any of these handlers"
    - "All queries use prepare().run/get/all() with ? params"
  artifacts:
    - path: "electron/main.ts"
      provides: "Parameterized task, attachment, library, and inbox handlers"
      contains: "prepare("
  key_links:
    - from: "electron/main.ts (task/library/inbox handlers)"
      to: "electron/database.ts"
      via: "import { prepare } from './database'"
      pattern: "prepare\\("
---

<objective>
Migrate tasks:create, task attachment, library, and remaining inbox IPC handlers from shell-exec sqlite3 to parameterized queries.

Purpose: Close SQL injection gap — these ~20 sites use manual escaping with shell-exec sqlite3.
Output: All task, attachment, library, and remaining inbox handlers use `prepare().run/get/all()` with `?` parameters.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-security-hardening/01-02-SUMMARY.md
@electron/database.ts
@electron/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate tasks:create, attachment, and library handlers</name>
  <files>electron/main.ts</files>
  <action>
  Migrate tasks:create/sync, task attachment, and library handlers to prepare() queries.

  **tasks:sync** (or tasks:create — around line 996):
  - Currently uses `exec()` to run `froggo-db task-get` then falls through to shell-exec sqlite3 INSERT
  - The `froggo-db task-get` CLI call can stay (it's a read-only check via CLI tool, not SQL injection)
  - Replace the INSERT shell-exec with: `prepare('INSERT OR REPLACE INTO tasks (id, title, description, status, project, assigned_to, priority, due_date, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)').run(task.id, task.title, task.description || '', task.status || 'todo', task.project || 'Default', task.assignedTo || '', task.priority || '', dueStr, nowMs, nowMs)`
  - Remove the 3 `.replace(/'/g, "''")` calls on title, desc, project

  **attachments:add** (~line 1985):
  - Replace shell-exec INSERT with: `prepare('INSERT INTO task_attachments (task_id, file_path, filename, file_size, mime_type, category, uploaded_by, uploaded_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?)').run(taskId, filePath, filename, fileSize, mimeType, category, uploadedBy, now)`
  - Get lastInsertRowid from RunResult for the attachment id
  - Remove `.replace(/'/g, "''")` on filePath and filename
  - The `froggo-db task-activity` CLI call can stay (it's a fire-and-forget log, not SQL injection)

  **attachments:delete** (~line 2023):
  - Replace shell-exec SELECT with: `prepare('SELECT task_id, filename FROM task_attachments WHERE id = ?').get(attachmentId)`
  - Replace shell-exec DELETE with: `prepare('DELETE FROM task_attachments WHERE id = ?').run(attachmentId)`

  **attachments:list** (~line 2047 — check if it uses shell exec):
  - If it uses shell exec with interpolation, migrate to: `prepare('SELECT * FROM task_attachments WHERE task_id = ? ORDER BY uploaded_at DESC').all(taskId)`
  - Check for any `.replace(/'/g` on filename around line 2052

  **library:add** (~line 4460):
  - Currently does CREATE TABLE IF NOT EXISTS + INSERT in one shell-exec call
  - Split: use `db.exec('CREATE TABLE IF NOT EXISTS library ...')` for DDL (no params needed)
  - Then: `prepare('INSERT INTO library (id, name, path, category, size) VALUES (?, ?, ?, ?, ?)').run(fileId, fileName, destPath, category, stats.size)`
  - Remove `.replace(/'/g, "''")` on fileName and destPath

  **library:delete** (~line 4491):
  - Replace two shell-exec calls with:
    - `prepare('SELECT path FROM library WHERE id = ?').get(fileId)` — get path
    - `prepare('DELETE FROM library WHERE id = ?').run(fileId)` — delete record
  - Note: `fileId` is used directly in shell exec currently — parameterize it

  **library:link** (~line 4513):
  - Replace shell-exec SELECT + UPDATE with:
    - `prepare('SELECT linked_tasks FROM library WHERE id = ?').get(fileId)`
    - `prepare('UPDATE library SET linked_tasks = ?, updated_at = datetime(\'now\') WHERE id = ?').run(JSON.stringify(linkedTasks), fileId)`

  **library:view** (~line 4535):
  - Replace shell-exec SELECT with: `prepare('SELECT path, mime_type, name FROM library WHERE id = ?').get(fileId)`
  - NOTE: This handler currently references `~/Froggo/clawd/data/froggo.db` (wrong path!) — it will now use the shared `prepare()` which uses the correct DB path from database.ts

  IMPORTANT:
  - Do NOT change IPC channel names or response formats
  - For library:add, the CREATE TABLE IF NOT EXISTS DDL should use `db.exec()` (imported from database.ts) since it has no user params
  </action>
  <verify>
  Run: `grep -n "replace(/'/g" electron/main.ts | grep -iE "title|desc|project|filePath|filename|fileName|destPath|fileId|library"` — should return zero lines for these handlers.
  Run: `grep -n "exec.*sqlite3.*task_attachments\|exec.*sqlite3.*library" electron/main.ts` — should return zero lines.
  </verify>
  <done>All tasks:create, attachment, and library handlers use prepare() with ? params. Zero manual escaping. Zero shell-exec sqlite3. library:view bug-fixed to use correct DB path.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining inbox handlers and email auto-check</name>
  <files>electron/main.ts</files>
  <action>
  Migrate remaining inbox handlers that still use shell-exec sqlite3.

  **inbox:addWithMetadata** (~line 3329):
  - Replace shell-exec INSERT with: `prepare('INSERT INTO inbox (type, title, content, context, status, source_channel, metadata, created) VALUES (?, ?, ?, ?, ?, ?, ?, datetime(\'now\'))').run(item.type, item.title, item.content, item.context || '', 'pending', item.channel || 'system', item.metadata || '{}')`
  - Remove 4 `.replace(/'/g, "''")` calls on title, content, context, metadata

  **inbox:list** (~line 3360):
  - Replace shell-exec SELECT with: `prepare('SELECT * FROM inbox WHERE status = ? ORDER BY created DESC LIMIT 50').all(effectiveStatus)`
  - The `effectiveStatus` is code-controlled (defaults to 'pending') but still parameterize for consistency

  **inbox:markRead** (~line 3630):
  - Replace `runMsgCmd(sqlite3 ... UPDATE comms_cache ...)` with: `prepare('UPDATE comms_cache SET is_read = ? WHERE external_id = ?').run(isRead ? 1 : 0, messageId)`
  - Remove `.replace(/'/g, "''")` on messageId
  - The `inbox-filter.sh` CLI call can stay (it's a script, not SQL)

  **inbox:filter** (~line 3722):
  - The `.replace(/'/g, "\\'")` on criteriaJson is for shell pipe, not SQL injection
  - This is a shell escaping pattern passing JSON to a script via pipe
  - Replace with a safer approach: write criteriaJson to stdin using child_process spawn, or pass as base64
  - OR: If the filter script queries the DB, move the filtering logic to prepare() queries directly
  - Minimum fix: replace `echo '${criteriaJson}'` with `Buffer.from(JSON.stringify(criteria)).toString('base64')` piped to the script, or use `execSync` with `input` option: `execSync('~/clawd/scripts/inbox-filter.sh filter', { input: JSON.stringify(criteria), encoding: 'utf-8' })`

  **inbox revision creation** (~line 3530):
  - The `inbox:revise` handler around line 3518-3536 currently uses `exec(sqlite3 ... SELECT ... WHERE id=${originalId})` then creates a revision with escaped strings
  - Replace SELECT with: `prepare('SELECT * FROM inbox WHERE id = ?').get(originalId)`
  - Replace revision INSERT with: `prepare('INSERT INTO inbox (type, title, content, context, status, source_channel, created) VALUES (?, ?, ?, ?, ?, ?, datetime(\'now\'))').run(type, newTitle, revisedContent, context, 'pending', channel)`
  - Remove `.replace(/'/g, "''")` on newTitle, revisedContent, feedback

  **Email auto-check inbox INSERT** (~line 6043):
  - The `runImportantEmailCheck` function creates inbox items via shell-exec
  - Replace with: `prepare('INSERT INTO inbox (type, title, content, context, status, source_channel, created) VALUES (?, ?, ?, ?, ?, ?, datetime(\'now\'))').run('email', title, content, priority + ' priority', 'pending', 'email')`
  - Remove `.replace(/"/g, '\\"').replace(/'/g, "''")` on title and content

  IMPORTANT:
  - Do NOT change IPC channel names or response formats
  - inbox:add was already migrated in 01-02 — do NOT touch it
  - inbox:approveAll was already migrated in 01-02 — do NOT touch it
  - inbox:listRevisions was already migrated in 01-02 — do NOT touch it
  </action>
  <verify>
  Run: `grep -n "replace(/'/g" electron/main.ts | grep -iE "inbox|Inbox|addWithMetadata|markRead|escapedTitle|escapedContent|escapedContext|escapedMetadata"` — should return zero lines for these handlers.
  Run: `grep -n "exec.*sqlite3.*inbox\|exec.*sqlite3.*comms_cache.*is_read" electron/main.ts` — only pre-existing migrated handlers should remain (if any).
  </verify>
  <done>All remaining inbox handlers and email auto-check use prepare() with ? params. Zero manual SQL escaping in inbox/email code. Zero shell-exec sqlite3 for DB operations.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "replace(/'/g" electron/main.ts` — count should decrease by ~20 from baseline
2. No task, attachment, library, or inbox handler uses exec/execSync with sqlite3 for user-controlled input
3. TypeScript compiles without errors
</verification>

<success_criteria>
- Zero `.replace(/'/g, "''")` in tasks:create, attachment, library, inbox:addWithMetadata/list/markRead/revise handlers
- Zero shell-exec sqlite3 calls in these handlers
- All queries use `prepare()` with `?` parameter placeholders
- Response formats unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-05-SUMMARY.md`
</output>
