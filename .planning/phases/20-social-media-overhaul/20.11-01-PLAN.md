---
phase: 20-social-media-overhaul
plan: 01
type: execute
---

<objective>
Upgrade the automation builder with more action types, AI engine selector, execution history UI, and conversational creation via agent chat.

Purpose: Make automations the central control surface for all social module background operations.
Output: Full-featured automation builder + execution log + agent-driven creation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-social-media-overhaul/ROADMAP.md
@src/components/XAutomationsTab.tsx
@app/api/x/automations/route.ts
@app/api/x/automations/execute/route.ts
@src/components/XAgentChatPane.tsx

Current trigger types: mention, keyword, time, follower, dm
Current action types: reply, like, retweet, dm, add_to_list, process_mentions, report
Missing: AI engine selector per automation, post_content action, execution history UI, conversational wizard
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI engine selector + new action types to automation builder</name>
  <files>src/components/XAutomationsTab.tsx, app/api/x/automations/execute/route.ts</files>
  <action>
  Update XAutomationsTab builder form:
  - Add `ai_engine` field to automation schema: 'gemini' | 'claude' (default: 'gemini')
  - Add a select dropdown in the builder between trigger and actions: "AI Engine: Gemini Flash Lite (fast, cheap) / Claude Haiku (interactive)"
  - Add new action types to ACTION_OPTIONS:
    - process_mentions: "Process mentions" — fetch + AI reply generation
    - report: "Generate report" — competitor analysis or weekly summary (add report_type config: competitor-analysis | weekly-summary)
    - post_content: "Post content" — create a draft post in pipeline (needs content template config)
  - Add new trigger type: 'engagement' — triggers when a mention exceeds a like/retweet threshold (config: min_likes, min_retweets)
  - Store ai_engine in the automation metadata when saving

  Update execute/route.ts:
  - Read ai_engine from automation config
  - For 'reply' action with ai_engine='claude': use /api/chat/generate-reply instead of hardcoded template
  - For 'post_content' action: create x_post via POST /api/x/posts
  - Add 'engagement' trigger evaluator: query x_mentions WHERE like_count >= config.min_likes OR retweet_count >= config.min_retweets AND fetched_at > lastExecutedAt
  </action>
  <verify>npx tsc --noEmit passes. API returns new action/trigger types. Builder shows AI engine dropdown.</verify>
  <done>Builder has AI engine selector, 3 new action types, engagement trigger. Execution engine handles all new types.</done>
</task>

<task type="auto">
  <name>Task 2: Add execution history UI + conversational creation</name>
  <files>src/components/XAutomationsTab.tsx, src/components/XAgentChatPane.tsx</files>
  <action>
  Execution History UI in XAutomationsTab:
  - Below the automation list, add an "Execution Log" section
  - Show `executionLog` state (already loaded from API) as a compact table/list
  - Each entry: timestamp, automation name, trigger type, actions taken, status (executed/error)
  - Collapsible detail showing full trigger_data and action results
  - Max 20 entries, "Load more" button
  - Filter by automation name

  Conversational Creation:
  - Add a "Create with AI" button at the top of automations tab
  - When clicked, opens the agent chat panel (dispatches event to open chat) with a pre-filled prompt:
    "I want to create a new automation for my X/Twitter account. Help me define the trigger, conditions, and actions. When ready, output the automation as a ```automation JSON block."
  - The agent chat already parses ```automation blocks and creates via POST /api/x/automations (wired in previous commit)
  - Add a listener in XAutomationsTab for 'x-automation-created' custom event → reload automations list
  - Show toast "Automation created via AI" when event received
  </action>
  <verify>npx tsc --noEmit passes. Execution log renders with real data. "Create with AI" opens chat. Automations refresh on creation.</verify>
  <done>Execution history visible in UI. Conversational creation works end-to-end. Automation list refreshes on AI creation.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` passes
- [ ] Automation builder shows AI engine dropdown (gemini/claude)
- [ ] New action types visible: process_mentions, report, post_content
- [ ] Engagement trigger type available
- [ ] Execution log renders with timestamps and status
- [ ] "Create with AI" button opens agent chat with prompt
- [ ] Agent ```automation blocks create automations and refresh list
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Automation builder is the central control surface for social background operations
- User can create automations via builder UI or conversational AI
- Execution history is visible and filterable
</success_criteria>

<output>
After completion, create `.planning/phases/20-social-media-overhaul/20.11-01-SUMMARY.md`
</output>
