# Plan: Measure (Analytics + Content Mix) + Configure (Agent Mode + Automations)

## Objective

Absorb XContentMixTracker as a card within XEnhancedAnalyticsView. Create XConfigureView wrapping Agent Mode + Automations + Credentials as sub-tabs.

## Execution Context

@src/components/XEnhancedAnalyticsView.tsx (1325 lines)
@src/components/XContentMixTracker.tsx (363 lines)
@src/components/XAgentContentQueue.tsx (704 lines — agent mode)
@src/components/XAutomationsTab.tsx (720 lines — IFTTT builder)
@src/components/XSetupWizard.tsx (404 lines — credentials)

## Tasks

### Task 1: Absorb Content Mix into Analytics

**Requirements:**
- XEnhancedAnalyticsView gets a new section/card: "Content Distribution"
- The card renders the content mix chart (pie chart + progress bars from XContentMixTracker)
- Uses the same `/api/x/analytics` data that's already loaded
- Remove XContentMixTracker as a standalone component import from routing
- Position: after the main metrics charts, before the suggestions section
- Card styling: `rounded-xl border border-mission-control-border bg-mission-control-surface p-6`
- Period filter should be shared with the main analytics period

### Task 2: Build XConfigureView

Create `src/components/XConfigureView.tsx`.

**Sub-tabs:**
- Agent Mode — renders XAgentContentQueue (agent toggle, brief, draft queue)
- Automations — renders XAutomationsTab (IFTTT rule builder)
- Credentials — renders link/button to re-open XSetupWizard or inline credential status

**Requirements:**
- Sub-tab bar matching XIntelligenceView style (consistency)
- Default sub-tab: 'agent-mode'
- Each sub-component rendered as-is (minimal changes)
- Credentials sub-tab: show current credential status (which keys are set), "Reconfigure" button to re-open wizard

### Task 3: Update routing and agent chat contexts

**Requirements:**
- Update XTab type and routing for 'measure' → XEnhancedAnalyticsView and 'configure' → XConfigureView
- Update XAgentChatPane TAB_CONTEXT:
  - 'measure' → analytics-focused context combining analytics + content mix
  - 'configure' → configuration-focused context covering agent mode + automations
- Update quick prompts for both new tabs

## Verification

```bash
npx tsc --noEmit
```

- Measure tab shows analytics with content mix card integrated
- Configure tab shows Agent Mode / Automations / Credentials sub-tabs
- Agent chat contexts updated for new tab names

## Success Criteria

- [ ] Content Mix renders as a card within Analytics (not separate tab)
- [ ] XConfigureView has 3 sub-tabs working
- [ ] Credentials sub-tab shows key status
- [ ] Routing and chat contexts updated
- [ ] TypeScript compiles clean
