---
phase: 20-social-media-overhaul
plan: 02
type: execute
---

<objective>
Merge dev branch into main, bump patch version in package.json, and run npm publish to release the social module v20 milestone.
</objective>

<execution_context>~/.claude/get-shit-done/workflows/execute-phase.md</execution_context>

<context>
@.planning/STATE.md
package.json
</context>

<tasks>

<task type="auto">
**Bump patch version in package.json**

Current version: 1.9.3
New version: 1.9.4

Edit `package.json`, change `"version": "1.9.3"` to `"version": "1.9.4"`.

Commit: `git add package.json && git commit -m "chore(20.12-02): bump version to 1.9.4"`
</task>

<task type="auto">
**Merge dev → main**

```bash
git checkout main
git merge dev --no-ff -m "feat: social module v20 — 5-tab layout, automation engine, AI replies, competitor intel"
git checkout dev
```

Note: If GitHub CLI is authenticated, also push main to remote after merge.
</task>

<task type="auto">
**npm publish**

From project root on main branch (or after merge, on dev):
```bash
npm publish
```

If publish fails due to authentication, note the error and continue. Document result in summary.
</task>

</tasks>

<verification>
- package.json version is 1.9.4
- main branch contains all dev commits (git log shows social module commits on main)
- npm publish either succeeded or failed with a clear error message
</verification>

<success_criteria>
- Version bumped to 1.9.4
- dev merged into main successfully
- npm publish attempted (success = milestone complete; auth failure = noted, not a blocker)
- dev branch is current branch at end
</success_criteria>

<output>
After completion, create `.planning/phases/20-social-media-overhaul/20.12-02-SUMMARY.md`
</output>
