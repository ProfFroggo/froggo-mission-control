---
phase: 11-chat-layout
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - src/components/writing/FeedbackPopover.tsx
  - src/components/writing/ChatPane.tsx
  - src/components/writing/ProjectEditor.tsx
autonomous: false

must_haves:
  truths:
    - "FeedbackPopover uses :feedback session key suffix — no session contamination with chat"
    - "Chat history persists across app restart (load from JSONL on project open)"
    - "Pane collapse state persists across sessions (localStorage)"
    - "User can copy and retry chat messages"
    - "Layout works at 1024px, 1280px, 1440px, and 1920px window widths"
  artifacts:
    - path: "src/components/writing/FeedbackPopover.tsx"
      provides: "Updated session key with :feedback suffix"
      contains: ":feedback"
  key_links:
    - from: "src/components/writing/FeedbackPopover.tsx"
      to: "src/lib/gateway.ts"
      via: "session key with :feedback suffix prevents contamination"
      pattern: ":feedback"
    - from: "src/components/writing/ChatPane.tsx"
      to: "electron/writing-chat-service.ts"
      via: "IPC bridge for loadHistory/appendMessage on project open/message send"
      pattern: "clawdbot.*chat.*loadHistory"
---

<objective>
Fix FeedbackPopover session key to use :feedback suffix, add copy/retry message actions, verify chat history persistence works end-to-end, ensure collapse state persistence, and verify the full feature at multiple screen widths.

Purpose: This plan closes the remaining requirements (CHAT-04, CHAT-08, CHAT-10, LAYOUT-04) and verifies the complete feature meets all success criteria before marking the phase complete.
Output: Updated FeedbackPopover, polished chat actions, and verified end-to-end feature.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-chat-layout/11-RESEARCH.md
@.planning/phases/11-chat-layout/11-01-SUMMARY.md
@.planning/phases/11-chat-layout/11-02-SUMMARY.md
@.planning/phases/11-chat-layout/11-03-SUMMARY.md

Key source files to read:
@src/components/writing/FeedbackPopover.tsx (update session key on line 258)
@src/components/writing/ChatPane.tsx (verify persistence, add retry)
@src/components/writing/ChatMessage.tsx (add copy/retry actions)
@src/components/writing/ProjectEditor.tsx (verify collapse persistence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix session keys and add copy/retry actions</name>
  <files>
    src/components/writing/FeedbackPopover.tsx
    src/components/writing/ChatMessage.tsx
    src/components/writing/ChatPane.tsx
  </files>
  <action>
1. **Fix `src/components/writing/FeedbackPopover.tsx`** — Update session key to prevent contamination with chat pane:
   - Find line 258 (or search for the session key construction): `const sessionKey = \`agent:${selectedAgent}:writing:${activeProjectId}\``
   - Change to: `const sessionKey = \`agent:${selectedAgent}:writing:${activeProjectId}:feedback\``
   - Also find the fact-check session key (line ~323): `const sessionKey = \`agent:researcher:writing:${activeProjectId}\``
   - Change to: `const sessionKey = \`agent:researcher:writing:${activeProjectId}:feedback\``
   - Verify that `buildMemoryContext` is already imported from `../../lib/writingContext` (refactored in Plan 01). If for any reason it still has an inline copy, replace it with the import.

2. **Add retry to `src/components/writing/ChatMessage.tsx`**:
   - Add a "Retry" button on assistant messages (visible on hover, next to Copy):
     - Icon: `RotateCcw` from lucide-react
     - On click: remove this message AND the preceding user message from store, put the user message content back into `input` field
     - Implementation: Accept an `onRetry?: (userContent: string) => void` prop. The parent ChatPane provides this.
   - Verify "Copy" button exists (should be from Plan 03). If missing, add: `navigator.clipboard.writeText(message.content)` with a brief "Copied!" tooltip/state.

3. **Add retry handler to `src/components/writing/ChatPane.tsx`**:
   - Implement retry: when user clicks retry on an assistant message, find the preceding user message, remove both from store, and prefill the input.
   - Implementation: Pass `onRetry` callback to ChatMessage. The callback:
     a. Finds the message index
     b. Removes the assistant message and the user message before it
     c. Sets `input` to the removed user message's content
   - Add this as a method that modifies the messages array in chatPaneStore (or just filter out the two messages directly).

4. **Verify collapse state persistence** in `src/components/writing/ProjectEditor.tsx`:
   - Ensure the `onLayoutChanged` callback also persists collapse state (the layout array from react-resizable-panels already includes collapsed panels as 0-width entries, so the existing `defaultLayout` restore should handle this automatically).
   - If collapsed panels are NOT restored correctly on reload, add separate localStorage keys for `writing-chapters-collapsed` and `writing-chat-collapsed`, saved in `onCollapse`/`onExpand` callbacks, and read on mount to call `panelRef.current?.collapse()` if needed.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Verify feedback session key: `grep ":feedback" src/components/writing/FeedbackPopover.tsx` returns 2 lines (regular + fact-check).
    Verify no old session key: `grep "writing:${activeProjectId}\`" src/components/writing/FeedbackPopover.tsx` returns 0 lines without :feedback suffix.
    Verify retry: `grep "onRetry\|RotateCcw" src/components/writing/ChatMessage.tsx` returns 2+ lines.
    Verify buildMemoryContext import: `grep "import.*buildMemoryContext.*writingContext" src/components/writing/FeedbackPopover.tsx` returns 1 line.
    Verify buildMemoryContext removed: `grep -c "function buildMemoryContext" src/components/writing/FeedbackPopover.tsx` returns 0.
  </verify>
  <done>
    FeedbackPopover uses :feedback session key suffix on both regular and fact-check session keys.
    FeedbackPopover imports buildMemoryContext from writingContext.ts (no duplicate code).
    ChatMessage has copy and retry action buttons.
    ChatPane handles retry by removing message pair and prefilling input.
    Collapse state persists across sessions.
    TypeScript compiles clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 11: Chat Pane + 3-Pane Layout feature:
    1. 3-pane resizable layout (chapters | AI chat | editor) with collapsible panes
    2. AI chat with agent selection (Writer, Researcher, Jess) and streaming responses
    3. Chat-to-editor content insertion ("Send to editor" button)
    4. Chat history persistence across app restarts
    5. Layout and collapse state persistence
    6. Session key isolation between chat and inline feedback
  </what-built>
  <how-to-verify>
    Build and launch the dev app:
    ```bash
    cd ~/froggo-dashboard
    npm run build:dev
    open release/dev/mac-arm64/Froggo\ Dev.app
    ```

    **Test 1 — Layout:**
    1. Navigate to Writing workspace, open a project
    2. Verify 3 panes visible: chapters sidebar (left), chat pane (center), editor (right)
    3. Drag the separators — panes resize smoothly
    4. Collapse the chapters sidebar — it hides completely, toggle button appears to restore
    5. Collapse the chat pane — same behavior
    6. Resize window to ~1024px wide — layout still works, no overflow

    **Test 2 — Chat:**
    1. Type a message in the chat input
    2. Select an agent (Writer, Researcher, or Jess)
    3. Send the message — AI response should stream in token by token
    4. Verify the response renders as formatted markdown

    **Test 3 — Send to Editor:**
    1. Get an AI response in the chat
    2. Click "Send to editor" on the response
    3. Content should appear at the end of the current chapter in the editor
    4. The button should change to "Inserted" badge

    **Test 4 — Persistence:**
    1. Send a few messages in chat
    2. Collapse the chapters sidebar
    3. Quit the app completely
    4. Reopen the app, navigate to the same project
    5. Chat history should be restored
    6. Chapters sidebar should still be collapsed
    7. Pane sizes should be preserved

    **Test 5 — Session Isolation:**
    1. Send a chat message about "write a paragraph about the ocean"
    2. Select text in the editor, use the inline feedback (BubbleMenu)
    3. Inline feedback should NOT reference the chat conversation
    4. Return to chat — it should NOT reference the feedback instructions

    **Test 6 — Copy/Retry:**
    1. Click "Copy" on an AI message — content copies to clipboard
    2. Click "Retry" on an AI message — the message pair is removed, user message goes back to input
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build:dev` succeeds
3. All 6 test scenarios pass in the dev app
4. All 15 requirements (CHAT-01 through CHAT-10, LAYOUT-01 through LAYOUT-05) satisfied
</verification>

<success_criteria>
All Phase 11 success criteria met:
1. 3-pane layout with drag-resizable and collapsible panes
2. Agent chat with streaming responses
3. Chat-to-editor content insertion
4. Chat history, pane sizes, and collapse states persist across app restart
5. Layout works at 1024px to 1920px+
</success_criteria>

<output>
After completion, create `.planning/phases/11-chat-layout/11-04-SUMMARY.md`
</output>
