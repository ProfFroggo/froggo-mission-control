---
phase: 11-chat-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/chatPaneStore.ts
  - src/store/writingStore.ts
  - src/lib/writingContext.ts
  - src/components/writing/FeedbackPopover.tsx
  - electron/writing-chat-service.ts
  - electron/main.ts
  - electron/preload.ts
autonomous: true

must_haves:
  truths:
    - "chatPaneStore manages messages, streaming state, agent selection, and input independently from feedbackStore"
    - "writingStore has pendingInsert mechanism for decoupled chat-to-editor content flow"
    - "writingContext.ts exports shared context builders used by both FeedbackPopover and ChatPane"
    - "IPC handlers persist and load chat history as JSONL per project on disk"
  artifacts:
    - path: "src/store/chatPaneStore.ts"
      provides: "Chat pane Zustand store with messages, streaming, agent selection"
      exports: ["useChatPaneStore", "ChatMessage"]
    - path: "src/store/writingStore.ts"
      provides: "pendingInsert + clearPendingInsert + setPendingInsert"
      contains: "PendingInsert"
    - path: "src/lib/writingContext.ts"
      provides: "Shared context builders for AI prompts"
      exports: ["buildMemoryContext", "buildChapterContext", "buildOutlineContext"]
    - path: "electron/writing-chat-service.ts"
      provides: "IPC handlers for chat history CRUD"
      exports: ["registerWritingChatHandlers"]
  key_links:
    - from: "src/store/chatPaneStore.ts"
      to: "src/store/writingStore.ts"
      via: "import for pendingInsert access"
      pattern: "writingStore.*setPendingInsert"
    - from: "electron/writing-chat-service.ts"
      to: "electron/paths.ts"
      via: "writingMemoryPath for chat-history.jsonl location"
      pattern: "writingMemoryPath"
    - from: "electron/main.ts"
      to: "electron/writing-chat-service.ts"
      via: "registerWritingChatHandlers import and call"
      pattern: "registerWritingChatHandlers"
    - from: "src/components/writing/FeedbackPopover.tsx"
      to: "src/lib/writingContext.ts"
      via: "import buildMemoryContext (replacing inline function)"
      pattern: "import.*buildMemoryContext.*writingContext"
---

<objective>
Create the data layer and shared utilities for the chat pane: chatPaneStore (Zustand), pendingInsert mechanism in writingStore, shared context builders extracted from FeedbackPopover, and IPC handlers for chat history persistence.

Purpose: These stores, utilities, and IPC handlers are the foundation that the chat UI components (Plan 02-03) will consume. They must exist before any UI can be built.
Output: 4 new/modified files providing chat state management, editor insertion mechanism, shared context utilities, and chat history persistence.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-chat-layout/11-RESEARCH.md

Key source files to read:
@src/store/feedbackStore.ts (pattern to follow for chatPaneStore)
@src/store/writingStore.ts (add pendingInsert mechanism)
@src/components/writing/FeedbackPopover.tsx (extract buildMemoryContext, context window logic)
@electron/paths.ts (writingMemoryPath for chat history location)
@electron/main.ts (register new IPC handlers — find writing service registration pattern)
@electron/preload.ts (add writing.chat bridge)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chatPaneStore, pendingInsert, and writingContext utility</name>
  <files>
    src/store/chatPaneStore.ts
    src/store/writingStore.ts
    src/lib/writingContext.ts
  </files>
  <action>
1. **Create `src/store/chatPaneStore.ts`** — Follow the `feedbackStore.ts` pattern exactly. Zustand store with:
   - State: `messages: ChatMessage[]`, `input: string`, `streaming: boolean`, `streamContent: string`, `selectedAgent: string` (default 'writer'), `error: string | null`
   - ChatMessage interface: `{ id: string; role: 'user' | 'assistant'; content: string; agent: string; timestamp: number; insertedToEditor?: boolean }`
   - Actions: `setInput`, `setSelectedAgent`, `setStreaming`, `setStreamContent`, `addMessage`, `setError`, `markInserted`, `clearMessages`, `loadMessages`
   - Export both `useChatPaneStore` and the `ChatMessage` type
   - See RESEARCH.md "Complete Chat Pane Store" for exact implementation

2. **Modify `src/store/writingStore.ts`** — Add pendingInsert mechanism:
   - Add `PendingInsert` interface: `{ content: string; mode: 'append' | 'cursor' | 'replace'; sourceMessageId?: string }`
   - Add to `WritingState` interface: `pendingInsert: PendingInsert | null`, `setPendingInsert: (insert: PendingInsert) => void`, `clearPendingInsert: () => void`
   - Add to initial state: `pendingInsert: null`
   - Add actions: `setPendingInsert: (insert) => set({ pendingInsert: insert })`, `clearPendingInsert: () => set({ pendingInsert: null })`
   - Do NOT change any existing state or actions

3. **Create `src/lib/writingContext.ts`** — Extract shared context builders from FeedbackPopover:
   - `buildMemoryContext(characters, timeline, facts)` — exact logic from FeedbackPopover lines 21-53
   - `buildChapterContext(chapterContent, cursorOffset?)` — extract the chapter context windowing logic from FeedbackPopover `buildPrompt` (lines 119-128)
   - `buildOutlineContext(chapters)` — extract outline building from FeedbackPopover `buildPrompt` (lines 131-137)
   - All three functions are pure (no React hooks, no store access) — they take data and return strings
   - See RESEARCH.md "Shared Context Utility" for reference implementation

4. **Refactor `src/components/writing/FeedbackPopover.tsx`** — Replace inline `buildMemoryContext` with import:
   - Add import: `import { buildMemoryContext } from '../../lib/writingContext';`
   - Remove the local `buildMemoryContext` function definition (lines 21-53) — it is now provided by writingContext.ts
   - No other changes to FeedbackPopover in this task — session key suffix changes happen in Plan 04
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Verify chatPaneStore exports: `grep -c "useChatPaneStore\|ChatMessage" src/store/chatPaneStore.ts` returns 2+.
    Verify pendingInsert in writingStore: `grep "pendingInsert" src/store/writingStore.ts` returns 4+ lines.
    Verify writingContext exports: `grep -c "export function" src/lib/writingContext.ts` returns 3.
    Verify FeedbackPopover imports writingContext: `grep "import.*buildMemoryContext.*writingContext" src/components/writing/FeedbackPopover.tsx` returns 1 line.
    Verify FeedbackPopover inline function removed: `grep -c "function buildMemoryContext" src/components/writing/FeedbackPopover.tsx` returns 0.
  </verify>
  <done>
    chatPaneStore.ts exists with full ChatMessage type and all actions.
    writingStore.ts has PendingInsert type and setPendingInsert/clearPendingInsert actions.
    writingContext.ts exports buildMemoryContext, buildChapterContext, buildOutlineContext as pure functions.
    FeedbackPopover.tsx imports buildMemoryContext from writingContext.ts (inline function removed).
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IPC handlers for chat history persistence and wire preload bridge</name>
  <files>
    electron/writing-chat-service.ts
    electron/main.ts
    electron/preload.ts
  </files>
  <action>
1. **Create `electron/writing-chat-service.ts`** — Follow the `writing-memory-service.ts` pattern for IPC handler registration:
   - Import `ipcMain` from electron, `writingMemoryPath` from `./paths`, `fs` and `path`
   - Export function `registerWritingChatHandlers()` that registers 3 IPC handlers:
     - `'writing:chat:loadHistory'` — params: `(event, projectId: string)`. Read `writingMemoryPath(projectId, 'chat-history.jsonl')`. Parse each line as JSON. Return `{ success: true, messages: ChatMessage[] }`. If file doesn't exist, return empty array.
     - `'writing:chat:appendMessage'` — params: `(event, projectId: string, message: object)`. Append `JSON.stringify(message) + '\n'` to `chat-history.jsonl`. Create parent directory if needed with `fs.mkdirSync(dir, { recursive: true })`. Return `{ success: true }`.
     - `'writing:chat:clearHistory'` — params: `(event, projectId: string)`. Delete the `chat-history.jsonl` file if it exists. Return `{ success: true }`.
   - Use `ipcMain.handle()` (not `ipcMain.on()`) for all three — consistent with existing writing service pattern.
   - Wrap all handlers in try/catch returning `{ success: false, error: err.message }`.

2. **Modify `electron/main.ts`** — Register the new service:
   - Add import: `import { registerWritingChatHandlers } from './writing-chat-service';`
   - Call `registerWritingChatHandlers()` in the same location where other writing services are registered (search for `registerWritingMemoryHandlers` or similar registration calls).

3. **Modify `electron/preload.ts`** — Add chat bridge:
   - Find the `writing` object in the preload bridge (search for `writing:` or `clawdbot.writing`).
   - Add a `chat` sub-object with:
     - `loadHistory: (projectId: string) => ipcRenderer.invoke('writing:chat:loadHistory', projectId)`
     - `appendMessage: (projectId: string, message: any) => ipcRenderer.invoke('writing:chat:appendMessage', projectId, message)`
     - `clearHistory: (projectId: string) => ipcRenderer.invoke('writing:chat:clearHistory', projectId)`
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors in electron/*.ts.
    Verify IPC handlers: `grep -c "ipcMain.handle.*writing:chat" electron/writing-chat-service.ts` returns 3.
    Verify main.ts registration: `grep "registerWritingChatHandlers" electron/main.ts` returns at least 1 line.
    Verify preload bridge: `grep "chat:" electron/preload.ts` returns at least 1 line.
  </verify>
  <done>
    writing-chat-service.ts exists with 3 IPC handlers (loadHistory, appendMessage, clearHistory).
    main.ts imports and calls registerWritingChatHandlers().
    preload.ts exposes writing.chat bridge with loadHistory, appendMessage, clearHistory methods.
    Chat history is stored as JSONL at ~/froggo/writing-projects/{projectId}/memory/chat-history.jsonl.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. chatPaneStore, writingStore (pendingInsert), writingContext, writing-chat-service all exist
3. Preload bridge exposes writing.chat.loadHistory/appendMessage/clearHistory
4. No existing functionality broken (feedbackStore, writingStore actions unchanged)
</verification>

<success_criteria>
All data layer infrastructure for chat pane is in place: state management (chatPaneStore), editor insertion bridge (pendingInsert), shared context utilities (writingContext.ts), and disk persistence (writing-chat-service with JSONL). TypeScript compiles clean. No regressions to existing writing features.
</success_criteria>

<output>
After completion, create `.planning/phases/11-chat-layout/11-01-SUMMARY.md`
</output>
