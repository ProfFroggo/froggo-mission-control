---
phase: 11-chat-layout
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/writing/ChatPane.tsx
  - src/components/writing/ChatMessage.tsx
  - src/components/writing/ChatInput.tsx
  - src/components/writing/ChapterEditor.tsx
  - src/components/writing/ProjectEditor.tsx
autonomous: true

must_haves:
  truths:
    - "User can type a message in the chat input, select an agent, and send it"
    - "AI responses stream token-by-token in the chat pane via gateway.sendChatWithCallbacks"
    - "User can click 'Send to editor' on an assistant message and see content inserted into the TipTap editor"
    - "Chat messages scroll to bottom on new messages"
    - "Chat pane uses session key with :chat suffix, separate from feedback :feedback suffix"
  artifacts:
    - path: "src/components/writing/ChatPane.tsx"
      provides: "Main chat pane container with message list, streaming display, and send handler"
      exports: ["default"]
    - path: "src/components/writing/ChatMessage.tsx"
      provides: "Individual message component with copy and send-to-editor actions"
      exports: ["default"]
    - path: "src/components/writing/ChatInput.tsx"
      provides: "Message input with agent picker and send button"
      exports: ["default"]
    - path: "src/components/writing/ChapterEditor.tsx"
      provides: "pendingInsert watcher + Selection extension + @tiptap/markdown"
      contains: "pendingInsert"
  key_links:
    - from: "src/components/writing/ChatPane.tsx"
      to: "src/lib/gateway.ts"
      via: "gateway.sendChatWithCallbacks for streaming"
      pattern: "sendChatWithCallbacks"
    - from: "src/components/writing/ChatPane.tsx"
      to: "src/store/chatPaneStore.ts"
      via: "useChatPaneStore for state"
      pattern: "useChatPaneStore"
    - from: "src/components/writing/ChatMessage.tsx"
      to: "src/store/writingStore.ts"
      via: "setPendingInsert for chat-to-editor insertion"
      pattern: "setPendingInsert"
    - from: "src/components/writing/ChapterEditor.tsx"
      to: "src/store/writingStore.ts"
      via: "useEffect watching pendingInsert to insert content"
      pattern: "pendingInsert.*insertContent"
    - from: "src/components/writing/ProjectEditor.tsx"
      to: "src/components/writing/ChatPane.tsx"
      via: "renders ChatPane in center panel (replacing placeholder)"
      pattern: "<ChatPane"
---

<objective>
Build the chat pane UI components (ChatPane, ChatMessage, ChatInput), wire streaming via gateway, implement chat-to-editor content insertion via pendingInsert, and add TipTap Selection + Markdown extensions to ChapterEditor.

Purpose: This is the core feature — the user can chat with AI agents in a persistent pane and insert AI-generated content into the editor. It brings together all infrastructure from Plans 01 and 02.
Output: 3 new components, 2 modified components, fully functional chat-to-editor flow.
</objective>

<execution_context>
@/Users/worker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-chat-layout/11-RESEARCH.md
@.planning/phases/11-chat-layout/11-01-SUMMARY.md
@.planning/phases/11-chat-layout/11-02-SUMMARY.md

Key source files to read:
@src/store/chatPaneStore.ts (from Plan 01)
@src/store/writingStore.ts (pendingInsert from Plan 01)
@src/lib/writingContext.ts (from Plan 01)
@src/components/writing/ProjectEditor.tsx (from Plan 02 — replace center pane placeholder)
@src/components/writing/ChapterEditor.tsx (add extensions + pendingInsert watcher)
@src/components/writing/FeedbackPopover.tsx (streaming pattern reference)
@src/components/writing/AgentPicker.tsx (reuse in ChatInput)
@src/lib/gateway.ts (sendChatWithCallbacks API)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatPane, ChatMessage, and ChatInput components</name>
  <files>
    src/components/writing/ChatPane.tsx
    src/components/writing/ChatMessage.tsx
    src/components/writing/ChatInput.tsx
  </files>
  <action>
1. **Create `src/components/writing/ChatInput.tsx`** (~60 lines):
   - Props: `onSend: (message: string) => void`, `disabled: boolean`
   - Uses `useChatPaneStore` for `input`, `setInput`, `selectedAgent`, `setSelectedAgent`
   - Renders: AgentPicker component (reuse existing from `./AgentPicker`) + textarea + send button
   - Textarea: auto-resizes (1 to 4 rows), sends on Enter (Shift+Enter for newline), clears on send
   - Send button: `Send` icon from lucide-react, disabled when empty or `disabled` prop is true
   - Styling: `bg-clawd-surface border-t border-clawd-border p-3` container, dark input matching existing theme

2. **Create `src/components/writing/ChatMessage.tsx`** (~80 lines):
   - Props: `message: ChatMessage`, `isStreaming?: boolean`
   - Renders user messages as right-aligned bubbles, assistant messages as left-aligned
   - Assistant messages rendered with `react-markdown` + `remark-gfm` (both already installed)
   - Action buttons on assistant messages (visible on hover):
     - "Send to editor" button: calls `useWritingStore.getState().setPendingInsert({ content: message.content, mode: 'append', sourceMessageId: message.id })` then calls `useChatPaneStore.getState().markInserted(message.id)`
     - "Copy" button: copies `message.content` to clipboard via `navigator.clipboard.writeText()`
   - If `message.insertedToEditor` is true, show a subtle "Inserted" badge instead of "Send to editor"
   - Streaming message: render `streamContent` from store with a blinking cursor indicator
   - Icons: `ArrowDownToLine` (send to editor), `Copy`, `Check` (inserted) from lucide-react

3. **Create `src/components/writing/ChatPane.tsx`** (~120 lines):
   - Main container: `flex flex-col h-full bg-clawd-bg`
   - Header: agent name display + "Clear" button to reset conversation
   - Message list: scrollable `div` with `ref` for auto-scroll. Maps over `messages` from `useChatPaneStore`. Renders `ChatMessage` for each. If `streaming`, render a streaming `ChatMessage` at the bottom with `streamContent`.
   - Auto-scroll: `useEffect` that scrolls to bottom when messages change or streamContent changes. Use `scrollRef.current?.scrollIntoView({ behavior: 'smooth' })` on a bottom sentinel div.
   - Empty state: centered text "Start a conversation with {agentName}" when no messages
   - Footer: `<ChatInput onSend={handleSendMessage} disabled={streaming} />`
   - **handleSendMessage function** — follows the FeedbackPopover streaming pattern exactly:
     a. Read `input`, `selectedAgent` from chatPaneStore
     b. Read `activeProjectId`, `activeChapterContent`, `activeProject` from writingStore
     c. Read `characters`, `timeline`, `facts` from memoryStore
     d. Create userMessage ChatMessage, add to store, clear input
     e. Set `streaming: true`, `streamContent: ''`
     f. Build session key: `agent:${selectedAgent}:writing:${activeProjectId}:chat` (NOTE the `:chat` suffix)
     g. Build context using `buildMemoryContext`, `buildChapterContext`, `buildOutlineContext` from `writingContext.ts`
     h. Build prompt: system preamble (same agent preambles as FeedbackPopover) + context sections + user message
     i. Call `gateway.sendChatWithCallbacks(prompt, sessionKey, { onDelta, onEnd, onError })`:
        - `onDelta`: accumulate into ref, call `setStreamContent`
        - `onEnd`: create assistantMessage, add to store, set `streaming: false`, persist both messages via IPC (`window.clawdbot.writing.chat.appendMessage`)
        - `onError`: set error, set `streaming: false`
   - **Chat history loading**: `useEffect` on `activeProjectId` change — call `window.clawdbot.writing.chat.loadHistory(projectId)` and `loadMessages()` into store. On project close, `clearMessages()`.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Verify gateway integration: `grep "sendChatWithCallbacks" src/components/writing/ChatPane.tsx` returns 1+ lines.
    Verify session key suffix: `grep ":chat" src/components/writing/ChatPane.tsx` returns 1+ lines.
    Verify pendingInsert: `grep "setPendingInsert" src/components/writing/ChatMessage.tsx` returns 1+ lines.
    Verify react-markdown: `grep "ReactMarkdown\|react-markdown" src/components/writing/ChatMessage.tsx` returns 1+ lines.
  </verify>
  <done>
    ChatPane.tsx renders message list + streaming display + input area.
    ChatMessage.tsx renders messages with markdown and action buttons.
    ChatInput.tsx renders agent picker + textarea + send button.
    Gateway streaming works with :chat session key suffix.
    "Send to editor" calls setPendingInsert on writingStore.
    Chat history loads from disk on project open.
    TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ChatPane into layout and add editor pendingInsert watcher + TipTap extensions</name>
  <files>
    src/components/writing/ProjectEditor.tsx
    src/components/writing/ChapterEditor.tsx
  </files>
  <action>
1. **Modify `src/components/writing/ProjectEditor.tsx`** (from Plan 02):
   - Replace the center pane placeholder with: `import ChatPane from './ChatPane';` and render `<ChatPane />` inside the center Panel.
   - Change the `MessageSquare` icon import to use for the chat collapse toggle button label.

2. **Modify `src/components/writing/ChapterEditor.tsx`** — Add 3 things:

   a. **Add TipTap extensions** to the `useEditor` config:
      - Import `{ Selection }` from `'@tiptap/extensions'`
      - Import `{ Markdown }` from `'@tiptap/markdown'`
      - Add `Selection` to the extensions array (after Link) — preserves selection highlight on blur
      - Add `Markdown` to the extensions array — enables `insertContent(md, { contentType: 'markdown' })`

   b. **Add pendingInsert watcher** — new `useEffect`:
      ```
      const { pendingInsert, clearPendingInsert } = useWritingStore();

      useEffect(() => {
        if (!pendingInsert || !editor || editor.isDestroyed) return;

        switch (pendingInsert.mode) {
          case 'append':
            editor.chain().focus('end')
              .insertContent(pendingInsert.content, { contentType: 'markdown' })
              .run();
            break;
          case 'cursor':
            editor.chain().focus()
              .insertContent(pendingInsert.content, { contentType: 'markdown' })
              .run();
            break;
          case 'replace':
            if (!editor.state.selection.empty) {
              const { from, to } = editor.state.selection;
              editor.chain().focus()
                .insertContentAt({ from, to }, pendingInsert.content, { contentType: 'markdown' })
                .run();
            } else {
              // Fallback to append if no selection
              editor.chain().focus('end')
                .insertContent(pendingInsert.content, { contentType: 'markdown' })
                .run();
            }
            break;
        }
        clearPendingInsert();
      }, [pendingInsert]);
      ```

   c. **Add selection tracking** — save editor selection to writingStore for future use by chat pane "insert at cursor" mode. Add an `onSelectionUpdate` callback to the editor config that stores `{from, to}`. This is optional (append mode works without it) but enables cursor-aware insertion.

   IMPORTANT: Do NOT remove or modify any existing editor functionality (autosave, BubbleMenu, FeedbackPopover, word count).
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Verify ChatPane in layout: `grep "ChatPane" src/components/writing/ProjectEditor.tsx` returns 2+ lines (import + render).
    Verify Selection extension: `grep "Selection" src/components/writing/ChapterEditor.tsx` returns 1+ lines.
    Verify Markdown extension: `grep "Markdown" src/components/writing/ChapterEditor.tsx` returns 1+ lines.
    Verify pendingInsert watcher: `grep "pendingInsert" src/components/writing/ChapterEditor.tsx` returns 3+ lines.
    Verify no v3 TipTap issues: `grep "contentType.*markdown" src/components/writing/ChapterEditor.tsx` returns 1+ lines.
  </verify>
  <done>
    ChatPane renders in center panel of 3-pane layout.
    ChapterEditor has Selection extension (selection visible on blur).
    ChapterEditor has Markdown extension (insertContent with contentType:'markdown' works).
    pendingInsert watcher inserts content into editor on append/cursor/replace modes.
    All existing editor features (autosave, BubbleMenu, word count) still work.
    TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. ChatPane visible in center pane of 3-pane layout
3. User can type message, select agent, send — AI response streams token by token
4. "Send to editor" on assistant message inserts content into TipTap editor
5. Chat uses `:chat` session key suffix (not same as feedback)
6. Editor selection stays visible when clicking into chat input
7. Existing FeedbackPopover, autosave, BubbleMenu all work unchanged
</verification>

<success_criteria>
User can chat with AI agents in the center pane, see streaming responses, and insert AI-generated content into the editor with one click. Chat uses separate gateway sessions from inline feedback. Editor selection is preserved when chat input has focus.
</success_criteria>

<output>
After completion, create `.planning/phases/11-chat-layout/11-03-SUMMARY.md`
</output>
