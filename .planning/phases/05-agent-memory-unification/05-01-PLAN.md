---
phase: 05-agent-memory-unification
plan: 01
type: execute
---

<objective>
Unify fragmented agent memory into single structured directory per agent.

Purpose: Agents have one place for all memory. Dispatcher reads from it, MCP writes to it.
Output: ~/mission-control/agents/{id}/memory/ for all 14 agents with migrated files.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create unified memory dirs + migrate existing files</name>
  <files>N/A — filesystem operations via bash</files>
  <action>
  For all 14 agents: create ~/mission-control/agents/{id}/memory/ and ~/mission-control/agents/{id}/handoffs/.
  Migrate files from:
  - ~/mission-control/memory/agents/{id}/*.md → agents/{id}/memory/
  - ~/mission-control/memory/memory/agents/{id}/*.md → agents/{id}/memory/
  - ~/mission-control/memory/agents/{id}/handoffs/*.md → agents/{id}/handoffs/ (if exists)
  Create README.md in each memory dir with schema description.
  </action>
  <verify>All 14 agents have memory/ and handoffs/ dirs</verify>
  <done>All agent memory files consolidated under ~/mission-control/agents/{id}/memory/</done>
</task>

<task type="auto">
  <name>Task 2: Update code paths to read/write from unified location</name>
  <files>src/lib/taskDispatcher.ts, src/lib/claraReviewCron.ts, tools/memory-mcp/src/index.ts</files>
  <action>
  1. taskDispatcher.ts loadRelevantMemory(): change agentDir from memory/agents/{id} to agents/{id}/memory
  2. taskDispatcher.ts loadHandoffNote(): change handoff path from memory/agents/*/handoffs/ to agents/*/handoffs/
  3. claraReviewCron.ts: change Clara pattern memory write path from memory/agents/clara/agent-patterns/ to agents/clara/memory/agent-patterns/
  4. memory-mcp src/index.ts: change CATEGORY_FOLDER and memory_write routing to write to agents/{id}/memory/ instead of memory/agents/ and memory/memory/agents/
  5. Rebuild MCP: cd tools/memory-mcp && npm run build
  </action>
  <verify>npx tsc --noEmit passes, MCP build succeeds</verify>
  <done>All code reads/writes from unified agents/{id}/memory/ path.</done>
</task>

</tasks>
