---
phase: 15-env-and-config
plan: "01"
type: execute
---

<objective>
Validate the Phase 15 environment config deliverables and wire database.ts to use the centralized ENV.DB_PATH from src/lib/env.ts.

Purpose: Make env.ts the true single source of truth for all paths. Currently .env, env.ts, and COST_STRATEGY.md exist and are complete. The only remaining gap is database.ts reads process.env.MC_DB_PATH directly, bypassing the centralized env wrapper.

Output:
- database.ts imports ENV from src/lib/env.ts and uses ENV.DB_PATH
- No direct process.env.MC_DB_PATH usage in src/
- TypeScript compiles clean
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/env.ts
@src/lib/database.ts

**Phase 15 deliverables already in place:**
- `.env` — complete (MC_DB_PATH, VAULT_PATH, LIBRARY_PATH, PROJECT_DIR, LOG_DIR, QMD_BIN, MODEL_LEAD, MODEL_WORKER, MODEL_TRIVIAL, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, TMUX_SESSION)
- `src/lib/env.ts` — complete typed wrapper with resolveHome(), all paths, all model tiers
- `COST_STRATEGY.md` — complete three-tier model hierarchy doc

**Remaining gap:**
- `src/lib/database.ts:10` uses `process.env.MC_DB_PATH` directly
- Should import `ENV` from `./env` and use `ENV.DB_PATH`
- MCP server at tools/ uses env vars directly (intentional — separate package, cannot import from src/)

**Established patterns:**
- ENV is a const object exported from src/lib/env.ts
- resolveHome() handles ~ expansion
- MC_DB_PATH is the env var name; ENV.DB_PATH is the resolved path
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire database.ts to use ENV.DB_PATH from env.ts</name>
  <files>src/lib/database.ts</files>
  <action>
    1. Add import at top: `import { ENV } from './env';`
    2. Replace the DB_PATH constant block (currently reads process.env.MC_DB_PATH with fallback + path.join) with simply: `const DB_PATH = ENV.DB_PATH;`
    3. Remove the now-unused direct imports of `path` and `os` if they were only used for DB_PATH resolution (check if they're used elsewhere in the file first — if used for dbDir = path.dirname(DB_PATH), keep path; if os.homedir() is referenced elsewhere, keep os).
    4. Do NOT change the dbDir, mkdir, or Database initialization logic — only the DB_PATH assignment.
    Note: env.ts already handles the MC_DB_PATH env var, fallback path construction, and ~ expansion via resolveHome(). No need to duplicate that logic.
  </action>
  <verify>grep -n "process.env.MC_DB_PATH" src/lib/database.ts — should return no matches</verify>
  <done>database.ts imports ENV from ./env and uses ENV.DB_PATH. No direct process.env.MC_DB_PATH reference in src/lib/database.ts.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript verification and audit</name>
  <files>none (verification only)</files>
  <action>
    1. Run `npx tsc --noEmit` — must pass with 0 errors.
    2. Scan for any other direct process.env.MC_DB_PATH usage in src/: `grep -r "MC_DB_PATH" src/`
       - If found elsewhere in src/, update those to use ENV.DB_PATH too.
       - tools/ MCP server references are intentional (separate package) — leave them.
    3. Confirm env.ts exports are correct: `grep "export const ENV" src/lib/env.ts`
  </action>
  <verify>npx tsc --noEmit exits 0</verify>
  <done>TypeScript clean. No MC_DB_PATH references in src/. ENV.DB_PATH is the single source of truth for DB path in the app layer.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `grep -r "process.env.MC_DB_PATH" src/` returns no matches
- [ ] `src/lib/database.ts` imports ENV from ./env
- [ ] `.env` file has all required variables (MC_DB_PATH, VAULT_PATH, MODEL_LEAD, MODEL_WORKER, MODEL_TRIVIAL, TMUX_SESSION)
- [ ] `COST_STRATEGY.md` exists at repo root
</verification>

<success_criteria>
- All tasks completed
- database.ts uses ENV.DB_PATH (not direct process.env)
- TypeScript compiles clean
- env.ts is the single source of truth for all paths and model tiers in src/
- Phase 15 deliverables verified: .env ✓, env.ts ✓, COST_STRATEGY.md ✓
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-and-config/15-01-SUMMARY.md`:

---
phase: 15-env-and-config
plan: "01"
subsystem: infra
tags: [env, config, database, typescript]

requires: []
provides:
  - ENV singleton as single source of truth for all paths and model tiers
  - database.ts wired to ENV.DB_PATH
  - COST_STRATEGY.md documenting three-tier model hierarchy

tech-stack:
  added: []
  patterns: ["Import ENV from src/lib/env.ts for all path/model config", "Never use process.env directly in src/ — always via ENV"]

key-files:
  modified: [src/lib/database.ts]
  verified: [.env, src/lib/env.ts, COST_STRATEGY.md]

key-decisions:
  - "ENV.DB_PATH replaces direct process.env.MC_DB_PATH in database.ts"
  - "MCP servers in tools/ use env vars directly (separate packages, cannot import from src/)"

affects: [16, 17, 19]
---

# Phase 15 Plan 01: Env & Config Summary

**[One-liner of what shipped]**

## Accomplishments

- [Deliverables verified and wired]

## Files Created/Modified

- `src/lib/database.ts` — imports ENV.DB_PATH from env.ts
- `.env` — verified complete (all 11 vars)
- `src/lib/env.ts` — verified (no changes needed)
- `COST_STRATEGY.md` — verified complete

## Decisions Made

- ENV.DB_PATH is canonical; tools/ MCP servers use env vars directly (separate packages)

## Issues Encountered

[None or describe]

## Next Phase Readiness

Ready for Phase 16 (Tmux Orchestration) — ENV.TMUX_SESSION available from env.ts
</output>
