# Phase 22 — Automations Overhaul Plan

## Goal
Upgrade automation `create-task` step to support structured subtasks with per-subtask `{title, assignedTo}` objects, and verify templates gallery wiring is functional.

## Analysis

### Current state
- `AutomationStepBuilder.tsx` `create-task` case uses `subtasks: string[]` (plain titles only)
- `AutomationTemplatesGallery.tsx` has `onUseTemplate` prop — already wired in `AutomationsPanel.tsx` via `handleUseGalleryTemplate` which creates automation via POST /api/automations
- Template flow: gallery → create via API → appears in My Automations list (already works)

### What needs changing
1. `AutomationStepBuilder.tsx` — upgrade subtask UI from string[] to `{title: string, assignedTo: string}[]`
2. `src/lib/automationExecutor.ts` — ensure executor handles both old format (string) and new format (object) for subtasks
3. Verify gallery → builder modal prefill path works (currently gallery creates directly via API, not via builder modal — this is acceptable behavior)

## Tasks

### Task 1: Upgrade create-task subtask UI
- Change subtask items from plain string to `{title: string, assignedTo: string}`
- Add assignedTo input field per subtask row
- Use Lucide icons (Users, Plus, Trash2) — no emojis
- Backward compat: if subtask is a string, treat as `{title: string, assignedTo: ''}`

### Task 2: Verify automationExecutor handles new subtask format
- Read `src/lib/automationExecutor.ts` create-task handler
- Ensure it can handle `{title, assignedTo}` objects
- Also handle string[] for backward compat

### Task 3: Verify template gallery wiring
- Confirm "From Template" button → gallery → "Use this template" → creates automation → appears in list
- No code change needed if already working

## Implementation Notes
- Use `inputStyle` and `selectClassName` already defined in the file
- Subtask assignedTo: text input with placeholder "agent id (optional)"
- Keep the existing string[] fallback handling
