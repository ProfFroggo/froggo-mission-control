---
phase: 77-audit-migration-map
plan: 01
type: execute
---

<objective>
Systematically catalog every Tailwind utility class pattern used across all src/ TSX files and fix the missing @radix-ui/themes package.json registration.

Purpose: Phases 78–85 migration agents need to know exactly which patterns exist, how frequently, and which have no Radix equivalent — so nothing is missed and the mapping table is grounded in real usage, not guesswork.
Output: `.planning/phases/77-audit-migration-map/AUDIT.md` — structured findings with occurrence counts per category, plus @radix-ui/themes properly registered in package.json.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tailwind.config.js
@src/index.css
@src/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register @radix-ui/themes in package.json</name>
  <files>package.json</files>
  <action>
@radix-ui/themes 3.3.0 is installed in node_modules but absent from package.json dependencies — it was manually placed there. Add it properly:

Run: npm install @radix-ui/themes

This ensures it's tracked as a real dependency and survives npm install in fresh environments. Verify the installed version remains 3.3.0 (do not upgrade).
  </action>
  <verify>grep "@radix-ui/themes" package.json confirms it appears in dependencies</verify>
  <done>@radix-ui/themes present in package.json dependencies at version ^3.3.0</done>
</task>

<task type="auto">
  <name>Task 2: Run comprehensive Tailwind utility audit across all TSX components</name>
  <files>.planning/phases/77-audit-migration-map/AUDIT.md</files>
  <action>
Run grep commands across all src/**/*.tsx files to catalog Tailwind utility usage by category. Write findings to AUDIT.md.

Commands to run (adapt as needed):
```bash
# All className values, one word per line, sorted by frequency
grep -roh --include="*.tsx" 'className="[^"]*"' src/ | \
  grep -oE '[a-zA-Z0-9:_/-]+' | sort | uniq -c | sort -rn > /tmp/tw-all.txt

# Count files using Tailwind layout
grep -rl --include="*.tsx" 'className="[^"]*flex' src/ | wc -l
grep -rl --include="*.tsx" 'className="[^"]*grid' src/ | wc -l
```

Structure AUDIT.md with these sections and counts for each pattern found:

## Summary
- Total TSX files scanned: [N]
- Total unique className patterns: [N]
- Files using flex layout: [N]
- Files using grid layout: [N]

## Top 50 Most-Used Patterns (by occurrence count)
Table: | Pattern | Count | Radix equivalent? |

## Category Breakdown

### Layout Primitives
Patterns: flex, flex-col, flex-row, flex-1, flex-wrap, flex-shrink-0, flex-grow, inline-flex, grid, block, hidden
For each: count occurrences

### Alignment & Justify
Patterns: items-center, items-start, items-end, items-stretch, justify-between, justify-center, justify-start, justify-end
For each: count

### Gap / Spacing
Patterns: gap-1 through gap-16, gap-x-*, gap-y-*
For each: count

### Padding
Patterns: p-1 through p-12, px-*, py-*, pt-*, pr-*, pb-*, pl-*
For each: count (focus on values actually used — not exhaustive)

### Margin
Patterns: m-*, mx-*, my-*, mt-*, mr-*, mb-*, ml-*, -m-*, space-x-*, space-y-*
For each: count

### Sizing
Patterns: w-full, w-*, h-full, h-*, min-w-*, max-w-*, min-h-*, max-h-*
For each: count

### Typography
Patterns: text-xs, text-sm, text-base, text-lg, text-xl, text-2xl, text-3xl, font-normal, font-medium, font-semibold, font-bold, leading-*, tracking-*, truncate, line-clamp-*
For each: count

### Border / Radius
Patterns: rounded-sm, rounded, rounded-lg, rounded-xl, rounded-2xl, rounded-full, border (standalone), border-b, border-t, border-l, border-r
For each: count

### Non-Migratable (keep as className — no Radix prop equivalent)
overflow-hidden, overflow-auto, overflow-y-auto, overflow-x-auto, scroll-*
relative, absolute, fixed, sticky, top-*, left-*, right-*, bottom-*, inset-*
z-*, z-[*]
cursor-pointer, cursor-default, pointer-events-none
opacity-*, whitespace-nowrap, whitespace-pre-wrap, sr-only
transition-*, animate-*, duration-*
For each: count and note "keep as className"

## Patterns to Keep (NOT migrating — design system classes)
List classes that look like Tailwind but are actually mission-control design token utilities:
- bg-mission-control-*, text-mission-control-*, border-mission-control-* → KEEP
- text-success, text-error, text-warning, bg-success, bg-error, etc. → KEEP
- focus-ring, transition-fast, transition-normal → KEEP

## Arbitrary Values Found
Any className="[anything with brackets like w-[200px]]" — list them, these need style={{}} in Radix.
  </action>
  <verify>AUDIT.md exists, contains all 9 category sections, top 50 table populated, non-migratable section complete</verify>
  <done>AUDIT.md written with full categorized inventory grounded in real grep counts from the codebase</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] @radix-ui/themes in package.json dependencies
- [ ] AUDIT.md exists at .planning/phases/77-audit-migration-map/AUDIT.md
- [ ] All 9 category sections present with occurrence counts
- [ ] Top 50 patterns table populated
- [ ] Non-migratable patterns section complete
- [ ] Design system classes (mission-control-*) flagged as KEEP
</verification>

<success_criteria>
- package.json properly tracks @radix-ui/themes
- AUDIT.md is a complete inventory of Tailwind usage in the codebase
- Every heavy-hitter pattern (>50 occurrences) identified
- Non-migratable patterns explicitly flagged
- 77-02-PLAN.md can use this as primary input
</success_criteria>

<output>
After completion, create `.planning/phases/77-audit-migration-map/77-01-SUMMARY.md`:

# Phase 77 Plan 01: Tailwind Audit Summary

**[Substantive one-liner — e.g. "Catalogued 847 Tailwind utility patterns across 185 components, flagged 12 non-migratable categories"]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Ready for 77-02-PLAN.md
</output>
