---
phase: 54-gemini-key-server-side
plan: 02
type: execute
domain: security
---

<objective>
Remove localStorage fallbacks and VITE_GEMINI_API_KEY / NEXT_PUBLIC_GEMINI_API_KEY env var references from all voice and meeting components. After Plan 01, the DB is the canonical source — all components must fetch via settingsApi only.

Purpose: Multiple `getGeminiApiKey()` functions across 7 components still check localStorage as a fallback. If the user ever had their key in localStorage (written by old SettingsPanel), those functions will still use it. Removing the fallback ensures the key is always fetched server-side, never served from browser storage.
Output: All 7 components — MeetingsPanel, MeetingScribe, TeamVoiceMeeting, VoiceChatPanel, QuickActions, MeetingTranscribe, MeetingTranscriptionPanel — fetch Gemini key via settingsApi only.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-gemini-key-server-side/54-01-SUMMARY.md
@src/components/MeetingsPanel.tsx
@src/components/MeetingScribe.tsx
@src/components/TeamVoiceMeeting.tsx
@src/components/VoiceChatPanel.tsx
@src/components/QuickActions.tsx
@src/components/MeetingTranscribe.tsx
@src/components/MeetingTranscriptionPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getGeminiApiKey in MeetingsPanel, MeetingScribe, TeamVoiceMeeting, VoiceChatPanel</name>
  <files>
    src/components/MeetingsPanel.tsx,
    src/components/MeetingScribe.tsx,
    src/components/TeamVoiceMeeting.tsx,
    src/components/VoiceChatPanel.tsx
  </files>
  <action>
All four files have a similar `getGeminiApiKey()` function. Read each file to find the exact text, then for each:

**MeetingsPanel.tsx** — the function currently checks (1) VITE_GEMINI_API_KEY env var, (2) settingsApi.get, (3) localStorage fallback. Remove steps 1 and 3, keep only step 2. The simplified function should be:
```ts
async function getGeminiApiKey(): Promise<string> {
  if (_cachedGeminiKey) return _cachedGeminiKey;
  try {
    const { settingsApi } = await import('../lib/api');
    const result = await settingsApi.get('gemini_api_key');
    if (result?.value) { _cachedGeminiKey = result.value; return result.value; }
  } catch { /* ignore */ }
  return '';
}
```

**MeetingScribe.tsx** — same pattern, same fix.

**TeamVoiceMeeting.tsx** — same pattern, same fix.

**VoiceChatPanel.tsx** — the function is a module-level function (not inside a component). Reads first from `NEXT_PUBLIC_GEMINI_API_KEY` env, then `VITE_GEMINI_API_KEY`, then `settingsApi`, then localStorage. Remove env var and localStorage checks, keep only settingsApi. The function uses `import('../lib/api').then(m => m.settingsApi.get(...))` — keep that pattern.

For each file: read first to find exact text, then make targeted edits removing only the localStorage try/catch block and the env var check at the top of the function.

**What to remove from each function:**
- `const viteKey = import.meta?.env?.VITE_GEMINI_API_KEY || ...` lines
- `if (viteKey && ...) return viteKey;` lines
- `const nextKey = ... NEXT_PUBLIC_GEMINI_API_KEY ...` lines
- The `try { const s = JSON.parse(localStorage.getItem('mission-control-settings') || '{}'); if (s.geminiApiKey) ... } catch { }` block

**What to keep:**
- The module-level cache variable (`_cachedGeminiKey`, `_cachedScribeKey`, etc.)
- The `if (cached) return cached;` early return
- The `settingsApi.get('gemini_api_key')` call and its result caching
- The final `return '';` fallback
  </action>
  <verify>`npx tsc --noEmit 2>&1 | head -10` — zero errors</verify>
  <done>
- MeetingsPanel, MeetingScribe, TeamVoiceMeeting, VoiceChatPanel: getGeminiApiKey uses only settingsApi
- No localStorage or env var references remain in these functions
- TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix QuickActions, MeetingTranscribe, MeetingTranscriptionPanel</name>
  <files>
    src/components/QuickActions.tsx,
    src/components/MeetingTranscribe.tsx,
    src/components/MeetingTranscriptionPanel.tsx
  </files>
  <action>
Read each file first to find exact text, then apply fixes:

**QuickActions.tsx** — `getGeminiApiKey()` checks localStorage FIRST (wrong priority), then env var, then `settingsApi.get('geminiApiKey')` (note: wrong key name — should be `gemini_api_key`). Fix: remove localStorage block, remove env var check, fix key name to `gemini_api_key`:
```ts
const getGeminiApiKey = async (): Promise<string> => {
  try {
    const result = await settingsApi.get('gemini_api_key');
    if (result?.value) return result.value;
  } catch { /* ignore */ }
  return '';
};
```
Note: QuickActions imports `settingsApi` directly (not via dynamic import) — check the import at top of file and keep using the same import style.

**MeetingTranscribe.tsx** — line 33 uses `localStorage.getItem('gemini_api_key')` directly. Find the usage context (likely inside a function or useEffect). Replace with a `settingsApi.get('gemini_api_key')` call. The component likely calls this once before starting transcription — make that call async and await the API result. Also remove any JSX that checks `localStorage.getItem('gemini_api_key')` for conditional rendering (line ~302: `{(!import.meta?.env?.VITE_GEMINI_API_KEY && !localStorage.getItem('gemini_api_key')) && ...}`) — replace with a state variable that tracks whether the key is loaded.

**MeetingTranscriptionPanel.tsx** — line 31: `const apiKey = import.meta?.env?.VITE_GEMINI_API_KEY || localStorage.getItem('gemini_api_key');`. Replace with `settingsApi.get`. Since this is likely inside an async function, it can be awaited directly. Import `settingsApi` from `'../lib/api'` if not already imported.

For MeetingTranscribe and MeetingTranscriptionPanel: read the full context around these usages before editing — you need to see whether they're in a sync or async context and adjust accordingly.
  </action>
  <verify>
1. `npx tsc --noEmit 2>&1 | head -10` — zero errors
2. `grep -rn "localStorage.*gemini\|gemini.*localStorage\|VITE_GEMINI\|NEXT_PUBLIC_GEMINI" src/components/` — returns nothing
3. `npm run build 2>&1 | tail -5` — build succeeds
  </verify>
  <done>
- QuickActions: getGeminiApiKey uses settingsApi only, correct key name `gemini_api_key`
- MeetingTranscribe: localStorage.getItem('gemini_api_key') replaced with settingsApi call
- MeetingTranscriptionPanel: localStorage/env var replaced with settingsApi call
- Grep sweep: no localStorage or env var references for Gemini key anywhere in src/components/
- Build passes with zero TypeScript errors
- Phase 54 complete
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `grep -rn "localStorage.*gemini\|gemini.*localStorage" src/components/` — nothing
- [ ] `grep -rn "VITE_GEMINI\|NEXT_PUBLIC_GEMINI" src/components/` — nothing
- [ ] `grep -rn "localStorage.*gemini\|VITE_GEMINI\|NEXT_PUBLIC_GEMINI" app/api/` — nothing
- [ ] `npm run build 2>&1 | tail -5` — build succeeds
</verification>

<success_criteria>
- All 7 components fetch Gemini key via settingsApi only
- No VITE_GEMINI_API_KEY or NEXT_PUBLIC_GEMINI_API_KEY references in any component
- No localStorage reads for Gemini key in any component
- TypeScript compiles clean, build passes
- Phase 54 complete
</success_criteria>

<output>
After completion, create `.planning/phases/54-gemini-key-server-side/54-02-SUMMARY.md`:

# Phase 54 Plan 02: Gemini Key Server-Side — Component Layer Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

Phase 54 complete. Ready for Phase 55: csp-security-headers.
`/gsd:plan-phase 55`
</output>
