# Plan 03-01: Sidebar section grouping

## Objective
Add section dividers to the sidebar nav so 18 flat items get grouped into readable sections.

## Execution context
- File: `src/components/Sidebar.tsx`
- Nav items come from `panelConfig` (user-configurable, sorted by order)

## Tasks
1. Add `SECTION_MAP` constant mapping panel IDs → section labels:
   - Core: dashboard, inbox, kanban, approvals, notifications
   - Team: agents, chat, schedule
   - Work: projects, campaigns
   - Intelligence: analytics, knowledge
   - Tools: library, automations, modules, social

2. Restructure nav render to a single `navItems` array built first, then rendered with section headers injected when `SECTION_MAP[id] !== SECTION_MAP[navItems[idx-1].id]`

3. Expanded sidebar: show text label (`10px uppercase tracking-widest text-dim/50`)
4. Collapsed sidebar: show thin `border-t` divider between sections (skip for first group)

## Verification
- Build clean
- Expanded sidebar: section labels appear between groups
- Collapsed sidebar: dividers appear between groups (no text)
- New/custom panels without SECTION_MAP entry render ungrouped at bottom

## Status: COMPLETE (executed directly)
