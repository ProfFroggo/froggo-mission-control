# Plan 03-03: Empty state differentiation

## Objective
Create 3 distinct empty state tiers instead of 15 identical `icon + title + subtitle` patterns.

## Execution context
- File: `src/components/EmptyState.tsx`
- Key call site: `src/components/Kanban.tsx:1290`

## Tiers
- `'default'` (local empty) — no content matches current filter/context; existing visual
- `'global'` (first-time) — section has never had content; accent bg hint, brighter icon
- `'error'` (load failure) — something went wrong; error colors, AlertCircle icon override

## Tasks
1. Add `variant?: 'default' | 'global' | 'error'` to EmptyStateBaseProps interface
2. Import `AlertCircle` from lucide
3. In component body:
   - `global`: add `bg-mission-control-accent/5 border border-mission-control-accent/20 rounded-xl` wrapper, icon `text-mission-control-accent opacity-60`
   - `error`: override Icon → AlertCircle, icon `text-error opacity-70`, title `text-error`, desc `text-error/70`
   - Action button: `error` → color="red"
4. Update Kanban.tsx:1290 — `variant={activeFiltersCount > 0 ? 'default' : 'global'}`

## Verification
- Build clean
- Kanban with no tasks (first time) shows accent-tinted empty state
- Kanban with filters active shows neutral empty state

## Status: COMPLETE (executed directly)
