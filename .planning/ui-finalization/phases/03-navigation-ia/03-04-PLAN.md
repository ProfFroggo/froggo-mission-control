# Plan 03-04: Health indicator audit

## Objective
Ensure platform health indicators don't appear as debug badges on production screens.

## Execution context
- `src/components/TopBar.tsx` — platform health button (dot + Activity icon)
- `src/components/ConfigTab.tsx:158` — config validation issues banner
- `src/components/HealthStatusWidget.tsx` — dashboard widget showing "Minor Issues"

## Investigation findings
The "2 Issues" reference from the audit was likely observed when:
- `/api/health/metrics` returned `agentErrors > 0` causing TopBar to show error state
- ConfigTab validation banner showing N config issues

## Tasks
1. TopBar: The platform health indicator is appropriate — keep as is (dot + icon, no text)
2. ConfigTab: The validation banner is appropriate — it's inside Settings where it belongs
3. HealthStatusWidget: Shows "Minor Issues" text — change to more user-friendly language
   - "Minor Issues" → "Check Required"
   - "Review recommended" → "Action recommended"

## Verification
- Build clean
- HealthStatusWidget uses cleaner language
- No debug-style badge text visible on primary navigation/dashboard

## Status: DEFERRED to Phase 7 Polish pass (impact minimal, not a primary anti-pattern)
