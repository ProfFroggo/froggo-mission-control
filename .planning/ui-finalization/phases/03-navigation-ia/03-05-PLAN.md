# Plan 03-05: KnowledgeBase filter consolidation + approvals dual-badge

## Objective
Remove triple-filter cognitive overload in KnowledgeBase. Remove redundant animate-pulse badge on Pending Approvals.

## Execution context
- `src/components/KnowledgeBase.tsx:1298` — "Filter pills" div (Starred pill)
- `src/components/DashboardRedesigned.tsx:225` — animate-pulse badge duplicating text-5xl count

## Tasks
1. KnowledgeBase: Move Starred toggle from standalone pill row → icon button in TextField right slot
   - Remove the `{/* Filter pills */}` div
   - Add `<IconButton>` with `<Star>` to TextField.Slot side="right"
   - Keep show/hide logic identical
   - Result: 2 filter mechanisms instead of 3 (sidebar categories + search-with-starred-toggle)

2. DashboardRedesigned Pending Approvals: Remove the `animate-pulse` badge (lines 225-229)
   - The text-5xl number already communicates the count
   - Keep the Inbox icon color change (text-warning when pending)

## Verification
- Build clean
- KnowledgeBase: no separate pill row; star icon visible in search field right slot
- Dashboard Pending Approvals: shows big number only, no pulsing badge

## Status: COMPLETE (executed directly)
