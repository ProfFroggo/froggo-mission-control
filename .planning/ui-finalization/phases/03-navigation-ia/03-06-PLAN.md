# Plan 03-06: Agent card density reduction

## Objective
Collapse agent cards from 12-15 data points to surface layer: avatar + name + status + current task only.

## Execution context
- `src/components/AgentPanel.tsx` — agent card render at ~line 485

## Current card data points (12+)
1. Avatar + status dot ✅ KEEP
2. Name ✅ KEEP
3. Status badge (busy/disabled) ✅ KEEP
4. Circuit open badge ✅ KEEP
5. Description + current task ✅ KEEP
6. Available / queued count ✅ KEEP
7. AgentMetricsCard (nested component) ❌ REMOVE → AgentDetailModal
8. Token stats 7d ❌ REMOVE → AgentDetailModal
9. Last active timestamp ❌ REMOVE → AgentDetailModal
10. Capability tags (up to 3) ❌ REMOVE → AgentDetailModal
11. Trust tier badge (editable) ❌ REMOVE → AgentDetailModal

## Tasks
1. Remove AgentMetricsCard section (border-t block)
2. Remove agentTokenStats state, load effect, setAgentTokenStats calls
3. Remove last active timestamp section
4. Remove capability tags + trust tier footer section
5. Remove handleTrustTierSave function (no more trust tier on card)
6. Remove editingTrustTierAgent, pendingTrustTier state
7. Remove `import AgentMetricsCard`
8. Remove `analyticsApi` from lib/api import (was only used for agent metrics)
9. Remove unused lucide icons: Pencil, Check
10. Remove agentMetrics state + loadAgentMetrics function
11. Remove `agentTokenStats` from handleRefresh → just loadGatewaySessions

## Verification
- Build clean
- Agent cards show: avatar + name + status + description + current task/available/queued
- No 12-point card — compact, scannable

## Status: COMPLETE (executed directly)
