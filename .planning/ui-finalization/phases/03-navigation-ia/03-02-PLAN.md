# Plan 03-02: Campaign tab audit

## Objective
Reduce 11-tab campaign detail panel to ≤9 tabs by removing duplicate and rarely-used tabs.

## Execution context
- File: `src/components/campaigns/CampaignWorkspace.tsx`
- TABS array at line 32: overview, chat, tasks, timeline, assets, channels, performance, roi, comments, checklist, context

## Tasks
1. Remove `comments` tab — exact duplicate of `chat` tab (both use MessageSquare icon, same functionality)
   - Remove from TabId type
   - Remove from TABS array
   - Remove `import CampaignCommentsPanel`
   - Remove `{activeTab === 'comments' && ...}` render block

2. Merge `performance` + `roi` → single `results` tab
   - Replace both TABS entries with `{ id: 'results', label: 'Results', icon: BarChart2 }`
   - Replace both render blocks with a single `results` block that renders PerformanceTab + CampaignROIDashboard stacked
   - Remove `TrendingUp` from icon imports if unused

3. Result: 9 tabs (was 11)

## Verification
- Build clean
- Campaign workspace renders 9 tabs (no Comments, Performance/ROI → Results)
- Results tab shows both performance KPIs and ROI dashboard

## Status: COMPLETE (executed directly)
