# Plan 01-01: Token File Consolidation

<objective>
Eliminate the parallel token file `src/design-system/tokens.css`. The file is currently NOT imported
anywhere (confirmed by grep — zero references in all of src/). It is a latent risk: any future
`@import './design-system/tokens.css'` would cause active conflicts. Migrate all genuinely unique
tokens from `design-system/tokens.css` into `src/design-tokens.css`, then delete the dead file.

Note: `design-tokens.css` is the canonical source — it is imported via `src/index.css:15`.
</objective>

<execution_context>
@src/design-tokens.css
@src/design-system/tokens.css
@src/index.css
</execution_context>

<context>
## Discovery Findings

**Canonical file**: `src/design-tokens.css` — imported in `src/index.css:15`
**Dead file**: `src/design-system/tokens.css` — zero imports anywhere in `src/`

**Key conflicts (if the dead file were imported — document, then delete):**
- `--radius-sm`: canonical=`0.5rem` (8px), dead=`0.375rem` (6px) — canonical wins
- `--z-dropdown`: canonical=`1000`, dead=`100` — canonical wins (10x higher, correct for app)
- `--z-sticky`/`modal`/`popover`/`toast`/`tooltip`: canonical range 1100–1600, dead range 100–800 — canonical wins
- `--shadow-sm/md/lg/xl`: canonical uses darker rgba(0,0,0,0.3–0.5), dead uses lighter rgba(0,0,0,0.1) — decision: keep canonical (dark-first app)
- `--ease-out`: canonical=`cubic-bezier(0.16, 1, 0.3, 1)` (expo-out), dead=`cubic-bezier(0, 0, 0.2, 1)` (standard) — canonical wins

**Unique tokens in dead file to migrate into canonical (grep-verified as NOT already in design-tokens.css):**
- `--font-sans` / `--font-mono` — useful for explicit font references
- `--ease-linear` / `--ease-smooth` / (note: `--ease-bounce` stays deleted — see Plan 01-06)
- `--transition-base` / `--transition-hover` / `--transition-modal` / `--transition-slide` / `--transition-fade` — semantic shorthand
- `--blur-sm` / `--blur-md` / `--blur-lg` / `--blur-xl` / `--blur-2xl` — used in backdrop filters
- `--priority-p0/p1/p2/p3` and `-bg` variants — used in task/urgency components
- `--agent-coder/researcher/writer/chief` and `-bg` variants — used in agent role coloring
- `--radius-none` / `--radius-md` / `--radius-xl` / `--radius-2xl` / `--radius-3xl` — fills gaps in canonical radius scale
- Semantic radius aliases: `--radius-button`, `--radius-input`, `--radius-card`, `--radius-modal`, `--radius-badge`, `--radius-avatar`
- `--button-height-sm/md/lg` / `--button-padding-x-sm/md/lg` / `--button-gap` — component sizing
- `--input-height-sm/md/lg` / `--input-padding-x` / `--input-padding-y` — component sizing
- `--z-fixed` / `--z-modal-backdrop` / `--z-max` — missing from canonical z-index scale
- `--shadow-xs` / `--shadow-2xl` — missing from canonical shadow scale
- `--duration-instant` (50ms) / `--duration-slower` (500ms) — fills gaps in canonical duration scale
- `:root.light` shadow overrides — lighter shadows for light theme

**Do NOT migrate:**
- `--ease-bounce` — being deleted in Plan 01-06
- Card/modal/container width tokens — no evidence of usage, verbose, add only if grep shows usage
- Avatar size tokens — canonical already has `--avatar-sm/md/lg/xl` (different naming, skip)
- `--space-0` through `--space-24` numeric scale — canonical has semantic spacing tokens, skip
</context>

<tasks>
1. **Grep for actual usage of dead-file-unique tokens** — before migrating, confirm which are actually used:
   ```
   grep -rn "\-\-font-sans\|\-\-font-mono\|\-\-ease-smooth\|\-\-ease-linear\|\-\-transition-base\|\-\-transition-hover\|\-\-transition-modal\|\-\-transition-slide\|\-\-transition-fade\|\-\-blur-sm\|\-\-blur-md\|\-\-blur-lg\|\-\-priority-p\|\-\-agent-coder\|\-\-agent-researcher\|\-\-agent-writer\|\-\-agent-chief\|\-\-radius-none\|\-\-radius-md\|\-\-radius-xl\|\-\-radius-2xl\|\-\-radius-3xl\|\-\-radius-button\|\-\-radius-input\|\-\-radius-card\|\-\-radius-modal\|\-\-radius-badge\|\-\-button-height\|\-\-input-height\|\-\-z-fixed\|\-\-z-modal-backdrop\|\-\-z-max\|\-\-shadow-xs\|\-\-shadow-2xl\|\-\-duration-instant\|\-\-duration-slower" src/
   ```
   Migrate only the tokens that appear in actual usage.

2. **Add migrated tokens to `src/design-tokens.css`** — append a new section after the existing `:root {}` block (before the `.radix-themes {}` bridge block), grouped logically:
   - Add `--font-sans` / `--font-mono` to TYPOGRAPHY TOKENS section
   - Add missing duration tokens (`--duration-instant`, `--duration-slower`) to TRANSITION TOKENS section
   - Add `--ease-linear`, `--ease-smooth` to TRANSITION TOKENS section
   - Add `--transition-base/hover/modal/slide/fade` semantic shorthands to TRANSITION TOKENS section
   - Add `--blur-*` tokens to a new BLUR EFFECTS section
   - Add missing radius tokens (`--radius-none`, `--radius-md`, `--radius-xl`, `--radius-2xl`, `--radius-3xl`) to BORDER RADIUS TOKENS section
   - Add semantic radius aliases (`--radius-button`, `--radius-input`, etc.) to BORDER RADIUS TOKENS section
   - Add `--button-height-*` / `--button-padding-x-*` / `--button-gap` to COMPONENT SIZE TOKENS section
   - Add `--input-height-*` / `--input-padding-*` to COMPONENT SIZE TOKENS section
   - Add `--z-fixed`, `--z-modal-backdrop`, `--z-max` to Z-INDEX SCALE section
   - Add `--shadow-xs`, `--shadow-2xl` to SHADOW TOKENS section
   - Add `--priority-*` / `--agent-*` tokens to SEMANTIC COLOR TOKENS section
   - Add `:root.light` shadow overrides block after `:root {}` if light-mode shadows are needed

3. **Delete `src/design-system/tokens.css`**:
   ```bash
   rm src/design-system/tokens.css
   ```

4. **Check if `src/design-system/` directory is now empty and remove it**:
   ```bash
   ls src/design-system/
   rmdir src/design-system/ 2>/dev/null || echo "Directory not empty — review remaining files"
   ```

5. **Verify `src/index.css` does NOT import `design-system/tokens.css`** — confirm the import section still only has `./design-tokens.css`

6. **Run build verify**:
   ```bash
   rm -rf .next-verify && npm run build:verify 2>&1 | tail -20
   ```
</tasks>

<verification>
- `grep -rn "design-system/tokens" src/` returns zero results
- `ls src/design-system/` fails (directory removed) or shows only non-token files
- All migrated tokens (e.g. `--radius-md`, `--transition-base`) resolve correctly in browser DevTools
- `npm run build:verify` exits clean (no errors referencing token vars)
</verification>

<success_criteria>
- [ ] `src/design-system/tokens.css` deleted
- [ ] All actually-used tokens from dead file migrated to `src/design-tokens.css`
- [ ] `src/design-tokens.css` has exactly one canonical `:root {}` block with complete token set
- [ ] Build passes clean
- [ ] No TypeScript or CSS errors
</success_criteria>

<output>
Modified: `src/design-tokens.css` (additions only — new tokens appended to existing sections)
Deleted: `src/design-system/tokens.css`
Deleted: `src/design-system/` (directory, if empty)
</output>
